"""把 v7.9 的进展写进 Novel-Engine-Plan.md（头部状态 + 验收清单 + 修订记录）。

按文档纪律：所有替换先断言锚点唯一（count==1），一次脚本完成，
不用多次并行 Edit（会有 read-modify-write 竞态）。

修订记录表缺 v7.7/v7.8 两行（历史遗留：头部块有、表里没有），一并按序补齐。
"""

from __future__ import annotations

import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")

PATH = r"C:\regent\Novel-Engine-Plan.md"

V79_BLOCK = """> **v7.9 进展（B-06 闭合 / M2 浏览器旅程跑通 / M4 指标采集建成）**：B-06 以当前源码指纹在真实 PostgreSQL 上 23/23 通过（含跨作品遗留污染修复与依赖屏障反证，见 [pgverify 记录](docs/archive/pgverify-v7.9.1-20260909.txt)）。M2 用 agent-browser 在真实部署上跑通创建→澄清→方向→**开出章运行**（`POST /works/{id}/runs` 返回 202，run `RUNNING/ASSEMBLE/auto_advance=t`），并抓出三个只有真机能暴露的缺陷，全部修复+反证：①方向确认不幂等——双击最关键的「就写这个方向」得到 409 且前端中断、连运行都不开，改为同卡幂等回放已锁定路径、换卡仍是真冲突；②无章运行时编造 `QUEUED`——GET 入口返回假进度，前端把「有没有 progress」当「能不能开工」，永远显示「实时生成中」且开工按钮永不出现，改为 GET 返回 404 + 前端诚实空态（「方向已确定／开始第一章」）；③部分部署错位——手工维护的热补丁文件清单漏掉 `executor.py`，`POST /runs` 直接 500（`executor has no attribute 'executor_version'`），改为整树部署 + 逐文件 sha256 核对（391 文件，含多余文件检测）+ 跨模块符号体检（9 个耦合点显式断言），注入错位必须在重启前被拦下。M4 建立逐章指标采集与生长形状判定：24 章真实账（339 次模型调用，零额外成本）显示输入 token 涨幅 **1.636×** 而章号跨度 **4.556×**，对数拟合 R²=0.704 优于线性 0.481 → **次线性（有界）**，且第 14 章后回落稳定在 120–137k；同报告显式列出「证不了什么」（`novel_memory_items` 全库 0 行 → R3 从未在生产跑过、缓存命中全 0、未达 150 章、该作品早于 B-04/R3/R4）。详见 [M2/M4 记录](docs/archive/novel-m2-m4-2026-09-10.md)。**仍开放**：B-04 真实双策略灰度、R4 真实 pilot 与人评冻结、M2 无障碍检查与裁决/纠错恢复旅程、M4 的 50/100/150 章分级与 R3 首次生产验证、评分标准确认/真实人评/用户封测。"""

CHANGES = [
    # (old, new, 说明)
    ("> 版本：v7.8", "> 版本：v7.9", "版本号"),
    ("> 更新：2026-09-09", "> 更新：2026-09-10", "更新日期"),
    (
        "- [ ] B-04/B-06、M2/R4/M4：真实灰度、运行时认证、浏览器旅程、人评与长篇验收继续开放。",
        "- [x] B-06：绑定当前源码指纹的生产运行时认证（23/23）+ 真实成章入口 202。\n"
        "- [x] M2（主体）：浏览器旅程自动化建成并跑通创建→澄清→方向→开出章运行；刷新恢复、断网恢复取证。\n"
        "- [ ] M2（余项）：无障碍检查、裁决/纠错恢复旅程。\n"
        "- [x] M4（主体）：逐章指标采集 + 生长形状判定建成（24 章：次线性）。\n"
        "- [ ] M4（余项）：50/100/150 章分级、R3 首次生产验证。\n"
        "- [ ] B-04：真实双策略灰度。R4：真实 pilot 采样与人评冻结。",
        "v7.7 提出的入口验收清单",
    ),
    (
        "- [ ] B-04/B-06、M2/R4/M4：实际灰度、生产入口认证、浏览器旅程、真实盲评及长篇验收保持开放。",
        "- [x] B-06（v7.9 闭合）；M2/M4 主体闭合、余项与 B-04/R4 保持开放（见上方 v7.9 清单）。",
        "v7.4 优先待办的开放项",
    ),
    (
        "**B-01～B-05 已完成并验收；B-04/B-06 仍开放。**",
        "**B-01～B-06 已完成并验收；B-04 仍开放。**（B-06 于 v7.9 以当前源码指纹闭合：真实 PG 23/23 + 真实成章入口 202。）",
        "B 批次状态行",
    ),
    # 修订记录：在 v7.6 行之后按序追加 v7.7 / v7.8 / v7.9
    # （表里本来就缺 v7.7/v7.8 两行——头部块有、表里没有，历史遗留，一并补齐）
    (
        "前端接线恢复按钮；0055 真机认证通过并修复 0048 全新库安装缺陷。B-04/B-06（运行时认证）、M2/R4/M4 仍开放。 |",
        "前端接线恢复按钮；0055 真机认证通过并修复 0048 全新库安装缺陷。B-04/B-06（运行时认证）、M2/R4/M4 仍开放。 |\n"
        "| 2026-09-10 | v7.7 | 完备性复核（未改业务代码）：隔离探测提出 D-01～D-04 四个入口级闭环缺口（纠错输入、依赖屏障、记忆投影接线、终局预算硬门）。 |\n"
        "| 2026-09-10 | v7.8 | D-01～D-04 全部修复并通过反证：纠错语义穿过 ASSEMBLE 且重演去重/合并、后台领取加依赖屏障（只拦起跑不拦续跑）、六视角记忆投影下沉 domain 并接入 director_v2 全链、终局调用前剩余预算硬门；新增 `test_d_batch.py` 15 例，反证 `mutate_check_d.py` 退回旧行为 11 例次变红；308 项定向回归通过。 |\n"
        "| 2026-09-10 | v7.9 | B-06 闭合（当前源码指纹，真实 PG 23/23）；M2 浏览器旅程跑通并修复三个真机缺陷（方向确认幂等、无章运行不得编造 QUEUED、整树部署防错位），全部带反证；M4 逐章指标采集与生长形状判定建成（24 章真实账：上下文涨幅 1.636× vs 章号跨度 4.556× → 次线性），报告显式声明证据边界。仍开放：B-04 灰度、R4 真实人评、M2 无障碍与纠错恢复旅程、M4 分级验收。 |",
        "修订记录追加",
    ),
]

INSERT_AFTER = "> **v7.8 闭合**："


def main() -> int:
    with open(PATH, encoding="utf-8") as fh:
        text = fh.read()

    for old, new, label in CHANGES:
        n = text.count(old)
        if n != 1:
            print(f"[FAIL] 锚点不唯一（{label}: count={n}）: {old[:60]!r}")
            return 2
        text = text.replace(old, new)

    # v7.9 进展块插在 v7.8 闭合块所在行之后（另起一段）
    n = text.count(INSERT_AFTER)
    if n != 1:
        print(f"[FAIL] v7.9 插入锚点不唯一（count={n}）")
        return 2
    idx = text.index(INSERT_AFTER)
    end = text.index("\n", idx)
    text = text[: end + 1] + "\n" + V79_BLOCK + "\n" + text[end + 1 :]

    with open(PATH, "w", encoding="utf-8", newline="") as fh:
        fh.write(text)
    print("[OK] 计划文档已更新到 v7.9")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
