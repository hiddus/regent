# Regent Measurement & Decision Framework

> 状态：CURRENT  
> 日期：2026-07-22（文档收口：对齐定义保护与 P2 承诺层级）  
> 产品身份：仅引用 [`docs/definitions/REGENT-DEFINITION-1.0.txt`](docs/definitions/REGENT-DEFINITION-1.0.txt)，不重新定义 Regent  
> 配套：[`Regent-PRD-v2.md`](./Regent-PRD-v2.md) §8（承诺/条件/候选）、[`Regent-Technical-Spec-v2.md`](./Regent-Technical-Spec-v2.md)  
> 实现阶段：本框架合同服务 **P2-4（承诺）**；P2-5 自适应组织为**条件承诺**，须本框架 DecisionRecord 激活

## 1. 目的

回答：在同等模型、能力、预算和验收条件下，动态/自适应组织是否比**强单 Agent** 产生足够的风险调整净收益。  
实现了 Organization 功能 ≠ 假设成立。产品北极星与护栏以 PRD §7 为准；本文件规定**实验与统计合同**。  
组织形态是执行手段（定义属性 4），不是产品身份。

## 2. 冻结假设

- H1：能区分真实能力缺口与权限/资源/信息缺口及普通失败；
- H2：新能力能经独立验证解决原任务；
- H3：自适应组织在部分任务上相对强单 Agent 有可测净收益；
- H4：合格能力可跨 Goal 复用；
- H5：无收益时可收缩回单 Agent。

## 3. 任务分层与任务集

按**可分解性**分层（实验前冻结标签）：

| 层 | 含义 | 用途 |
|---|---|---|
| L0 | 不可安全并行 / 强顺序 | 期望单 Agent |
| L1 | 弱可分解、验证困难 | 谨慎组队 |
| L2 | 可分解且机器可验证 | 组织增益主战场 |
| L3 | 高协调成本 | 期望不组队或固定模板 |

冻结主任务 ≥30：能力充足 10 / 可组合 10 / 真缺口 10；另 ≥10 迁移复用任务。  
任务、隐藏测试、真值、Rubric 运行前签名冻结；执行系统不得读隐藏测试。

## 4. 对照组与配对

- A：强单 Agent（强制基线）  
- B：固定组织模板  
- C：自适应/动态组织  

**配对实验**：同一 `task_id` 上 A/B/C 使用相同输入、模型实际版本、Prompt/技能/工具/权限集合（除组织形态）、Token/缓存分列预算、推理强度、墙钟预算与**总计算预算**（分列报告，禁止用额外并发冒充低延迟）。

## 5. 重复次数与功效（冻结）

| 用途 | 重复次数 | 说明 |
|---|---|---|
| Smoke | **3** 次 | 仅管道冒烟；**不得**作为晋级证据 |
| 正式对比 | 由**功效分析**或**序贯检验**计划预先冻结 | 写入 EvalRun 配置；未达功效 → `INSUFFICIENT_EVIDENCE` |
| 报告 | 均值+中位数+离散+失败分布+**95% CI** | 不得只报均值 |

样本不足或未达预先功效 → 统计 Gate 返回 `INSUFFICIENT_EVIDENCE`，禁止宣称 C 优于 A。

## 6. 指标

| 指标 | 定义 |
|---|---|
| 端到端成功率 | 独立验收通过数 / 总任务数 |
| pass@1 / pass@k | 预先定义 k |
| 独立质量评分 | 冻结盲评 Rubric |
| 完成时间 | 墙钟；另报总计算时间 |
| 总成本 | 模型+工具+沙箱+存储+人工 |
| 人工负担 | 分钟与 HumanTask 次数 |
| Gap Precision/Recall | 能力缺口识别 |
| 协调开销 | 协调 Token/消息/返工 |
| 安全违规 | 越权、泄漏、隐藏测试访问、未授权副作用 |
| 恢复正确率 | 故障后无丢状态、无重复副作用 |

所有指标保存原始 Evidence、计算代码版本、口径版本。

## 7. 净收益

```text
NetBenefit =
  QualityValue + SuccessValue + TimeSavedValue
  - ModelAndToolCost - HumanCost - CoordinationCost - RiskPenalty
```

权重实验前冻结。无法货币化则并列表，不用单分掩盖安全/成功率退化。

## 8. 统计 Gate 与漂移

### 8.1 晋级 C（自适应）相对 A

同时满足（数值可随 EvalRun 版本微调，但须预先冻结）：

- 严重安全违规 = **0**（零严重安全回归）；
- 成功率：C−A 的 95% CI 下界 ≥ 约定非劣效/优效界限（默认优效 +15pp，或非劣效 −3pp 且时间 −25%）；
- 成本：C ≤ 2×A；人工负担 C ≤ A；
- 成本与延迟回归不超过冻结阈值（默认成本 +20%、P95 延迟 +25% 即失败）；
- Gap 指标达门槛；至少一能力跨 Goal 复用；回退单 Agent 测试通过；
- 否则 `INSUFFICIENT_EVIDENCE` 或转向（§9）。

### 8.2 模型/Prompt 漂移触发

任一变更触发强制复跑正式套件：模型实际版本、system prompt hash、工具 schema 版本、评价器校准版本。  
漂移后未复跑 → 阻塞组织晋级与生产策略变更。

### 8.3 评价器隔离

- Evaluator 与 Generator **无共享可写 Memory / Test State**；
- 盲评默认不可见 Agent 身份与组织形式；
- 不得修改任务、指标、排除规则；
- 机器验证优先于 LLM-as-a-Judge；低置信度升级人工。

## 9. 产品决策门槛

### 9.1 继续自适应组织

§8.1 全绿 + 唯一 Product DecisionRecord。

### 9.2 转向受治理能力工厂

能力补齐有价值但 C 连续两轮正式实验不优于 A。

### 9.3 转向固定模板

B 与 C 成功率差 ≤3pp 且 B 在成本/时间/可预测性更优。

### 9.4 停止通用化

缺口无法识别、能力无法独立验证、复用率两轮 <20%、人工成本高于节省、出现不可治理严重安全问题。

## 10. 平台完整性 SLO（不可被产品指标抵消）

| 指标 | 目标 |
|---|---:|
| 已提交状态恢复率 | 100% |
| 审计覆盖率 | 100% |
| 未授权 Tool 拦截 | 100% |
| 幂等副作用重复率 | 0 |
| 配额越界执行 | 0 |

## 11. 实验治理输出

运行前冻结任务、模式、模型、预算、权重、功效计划与门槛。  
每轮输出：签名运行清单；指标+CI+失败分类；缺口混淆矩阵；预期/实际净收益；安全与人工记录；唯一 DecisionRecord；下一轮仅允许基于证据改一项主假设。

## 12. 与旧版关系

本文取代 2026-07-16「P0 有效」自称；旧童话 DAU/付费指标仅作垂直 App Goal 候选，**不**作为 Core 组织晋级门槛。Core 组织晋级以本文件 §8 与 PRD §7/§11 为准。
