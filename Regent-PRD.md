# Regent 产品定义与需求文档

> 状态：CURRENT  
> 日期：2026-08-23（产品定位升级为“围绕经营目标持续工作的智能体团队”，并确立开放探索式进化；其余实现状态沿用 2026-08-03 基线；2026-08-23 登记组织修复脚手架——hub-and-spoke 纪律、规则式目标分类、运行时行为监测，均已实现但主链未接线）
> 性质：权威执行基线（Owner 批准）  
> 配套技术规范：[`Regent-Technical-Spec.md`](./Regent-Technical-Spec.md)  
> 测量框架：[`Regent-Measurement-Decision-Framework.md`](./Regent-Measurement-Decision-Framework.md)  
> 编码执行清单：[`Regent-Plan.md`](./Regent-Plan.md) §14；CD-0…5：[`docs/conversational-delivery-plan-2026-07-31.md`](docs/conversational-delivery-plan-2026-07-31.md)  
> **M6 状态（HALTED）**：[`docs/m6-canary-watch-plan-2026-08-01.md`](docs/m6-canary-watch-plan-2026-08-01.md)（窗记录 CLAMPED；扩流禁止）；内核：[`docs/agent-core-restoration-executable-plan-2026-08-01.md`](docs/agent-core-restoration-executable-plan-2026-08-01.md)  
> 对齐审计：[`docs/doc-implementation-alignment-audit-2026-07-31.md`](docs/doc-implementation-alignment-audit-2026-07-31.md) §8  
> 编码门禁：`P2StartDecisionRecord` 已签署 → **允许** `p2-scheduler-01`（该记录及 AAR1* DecisionRecord 签署于定义 1.0，属历史门禁；**当前编码规范源为 REGENT-DEFINITION-3.0**，见 `docs/decision-record-definition-supersession-2026-08-11.md`）；生产小比例 canary 须满足沙箱前置与窗记录；**GQ-4 正式晋级仍禁止**（需独立 DecisionRecord）

---

## 0. 产品定义

> **当前规范源**：[`docs/definitions/REGENT-DEFINITION-3.0.txt`](docs/definitions/REGENT-DEFINITION-3.0.txt)（`REGENT-DEFINITION-3.0`，冻结，不得修改；取代 2.0）
> CI 在定义 ID/哈希漂移或出现第二规范副本时阻止合并。

### 0.1 一句话定位与规范定义

> **Regent：围绕经营目标持续工作的智能体团队。**

规范定义正文（`DEFINITION_TEXT` 与 9 条 `ATTRIBUTE_*`）唯一存放于 [`docs/definitions/REGENT-DEFINITION-3.0.txt`](docs/definitions/REGENT-DEFINITION-3.0.txt)。按 3.0 文件第 4 行要求，本文只链接、不复述正文，以免构成第二规范副本；下文 §0.2 是对属性的执行口径解读，冲突时以 3.0 正文为准。

```text
P_t = Explore(G, S_t, R_t)                 # 自主生成并行探索组合
O_t = SelfOrganize(P_t, S_t)               # 团队按探索需要自行组织
A_t = Act(P_t, O_t)                        # 研究、实验、原型或现实行动

if ReversibleOrSandboxed(A_t):
    execute(A_t)                            # 默认开放
else:
    execute(A_t) only if Authorized(A_t)    # 按现实影响治理

S_{t+1} = Learn(S_t, A_t, Observation_t)   # 成功、失败和意外结果都更新认知
R_{t+1} = Reallocate(R_t, PortfolioValue)   # 在总资源池内自主再配置
```

其中：
- `G`：经营目标及其阶段性指标，经解释后形成版本化 `GoalSpec`。
- `C`：现实影响边界（法律、安全、隐私、不可逆副作用和明确禁止事项），不是对思考路径的约束。
- `V`：影响分级治理（身份权限、审计、Permit、合规检查和不可逆行动授权）。
- `R_t`：实时资源（Agent/技能/工具、数据/模型/算力、外部服务、人工窗口）。
- `S_t`：系统状态（目标/工作/执行/证据状态、能力注册表、组织运行状态、记忆）。
- `P_t`：同时存在的探索路径组合，包含已知收益、信息价值、长期潜力和未知可能性。
- `O_t`：团队当前组织形态，可随探索需要自行分化、组合和重组。

**Regent 的产品形态不是聊天助手、一次性工作流或多智能体开发框架，而是一支能够自由探索、持续实践、自我组织并从现实中进化的智能体经营团队。**

### 0.2 不可分割的恒定属性

1. **目标定方向，不定路径**：经营目标提供方向；团队可以质疑假设、重构问题和探索非预设路线。
2. **探索默认开放**：研究、模拟、讨论、原型、实验、创建临时角色和能力无需先证明必然有效。
3. **在实践中进化**：持续缩短“提出—尝试—观察—学习—再尝试”的循环，允许多条路径并行、组合和淘汰。
4. **团队是生命组织**：智能体可以自行分化、协作、重组和创造新角色，不预设固定成员数量和拓扑。
5. **证据用于学习**：Evidence 与 Observation 用于接触现实、比较路径和更新认知，不作为探索资格许可证；证据不足通常意味着继续探索。
6. **资源自治**：以总资源池、时间尺度、优先级和机会成本管理消耗，由团队自主配置探索资源，避免逐任务审批和僵硬 ROI 证明。
7. **边界落在现实影响上**：不可逆或高影响外部行动受权限、安全、合规和法律责任约束；思考、仿真、沙箱实验、可逆原型、复盘与组织设计默认开放。
8. **自由但不脱离现实**：重要行动、成本、结果和组织变化可追溯；团队必须从真实结果修正自己，人类或法定主体保留法律责任与接管权。
9. **长期连续、开放演化**：阶段目标结束不清零学习；团队可以跨情境重组经验，产生初始设计者未预见的能力和路径。

### 0.3 不变原则

1. 现实接触、探索开放、持续学习、自主组织；失败关闭只适用于越权或不可逆现实影响。
2. Generated App 与 Core 分离；业务模型不反向固化进内核。
3. PostgreSQL + Outbox + 不可变 Artifact + Permit + UNKNOWN 对账。
4. 内部 Smoke 不得满足产品价值 Gate。
5. Provider 不得注入缺失的业务功能。
6. 智能体团队是产品形态；成员与拓扑可以自由演化。系统记录成本和结果并促进自然选择，不以事前净收益证明限制探索。
7. 投票不能替代机器验证或真实 Observation。
8. 团队可以自评、互评和继续实验；不可逆生产发布必须满足对应现实权限，并保留独立验证或人类接管通道。
9. 禁止用源码字符串、类名存在或伪 Observation 代替行为验收。
10. 变更门禁：定义以当前规范源为准；如需修改则新建定义 ID，禁止原地修改。

### 0.4 产品边界

```text
regent/
├─ core/   # 通用自治组织运行内核（G/C/V/R/S/O 的通用实现）
└─ apps/   # Regent 创建和运营的独立应用（目标成果）
```

- Core 只表达目标、能力、组织、工作、执行、证据、策略和资源。
- 每个 App 拥有独立源码、依赖、数据、测试与部署，可以脱离 Core 运行。
- 具体内容类型、订阅方式和业务指标不得成为 Core 领域对象。

---

### 0.5 近期架构演进登记（2026-08-23）

> 本条登记 2026-08-13 之后落地的架构改动，属代码级底座，不立即改变对外产品语义。

近期提交引入一组"组织修复"底座模块，统一以规则与确定性服务实现，默认不额外调用 LLM 推断：

- **Hub-and-spoke 执行纪律**：`application/agent_invocation_guard.py` 通过 `config.max_subagent_depth=1` 落地（默认禁止 sub-agent 二次委派），并定义交付角色 `{product,tech,test,ux,ops}` 之间"只向编排器汇报、互不调用"的约束与跨部署调用环检测。其余 guard 逻辑已实现并单测，主链消费待接入。
- **规则式目标分类**：`application/goal_classifier.py` 依据目标规模 / 领域 / 复杂度 / 迭代需求 / 监测需求给出 `GoalProfile`（**规则驱动、不调用 LLM**），写入 `goal.metadata_json["goal_profile"]`；配套的 `application/organization_mode_selector.py` 据 profile 推荐 `WATERFALL / AGILE / HUB_SPOKE / BATCH` 组织模式。
- **运行时行为监测**：`application/runtime_behavior_monitor.py` 作为独立后台观察器，对已发布预览按指标（内容体量、对话真实感、角色多样性、世界观一致性）做异常检测；对 `domain=interactive-app` 额外做 **SPA JS 深度分析**（抓取脚本、统计角色 / 场景 / 周期 / 对话护栏 / 角色深度）。`behavior_repair_loop.py` 消费观测驱动修复：REPAIR 决策合并写入 steering（用户指令优先），并在护栏内（目标 ACTIVE、无存活 run、`org_mode.max_iterations` 上限、预算未 blocked）经用户 RESUME 同通道自动再调度；`behavior_monitor_tick.py` 在 worker 主循环按可配置间隔（默认 600s）周期重观测。

> **接线状态（2026-08-23 复核更正）**：上述模块**已接入主链**——commit `40e5378` 起目标分类于执行启动时调用并持久化 `goal_profile`，组织模式选择驱动 agile/hub_spoke/batch 直达生成旁路，行为监测接线于 `PREVIEW_DEPLOYMENT_SUCCEEDED` 回调（gate：`org_mode.enable_monitoring`）；同日补齐修复环自动再调度与 worker 周期监测 tick（见上条）。**仍未接线**：`agent_invocation_guard.check_cross_deployment_invocation`（已实现待接入）；组织模式为一次性选择，里程碑边界重评估未实现（见架构对比文档 R2）。本节不构成对外的夸大表述依据；与既有口径一致（见 Tech-Spec §0.1 / §25）。

- **执行编排器精简（2026-08-23）**：`execution_orchestrator.py` 移除 7 个旧处理器（requirement_validated / app_build_passed / reorganization_triggered / constraint_violated / organization_selected / adaptive_organization 等），并新增“直达生成”旁路（`_is_direct_generation_goal` / `_bypass_pipeline_to_generation`），缩短纯生成型目标的路径。

- **目标进化外环（Goal Revision Loop）（2026-08-25）**：`application/goal_revision_service.py` 在里程碑边界、交付失败用尽、修正累积、用户方向信号四个检查点，评估当前 GoalSpec 是否仍充分，若否创建进化版本（旧版 SUPERSEDED）。与内环（GoalAnchor 防漂移）构成两层稳态架构。`feedback_service.py` 新增 `PIVOT` 决策类型（重复 REVISE 未通过 gate → spec 本身可能不足）。GoalAnchor 拆分为方向锚点（稳定）与规格锚点（可进化），`validate_goal_alignment` 支持 spec_text 作为主要对齐目标。FROZEN 检查放宽为只验证“当前最新 spec 是 FROZEN”，允许外环进化后更新 locked 指针。

---

## 1. 当前阶段产品使命

当前阶段把产品定义落实为一支可治理、可审计、可恢复、能持续工作的最小智能体经营团队。P1/P2 先在互联网产品创建与经营场景闭合端到端能力：理解经营目标、连接数据、发现问题与机会、形成策略、分工执行、生成和运营独立应用，并根据真实经营证据继续、修订、扩张、收缩或停止。

**当前差异化**：聊天助手等待人逐项派单，工作流执行预设步骤，多智能体框架向开发者提供编排组件；Regent 面向经营负责人交付一支持续工作的智能体团队，把经营目标转化为可追溯、可验证、可迭代的**经营闭环**：`经营目标 → 经营观测 → 问题/机会 → 策略 → 分工执行 → 外部结果 → 经营决策`。产品创建只是当前首条验证路径，不是最终产品边界。

### 1.1 核心产品能力

| 能力 | 产品要求 | 最小可验收结果 |
|---|---|---|
| 经营目标合同 | 将自然语言目标解释为指标、口径、基线、期限、预算、约束、授权和停止条件 | Goal Owner 能审阅和修订；指标口径版本化 |
| 经营状态感知 | 持续接入获授权的业务系统、数据源、用户反馈和外部环境信号 | 每项 Observation 有来源、时间、完整性和可信度记录 |
| 问题与机会发现 | 主动识别指标偏差、异常、风险和增长机会，而不是等待用户逐项派单 | 每个候选项包含证据、影响估计、紧迫度和置信度 |
| 策略与计划 | 根据经营目标形成阶段策略、行动组合、依赖、预算和预期影响 | 策略可解释、可版本化；重大变更进入相应治理 Gate |
| 团队组建与分工 | 智能体可根据目标和实践自主形成、分化、合并、退出角色及改变协作关系 | 组织变化可观察；涉及新增现实权限时按影响级别治理 |
| 持续行动 | 调用工具和业务系统推进任务，跟踪状态，处理失败并跨中断恢复 | 行动幂等、可恢复；外部副作用受 Permit 和策略约束 |
| 结果验证与调整 | 将行动与外部经营结果关联，判断继续、修订、扩张、收缩、停止或移交 | 不以任务完成或 Agent 自评替代经营结果；形成 DecisionRecord |
| 经营记忆与能力进化 | 沉淀有效策略、失败教训、业务知识、能力和组织经验 | 后续周期可检索复用；变更有版本、评测、回滚和审计记录 |
| 人类治理与接管 | 人可查看团队为何行动、花费多少、产生什么结果，并在关键节点批准、纠偏、暂停或接管 | 权限、资金、安全、合规和根目标变更不得越权执行 |
| 资源与可能性组合 | 同时管理模型、工具、算力、人工成本和探索组合，不因短期 ROI 不确定而提前淘汰高潜路径 | 总资源消耗可见；团队能自主再分配资源并说明机会成本 |

### 1.2 产品承诺与当前实现边界

- **3.0 愿景**：围绕经营目标持续工作的智能体团队；团队可以长期感知环境、自主形成探索组合、组织行动，并从可验证经营结果中持续学习和演化。
- **当前可交付能力**：受治理的目标执行、应用生成、验证、预览发布与初步 Observation/Decision 闭环。持续在线经营学习、自由拓扑的生产权限继承和成熟行业经营模型尚未完成，不得用愿景替代当前能力说明。
- **当前商业产品**：面向互联网产品增长经营的限定范围付费试点。客户购买的是一个有明确指标、预算、授权动作、周期和验证报告的经营闭环，不是“无限自治”或一次性生成完整产品。
- **当前尚不能宣称**：已能无人经营完整景区、完整智慧城市或任意大型互联网业务；已证明智能体团队在所有任务上优于强单 Agent；已形成可复制的垂直行业经营模型。
- **阶段演进原则**：先对一个可度量经营目标负责，再覆盖一个业务职能，随后扩展为跨职能团队，最终才承担复杂实体的整体经营目标。

---

## 2. ICP、购买者、主用户与首发场景

### 2.1 目标市场与首发范围

**长期目标场景**：景区、大型互联网产品、智慧城市等需要多职能协作、持续观测和长期改进的复杂经营项目。它们属于公司愿景，不构成当前统一销售范围；智慧城市不进入首期产品承诺、案例或近期收入预测。

**滩头市场（冻结）**：互联网产品增长经营。首期围绕一个可控、可测的增长目标，持续完成“观测数据—发现机会—形成假设—执行获授权实验—验证增长结果—调整组合”。应用创建只是可调用能力，不是首发产品本身。

> 首期只接受具有可信事件数据、明确指标负责人、可建立基线或对照、且允许开展低风险实验的互联网产品。

**首批支持**：增长诊断、漏斗与留存机会发现、内容/触达策略、低风险页面或流程实验、经营周报，以及为上述行动生成的内部工具或 Web 增量。
**明确排除**：医疗诊断、金融交易、招聘/信贷决策、未成年人数据、高风险基础设施控制、大规模 PII 处理、支付清算。

### 2.2 第一批核心用户（ICP）

| 角色 | 是谁 | 痛点 |
|---|---|---|
| 购买者（Buyer） | 有存量互联网产品和增长压力的业务负责人、产品负责人或增长负责人 | 希望持续发现并验证增长机会，减少跨产品、运营、数据和研发的人工推进成本 |
| 操作者（Operator） | 增长、产品、运营或技术负责人 | 需要设定边界、观察团队工作、批准高风险动作并衡量经营结果 |
| 经营目标负责人（Goal Owner） | 提出目标并对指标口径与治理边界负责的人 | 对经营结果、预算、权限和关键决策负责 |
| Product User | 生成 App 的真实外部或内部试用用户 | 其行为才是产品证据 |

### 2.3 首个高频问题

「我给出一个明确的经营目标后，怎样获得一支持续工作的智能体团队，让它主动发现问题、组织执行，并用真实业务数据证明是否改善了结果？」

### 2.4 首期付费试点与商业包装

首期以 **6–8 周付费试点**交付，不以一次演示或一次生成结果替代经营验证。每个试点限定：

1. 一个互联网产品、一个主要增长目标和 2–4 个护栏指标；
2. 2–3 个只读数据连接器，以及一组明确授权的低风险行动；
3. 一份经双方确认的 Goal Charter、资源上限和最大可接受损失；
4. 每周经营简报、完整探索组合、行动与证据记录；
5. 试点结束时给出 `EXPAND / REVISE / STOP / HANDOFF` 结论，不以必须获得正向增长作为唯一成功条件，但必须产生可验证经营学习并说明成本和归因可信度。

**定价建议（商业假设，签约前按客户复杂度报价）**：

- 一次性接入与 Goal Charter 设计费；
- 6–8 周智能体经营团队服务费，覆盖平台、运营支持和约定资源池；
- 超出资源池的模型、第三方工具、广告或人工费用按实结算并预警；
- 可选结果奖金只绑定双方预先冻结且可归因的增量指标，不采用纯 Token、Agent 数量或无法归因的纯效果收费。

正式报价必须列明包含项、资源上限、超额单价、第三方费用、税费、暂停规则和退款/提前终止规则。

### 2.5 首次价值与试点服务水平

在客户按约提供可用数据、账号与责任人的前提下，首期服务目标为：

| 时间 | 客户可见结果 |
|---|---|
| 1 个工作日内 | Goal Charter 草案、数据与权限缺口、当前基线可计算性判断 |
| 3 个工作日内 | 首份经营体检、首批机会组合、风险与预期验证成本 |
| 7 个自然日内 | 至少完成一次影子验证、离线分析或获授权的低风险行动，并给出结果 |
| 每周 | 目标趋势、已验证学习、资源消耗、风险、下一周期组合与待决策事项 |
| 6–8 周结束 | 指标变化与归因报告、有效/无效路径、能力与资产清单、扩大/修订/停止/移交建议 |

试点 SLA 至少约定：控制台与任务服务可用性、数据刷新频率、严重事件通知和响应时间、人工接管窗口、备份恢复目标及维护窗口。具体数字写入客户合同；在基础设施和支持流程未通过生产验收前，不得宣传企业级 SLA。

产品必须跟踪 `TimeToFirstInsight`、`TimeToFirstAuthorizedAction`、`TimeToFirstVerifiedLearning`、客户人工介入时长和每次可验证学习成本。

---

## 3. RACI

| 活动 | Goal Owner | Operator | Capability Provider | Product User | 独立评价者 | 安全/合规签署 |
|---|---|---|---|---|---|---|
| 确认 GoalSpec | A/R | C | I | I | I | C（高风险） |
| 授权外部证据源 / Permit | A | R | C | I | I | C |
| Discovery / Generation 执行 | I | C | C | I | I | I |
| Preview 核心任务验收 | C | R | I | A | C | I |
| Gate / IterationDecision | A | R | I | I | R（盲评） | C |
| 生产发布批准 | A | R | I | I | I | VETO |
| 能力包认证与撤销 | I | C | R | I | C | A |
| P1 Graduation 签署 | C | R | I | C | R | A |

图例：R=执行，A=问责，C=咨询，I=知会，VETO=独立否决权。

### 3.1 Goal Charter（经营委托书）

> **实现状态（2026-08-10）：已定义 / 部分接线，未端到端。** `application/goal_charter.py` 提供契约与包络校验，`api/goals.py` 在创建 Goal 时校验并写入 metadata；**尚未被执行编排器/调度器作为治理包络消费**。本节描述目标契约要求，不代表运行时已强制执行。

任何 Goal 发布后先保持 `DRAFT`，必须通过多轮对话逐项确认目标价值与责任人、结果与验收、范围内外、依赖与环境、权限与风险、分析/执行预算及候选路径。任何连接真实客户数据或持续消耗付费资源的试点，还必须由客户与 Regent 共同确认 Goal Charter。确认后的授权包络内无需逐任务审批；越出包络则退回 `DRAFT`，进入新的可行性评估、授权或修订流程。Goal Charter 至少包括：

- 主指标、护栏指标、口径、基线、对照或归因方法、观察窗口；
- 可控动作目录、禁止项、数据源、敏感级别、影响对象和允许触达范围；
- 总预算、探索/利用资源比例、并发上限、单次与累计最大可接受损失；
- 自动执行、通知后执行、前置审批和绝对禁止四类动作；
- 客户 Goal Owner、Operator、Regent 服务负责人、安全联系人和升级路径；
- 成功、有效学习、暂停、停止、移交和责任范围扩大的判定条件。

边界确认后必须形成 FeasibilityReport，并给出 `FEASIBLE / REVISION_REQUIRED / NOT_FEASIBLE`。只有 `FEASIBLE`、Goal Owner 明确确认报告与 Goal 版本、分析预算未越界且所需 Goal Charter/Permit 满足时，Goal 才能锁定为 `FROZEN` 并进入正式执行。未过闸仅允许预算内追问、只读检查、估算、设计推演和明确授权的隔离验证；禁止业务编码、生成或修改 workspace、启动正式 Worker、生产写入和对外触达。具体决策见 [`docs/decision-note-minimum-start-continuous-correction-budget-stop-2026-08-13.md`](docs/decision-note-minimum-start-continuous-correction-budget-stop-2026-08-13.md)。

### 3.2 责任、数据/IP 与退出

- **客户责任**：经营目标和指标口径的业务合法性；其提供数据、账号、素材和指令的授权；依法必须由客户承担的最终业务决策和法定责任。
- **Regent 责任**：按 Goal Charter 和合同运行服务；保护凭据与数据；执行授权、审计、预算和停止机制；如实报告能力、失败、成本和证据，不把 Agent 自述当作经营结果。
- **共同责任**：归因假设、实验伦理、对真人产生影响的评估、事故演练、重大方向变更和责任范围扩大。
- **数据与 IP**：客户原始数据、客户业务规则、客户专属策略和经营结果默认归客户并保持租户隔离；Regent 平台及既有通用能力归 Regent。任何跨客户复用必须经过明确授权、脱敏、来源记录和重验证。
- **保密与训练**：默认不得使用客户数据、提示、产物或经营轨迹训练跨客户模型；例外必须单独书面选择加入。
- **退出与移交**：客户可暂停、终止和导出 Goal Charter、经营报告、证据索引、行动记录、配置和客户资产；合同规定数据保留期、删除证明、第三方订阅处理和未完成外部行动的对账。停止服务不声称自动回滚已发生的现实副作用。
- **事故与赔付**：严重事件分级、通知时限、处置责任、证据保全、责任上限和赔付安排由合同明确；产品文案不得把“客户最终负责”解释为 Regent 对越权、失控或数据保护失责免责。

---

## 4. 用户旅程

每一步必须向用户展示：**当前状态解释**、**已有证据引用**、**下一步可操作命令**。

### 4.1 成功路径

> 既有快照启动机制继续作为执行实现，但真实客户试点受本节与 §3.1 Goal Charter 约束；发生冲突时，不得用历史自动启动决定绕过经营委托。

1. 创建意图 → 可解释 GoalSpec（显式约束、推断、未知项、非目标）。
2. **经营委托**：真实客户试点先确认 Goal Charter，再建立可自动执行的授权包络。纯沙箱演示或不使用客户数据、不会持续产生付费资源消耗的内部任务，才可由机器身份写入执行快照并启动。
3. **包络内自治、随时纠偏**：系统在已确认范围内无需逐任务审批；用户随时可修订约束、范围、资源或停止。越出包络、高影响行动、根目标变更和生产发布仍须显式 HumanTask / 审批。
4. Evidence → 实质不同假设 → 唯一 HypothesisDecision。
5. Requirement → Capability Resolution → Generation → 隔离 Build → Preview。
6. 真实 Observation → Gate → CONTINUE / REVISE / STOP。

Goal Charter 的确认是一次性的经营委托，不是逐任务门闩。确认后，控制台「确认」卡主要承担纠偏、知情和新增授权；`/confirm` 端点保留给对话修订路径。

### 4.2 失败与例外路径

| 场景 | 用户可见结果 | 下一步 |
|---|---|---|
| GoalSpec 需修订 | SUPERSEDED 旧版，新 DRAFT | 编辑后重新冻结 |
| Evidence 不足或冲突 | RESEARCH_MORE / BLOCKED | 授权来源、绑定能力或停止 |
| 人工审批节点（发布 / 质量 / 外部效应 / Permit） | WAITING_HUMAN + HumanTask | 批准 / 拒绝 / 超时；可对真确认卡使用「总是允许」 |
| 交付缺口软暂停 / 进度停滞提示 | 对话提示 + `DELIVERY_SOFT_PAUSE`（或等价） | 用户补充方向后系统续跑，或停止；**非**审批卡 |
| BLOCKED / UNKNOWN / 超预算 | 明确 failure_code | 对账、扩预算、取消 |
| 暂停 / 恢复 / 取消 | PAUSED / ACTIVE / CANCELLED | 取消不回滚已发生副作用 |
| Preview 拒绝 | 用户拒绝或 Journey 失败 | REVISE 或 STOP；保留证据 |

### 4.3 控制台交互体验

Web Console 是主交付面；下列为产品语义，不规定具体组件实现。

默认信息架构面向经营负责人，而不是面向 Agent 调试者。首期必须提供以下六个客户界面：

1. **Goal Charter**：审阅目标、指标、基线、归因、预算、动作等级、禁止项、责任人与停止条件；清楚区分客户明确输入、系统推断和未知项。（实现状态：契约与 API 校验已定义，控制台界面与治理包络消费未端到端，见 §3.1）
2. **经营驾驶舱**：默认首页；展示目标与护栏趋势、本周期关键学习、投入与剩余预算、风险、下一动作和待客户决策。不得用任务完成率或 Agent 活跃度替代经营结果。
3. **探索组合**：按经营假设而非按 Agent 展示路径；每条路径包含理由、阶段、预算、证据、最新结果、下一验证以及继续、组合、暂停或停止状态。相似项必须聚合去重，并显示探索/利用资源比例与并发占用。
4. **行动中心**：集中呈现需要通知或审批的现实动作，说明影响对象、数据敏感度、可逆性、波及范围、费用、回滚/补偿方案和授权有效期；支持批量策略授权，不制造逐任务审批疲劳。
5. **团队视图**：展示当前角色、职责、协作关系、现实权限、成本和最近贡献；作为二级界面，不以角色数量或消息数量制造“团队正在工作”的错觉。
6. **审计与移交**：可按时间、目标、假设、行动和责任人检索证据；支持暂停、接管、导出、删除请求、退出清单和未完成外部动作对账。

7. **对话流动态进度**：执行过程中默认按经营事件聚合，仅主动突出异常、预算偏差、关键学习、外部动作和待决策项；Agent/工具原始轨迹放入可下钻的审计模式。节点结束后默认收敛为概览，用户可主动切换详略。
8. **工作区证据面（2026-08-03）**：原清单 / 运行 / 改动 / 预览 / 审阅作为执行下钻工作区保留，不作为经营默认首页；参与 Agent 名册与活动流在团队视图或运行下钻中展示。预览就绪时可首次自动切到预览 Tab。停止始终是一等控件，批准/拒绝仅在行动中心或对话闸门卡完成。
9. **与 Core 信号对齐**：名册与活动态应对齐 Core 提供的参与 Agent 列表及实时活动信号（含 live 活动摘要）；无列表时允许控制台用既有 Goal/组织信息做最小回退，但不得编造未部署的 Agent。Core 已在 guidance status 中提供 `agents` 时，控制台**必须**优先使用该列表，不得长期运行在推导回退分支。
10. **可审阅交付物**：用户须能查看本轮经营行动与产物摘要、验证结论和成本/剩余预算；涉及软件改动时再展示文件、smoke/测试日志摘要。仅预览 URL + zip 不足以宣称「完整交付」。审阅默认结构化卡片，裸 JSON 仅作高级展开。
11. **失败交人带答案**：当且仅当需要**真审批或主观产品判断**（发布、质量门、外部效应、Permit、goal_intent 等）时，交人界面须提供 2–3 个可执行选项及大致代价（轮次/预算），不得只给「允许 / 拒绝」而把开放式难题甩回用户。闸门卡统一为同一视觉族（计划批准 / 权限 / 问人 / 恢复）。
12. **人工介入边界（2026-08-01）**：人处理目标/指标口径、权限、危险效应和必须由法定主体决定的事项。授权包络内的交付缺口、进度停滞与自动修复属于系统自愈 / 对话续跑路径，不得要求用户反复点「允许」；越出 Goal Charter 则必须停在对应动作前请求修订或授权。
13. **停止一等**：Abort/停止控件须在运行控制区始终可发现；不得依赖翻开折叠面板才能停止。

### 4.4 对话式完整交付（新增需求）

> 本条为**新增产品条目**（见架构评审 §9.4），不是对既有 bug 的修复说明。编码前须遵循 [`docs/conversational-delivery-plan-2026-07-31.md`](docs/conversational-delivery-plan-2026-07-31.md) CD-4 门禁。

目标体验：用户通过自然对话让 Core **完整交付**可验证结果，接近 Claude Code / WorkBuddy 的「边说边做、过程可见、预算内自纠正」。

产品要求：

1. **单一自然语言入口**：用户消息应进入同一对话执行环；不得长期并存一条「可执行」入口与一条「纯 CRUD 死路」而不在文档中标明。
2. **对话层具备多步工具循环**：对话编排不得止步于单次意图分类枚举；须能在同一轮用户意图下多步澄清、调用已注册工具（含现有 guidance handler 升格为工具）并推进执行——状态写入仍由确定性 Application Service 完成。
3. **过程可见**：生成/修复过程须展示具名工具轨迹（读/写/命令与结果摘要），而非仅轮次计数。
4. **上下文可引用**：对话历史与相关 Evidence 须可被检索进入执行上下文（压缩与预算约束见 §10.4）。
5. **治理不降级**：沙箱内可逆效应可事后审计全速循环；不可逆/外部效应仍须前置 ExecutionPermit。不得为了顺滑删除 Outbox / Evidence / Audit / Reconciler。

验收锚点（摘要；细节见统一开发计划 CD-3/CD-4）：用户一句话可触发多步执行；工具轨迹可观测；失败交人带选项与产物；交付物可审阅。

### 4.5 边跑边想：方案可见与有限选项拍板（2026-08-02）

> 产品方向见 [`docs/direction-note-run-think-learn-2026-08-02.md`](docs/direction-note-run-think-learn-2026-08-02.md)；前置可行性与成本治理见 [`docs/decision-note-minimum-start-continuous-correction-budget-stop-2026-08-13.md`](docs/decision-note-minimum-start-continuous-correction-budget-stop-2026-08-13.md)。本节“边跑边想”只适用于 Goal 已取得 `FEASIBLE`、经用户锁定并进入正式执行之后；不得解释为 Goal 发布后立即编码或启动 Worker。

1. **方案可见**：意图进入后，控制台展示拟议方案（理解、步骤、未知项），消息类型 `GOAL_PLAN_PROPOSED`，而非仅 JSON 元数据。
2. **人辅助决断**：前置阶段按主题多轮确认边界；每轮提供聚焦问题或 2–4 个有限选项，并回显已确认/待确认内容。用户选择后经 guidance `SELECT_OPTION` 写回 GoalSpec / metadata。边界未确认或可行性未通过时，只能继续分析/追问，不得进入正式执行。
3. **失败即经验，但预算是硬边界**：生成失败与交付缺口写入 `failure_lessons`，后续轮次注入 acceptance / 生成上下文；经验沉淀是必要条件，不构成超预算授权。单步、单次 Run 或 Goal 累计硬上限命中后必须停止付费执行，保存恢复点并请求预算或范围修订。
4. **运行中价值校验**：每个高成本步骤必须关联交付物、待验证假设、风险或用户决策之一；无新证据的同策略重试、重复读取或角色转述不得继续消耗预算。正式执行中若目标、验收、范围、数据、权限、预算或不可逆影响实质变化，立即退回 `DRAFT` 并重新过可行性闸门。

---

## 5. P0 能力与验收

### 5.1 P0 核心能力

1. 保存原始 Goal，生成版本化 GoalSpec，区分显式约束、系统推断和未知项。
2. 持久化 Goal、Work、Run、Artifact、Evidence、HumanTask 和 Audit，进程重启后恢复。
3. 从 Goal 与计划推导能力需求，区分能力、权限、资源、信息缺口和普通失败。
4. 通过复用、配置、组合、构建或请求人类补齐能力。
5. 根据能力覆盖组建最小组织，并能收缩、替换和解散。
6. 在 `apps/<app-id>` 创建、构建和测试独立 App。
7. 使用外部 Observation 和 Evidence 评价进展并重规划。
8. 所有副作用行动经过策略判断和一次性 ExecutionPermit。
9. 人工输入与审批使用独立 HumanTask，等待期间不占 Worker。
10. 支持暂停、恢复、取消、预算限制、无进展停止和完整审计。

### 5.2 P0 固定验收

```text
名称：CSV_SUMMARY_BASELINE
Goal：读取授权目录中的 orders.csv，生成 summary.json。
数据：1,12.50 / 2,7.50 / 3,INVALID / 4,10.00
约束：禁止联网；不得修改输入；只能写入 output/。
输出：{"row_count":4,"valid_count":3,"invalid_count":1,"total_amount":30.0}
```

自动验收必须证明：原始 Goal 保存；约束与推断分离；形成 Work 和 Run；输出逐字段相等；Evidence 包含输入与输出哈希；Worker 中断后恢复；幂等重放不产生第二份输出。

```text
名称：EVT_PARSER_GAP
输入：timestamp|category|value|crc32，共 6 行，1 行 CRC32 错误
预置能力：无 EVT Parser
输出：valid_count=5, invalid_count=1
约束：断网；fixtures/ 只读；只写 output/
```

### 5.3 P0 完成定义

P0 作为整体完成，必须同时满足：

1. Core 在空 Apps 条件下通过 `CSV_SUMMARY_BASELINE`；
2. 仅凭普通 Goal 形成可解释的最小组织，补齐至少一个能力缺口，并通过 `EVT_PARSER_GAP`；
3. 在独立 Apps 目录创建可运行产品候选，新 App 接入不改变 Core 领域模型；
4. 运行可恢复、副作用幂等、高风险行动受控，状态、Evidence、Permit 和审计可追溯；
5. 完成 A/B/C 冻结任务集的首轮对照实验，并形成产品 DecisionRecord。

---

## 6. P1 产品创建与运营

### 6.1 P1 目标

让 Regent Core 在未知具体产品实现的前提下，从 Goal 和治理约束出发，自主完成证据发现、产品假设比较、需求修订、能力解析、应用生成、隔离构建、发布和观测闭环。

### 6.2 强制边界

- Core 预置治理和生成机制，不预置各种垂直 App 功能。
- 生成结果必须由版本化需求、证据引用、生成计划和文件变更集共同约束。
- 构建与发布均视为受治理副作用，必须可幂等、可恢复、可审计。
- 未知外部结果不得推定成功；必须进入 UNKNOWN 并通过查询或对账收敛。

### 6.3 P1 Graduation 验收矩阵

未满足本矩阵时产品状态为 `P1_GRADUATION_REQUIRED`，不得把剩余工作改名为 P2。

| 层级 | 含义 | 最低门槛 |
|---|---|---|
| `SYSTEM_GRADUATED` | 系统主链、构建、治理与耐久副作用可证 | G1–G5、G8–G12 签署 |
| `PRODUCT_EVIDENCE_GRADUATED` | 演进闭环可证（外部结果 → 决策 → 调整） | G6–G7 签署 |

#### SYSTEM_GRADUATED

| ID | 验收项 | 成功阈值 | 失败结果 |
|---|---|---|---|
| G1 | 事前未知 Goal | ≥3 个独立 Goal 全部到达 Decision | FAIL_KNOWN_GOAL |
| G2 | 合法外部证据 | 决策引用 EvidenceRef | FAIL_NO_EVIDENCE |
| G3 | 实质不同假设 | ≥2 假设，差异维度可机器列出 | FAIL_NO_DIFF |
| G4 | Requirement 驱动生成 | Plan 哈希含 Requirement 摘要 | FAIL_TEMPLATE |
| G5 | 可复现构建 | 哈希一致且 PASSED | FAIL_BUILD |
| G8 | Durable 副作用 | 0 重复副作用；UNKNOWN 在 15 min 内收敛 | FAIL_SIDE_EFFECT |
| G9 | 凭据治理 | 0 明文密钥 | FAIL_SECRET |
| G10 | 质量门禁 | Ruff/mypy/Pytest/迁移全绿 | FAIL_CI |
| G11 | 发布基线 | 可回滚上一 tag | FAIL_RELEASE |
| G12 | 证据包 | 100% 可解析 | FAIL_PACK |

**G0 前置（阻塞 P2）**：最小 `ExternalOperation + Permit 原子消费 + operation_key 幂等 + 恢复/对账` 必须在 G8 之前合入。

#### PRODUCT_EVIDENCE_GRADUATED

> 废除强制 ≥7 天观察窗。不要求首轮 Preview 满足用户预期；用户「不是想要的」是合法输入，必须进入 REVISE。

| ID | 验收项 | 成功阈值 | 失败结果 |
|---|---|---|---|
| G6 | 演进闭环可演示 | ≥1 次完整闭环（不满→Gate→REVISE→新 Preview）+ 1 次独立路径 | FAIL_NO_LOOP |
| G7 | 真实 Observation 决策 | ≥1 次唯一决策，所依 Observation ≥3 条合格 | FAIL_FAKE_OBS |

---

## 7. 隐私、同意与数据治理

1. **告知与同意**：收集 Observation、Evidence、对话内容前展示用途；可撤回。
2. **PII 最小化**：默认不采集敏感 PII；必要字段分级。
3. **保留**：运营日志与 Observation 默认保留期可配置；超期匿名化或删除。
4. **导出与删除**：Goal Owner 可请求 Goal 级导出包与删除；删除回执入审计。
5. **隔离**：tenant / org / project / goal 四级隔离（首发可简化为 org/project/goal）。
6. **驻留**：数据不出声明区域，除非独立批准。
7. **UNTRUSTED_DATA**：外部网页、工具输出、Generated App、Agent 消息默认不可信，不得成为指令或授权来源。

---

## 8. 北极星指标与护栏

### 8.1 北极星

```text
CostPerVerifiedSuccess
= (模型成本 + 工具成本 + 基础设施成本 + 人工成本 + 失败恢复成本)
  / 独立验证成功的 Goal 数
```

| 字段 | 冻结定义 |
|---|---|
| 窗口 | 滚动 28 天，按 UTC 对齐 |
| 最小样本量 | 分母 ≥10 个 VerifiedSuccess Goal；否则 INSUFFICIENT_EVIDENCE |
| 目标 | 相对基线 ≤100% 且全部护栏绿 |
| 决策阈值 | 连续 2 窗：北极星 > 基线×1.20 或任一护栏红 → STOP 投资评审 |

### 8.2 护栏（不可被收益抵消）

| 指标 | 红线 |
|---|---|
| 核心任务完成率 | < 70% |
| 证据不足率 | > 30% |
| 端到端 P95 延迟 | > 4h |
| 人工介入分钟/VerifiedSuccess | > 120 min |
| 重复副作用 | > 0 立即停 |
| 未对账 UNKNOWN | > 15 min 立即停 |
| 安全违规/凭据泄露 | > 0 立即停 |
| 内部流量误入产品决策 | > 0 立即停 |

---

## 9. 渐进经营与进化路径

Regent 不以“单个 Agent 一次性生成完整项目”、固定团队模板或事前 ROI 证明作为团队实践、组织进化或长期经营的前置资格。复杂经营允许从多个方向同时探索，在持续行动、失败和外部反馈中形成新的能力、角色、策略乃至问题定义。

探索不要求每轮都产出正向经营结果；有效失败、排除错误路径、发现新问题、形成新能力和修正认知同样是进展。门槛只作用于不可逆现实影响、法定责任和资源总上限，不得成为思考、实验、并行探索、复盘、能力创造和组织重组的许可制度。

### 9.1 承诺（Committed）

| 阶段 | 摘要 | 前置 |
|---|---|---|
| E1 开放探索空间 | 经营方向、可用资源、现实影响边界；团队自行产生和组合探索路径 | 资源池与不可逆行动边界明确，不预设解决方案和固定组织 |
| E2 持续工作循环 | 周期性观测、主动发现、滚动计划、跨中断恢复和经营记忆 | E1 至少形成一轮可审计闭环 |
| E3 自主演化 | 团队可随时创建、替换、合并或退出角色，创造能力并改变协作关系 | 沙箱内默认开放；只有新增现实权限时进入影响治理 |
| E4 责任范围扩大 | 从单一指标扩展到一个职能，再扩展到跨职能经营 | 前一范围安全稳定且单位结果成本可接受 |
| E5 多目标与多运行时 | 并发、公平排队、优先级、抢占及不同运行环境 | 不能破坏既有目标的状态与副作用一致性 |
| E6 持续评测 | 在线经营结果、回放实验、基线与挑战者共同驱动策略更新 | 评价口径、成本与安全事件可追溯 |

### 9.2 条件承诺（需前序 DecisionRecord）

| 阶段 | 激活条件 |
|---|---|
| 扩大生产权限与不可逆行动范围 | 现有范围有充分安全证据，并获得相应授权 |
| 扩大现实资源池 | 当前资源接近上限；团队说明已有探索、机会成本和新增资源用途，不要求证明必然回报 |
| 跨租户能力复用 | 隐私、授权、来源、衰减、撤销和重验证合同完整 |

### 9.3 候选（需单独产品 DecisionRecord）

开放 Agent/能力市场、无人工责任主体的全自治经营、无边界访问生产系统。

---

## 10. 智能体团队学习与评测协议

评测服务于学习、认知更新和资源配置，不是团队开始实践或选择方向的资格考试。团队可以保留暂时没有统计优势但具有长期潜力、信息价值或战略选择权的路径；系统应同时记录短期结果、学习价值、可能性空间和资源消耗。

需要形成可比结论或扩大不可逆现实影响时，对比不同团队编成、策略或局部执行方式应报告：

| 维度 | 要求 |
|---|---|
| 模型 | 相同实际版本与端点 |
| Prompt/技能/工具/权限 | 集合与版本哈希一致（除组织形态差异） |
| Token | 总 Token 与缓存 Token 分列 |
| 算力 | 墙钟预算与总计算预算分别报告 |
| 人工分钟 | 计入净收益 |
| 口径 | pass@1 / pass@k 预先定义 |
| 随机性 | 种子、重复次数、95% 置信区间 |
| 惩罚 | 安全违规与不可恢复失败计入负向 |
| 隔离 | 训练/调参/测试集隔离 |

评价器：冻结 Rubric；默认盲评；机器验证优先于 LLM-as-a-Judge。
### 10.1 团队编成与新增冻结指标

智能体团队是 Regent 的产品形态，但不意味着每项工作都必须同时调用多个 Agent。经营目标由团队承担，局部工作交给最合适的责任 Agent；统筹、执行和验证可以在同一最小团队内随实践逐步分化。单 Agent 只作为局部任务的成本与质量参照，不是团队成立、持续学习或新增角色的前置门槛。

组织路由至少使用以下可测任务特征：工具调用密度、子任务可分解性、顺序依赖强度、单 Agent 基线成功率、独立验证需求和并行收益上限。初始裁剪规则冻结为：

1. 团队可以自行决定从单一路径、多角色并行或多个竞争团队开始，不预设“最小”就是最优；
2. 强顺序依赖的局部任务可由一个责任 Agent 连续完成，团队保留目标统筹与结果验收；
3. 团队无需等待已知缺口出现即可创建新角色、提出新能力或尝试新组织；探索本身可以发现此前无法描述的可能性；
4. 沙箱、仿真和可逆环境中的新编成默认可运行；进入不可逆现实环境时再依据影响级别配置权限和保护措施；
5. 安全与权限 Gate 可以阻止危险行动，但不得阻止团队生成假设、离线验证、复盘和提出改进。

除 §10 既有字段外，每个候选组织还必须报告：

| 指标 | 冻结定义 | 用途 |
|---|---|---|
| `coordination_token_share` | Agent 间协调消息 Token / 总 Token | 识别协调开销吞噬执行预算 |
| `error_amplification_factor` | 注入受控上游错误后，下游错误影响量 / 注入错误量 | 识别拓扑级错误放大 |
| `dispatch_entropy` | Orchestrator 每步派工分布熵的时间序列 | 识别调度发散和失焦趋势 |

20% 的 `coordination_token_share` 为工程告警线，不是自动失败线；最终采用与否仍由 `CostPerVerifiedSuccess`、护栏和置信区间共同决定。任何阈值调整必须版本化并形成 DecisionRecord。

### 10.2 协作失败分类与可恢复行为

**目标态**：多 Agent Run 应将 MAST 类结构性失败映射为稳定 `failure_code`，至少覆盖：步骤重复、提前终止、角色越界、推理与行动不一致、未请求澄清、忽略同伴输出、隐式决策冲突、缺少验证和验证者失败。

**当前状态（2026-08-11）**：词表与 `classify_mast_failure` 已定义并有单测，**生产分类路径尚未接线**（见 §12 未实现清单 / Tech-Spec §25）；不得宣称生产归因已生效。接线属 P2-4 范围。

- 不确定性达到模板阈值时，Agent 必须进入澄清动作或 `WAITING_HUMAN`，不得静默继续；
- 不确定性不影响关键分叉时，可在已设硬预算的最小验证中继续，不要求为清零未知而反复询问；
- Orchestrator 的“派给谁、为什么、依据哪些证据”必须可回放；
- 失败归因用于实验、恢复和产品决策，不得以增加提示词替代状态机、验证 Gate 或角色边界。

### 10.3 固定组织模板认证

`pm-dev-independent-qa-v1` 及后续固定模板中的每个 Agent 必须冻结：

1. 角色边界与明确非职责；
2. 工具/能力 allowlist 与可委托范围；
3. 停止、澄清、失败和移交条件。

模板认证绑定完整成员清单、模型版本、Prompt/技能/工具哈希、拓扑和验证合同。替换任一成员或上述任一绑定项都产生新模板版本并触发**整体验证与整体回归**；禁止沿用旧模板认证。QA 必须与生成者身份和权限分离。

`REGENT_AAR1_CERTIFIED_HIVE` **代码默认 True**（`.env.example` 默认 on），启用认证固定模板 `pm-dev-independent-qa-v1`；可按 Goal metadata `force_single_agent` / `hive_enabled=false` 或 env=false 退出。该 flag 曾早于本小节整体认证合同存在；MA-2（2026-07-31）落地成员三要素、五类 hash 摘要与回归套件后，仅在 digests 与认证合同一致时保持有效，摘要变更须重新核验（见 Plan §12.6）。

### 10.4 长任务上下文产品要求

现有会话内 todo 与自动压缩能力升级为可恢复合同：

- 计划对象在 Worker 重启和上下文压缩后仍可恢复，至少保存状态、责任 Agent、依赖、完成证据和下一步；
- 大于配置阈值的工具结果写入不可变 Artifact，消息仅保留引用、哈希和短预览；
- 压缩前完整原文写入可检索 Artifact；压缩摘要必须结构化保留 Goal 意图、已产 Artifact、未决风险和下一步；
- 压缩不得删除 Permit、约束、失败码、证据引用或用户尚未解决的决定；
- **前缀稳定性与成本（2026-08-01）**：同一次 Run 内，易变的 workspace / todos / 失败缺口不得破坏可缓存的稳定前缀；默认不把文件全文塞进每轮 prompt（按需 `read_file`）。产品与运维须能观测 prompt cache 命中（`cached_tokens`），区分「模型贵」与「拼装浪费」；成本护栏未达标前不得扩大 agentic 流量。
- **硬预算止损（2026-08-13）**：单步骤、单次 Run、Goal 累计与第三方费用均须有硬上限；任一上限命中即停止新的付费调用和自动重试，保存产物、证据、成本、失败原因与最小恢复点。继续执行必须由 Goal Owner 增加预算或修订范围，禁止以“沉淀经验”为由隐藏续跑。

这些能力是 Worker/Agent harness 的可靠性与成本增强，不构成增加团队成员或协作层级的理由。

### 10.5 局部执行闭环与渐进交付基线

复杂项目不得以“一次生成完整项目”作为验收单位。团队必须把经营目标拆成可行动、可验证、可产生外部反馈的增量，每一轮交付一个可运行改进、一次经营行动或一个已验证结论，再基于反馈继续工作。

面向用户的目标是提高首次生成可运行率，缩短从首次失败到可用交付的等待时间与修正轮次，并降低人工介入率。GQ-3 报告必须同时给出这些用户结果指标，不能只报告内部 pass rate。

- **Settings 代码默认** `generation_strategy=agentic`；`artifact-backed` 为 kill-switch / scaffold fallback（见 Tech-Spec §13.7、`config.py`）；
- 主 Worker 必须真正根据 `generation_strategy`（`artifact-backed` / `agentic`）选择生成器，不得无条件硬编码某一种（见 Tech-Spec §13.4）；
- 注入生成器对象的实际类型必须与运行元数据 `generator_ref` 一致，禁止「标注 agentic、实际仍 artifact-backed」（见 Tech-Spec §13.4）；
- artifact-backed 路径保留下游依赖构建、真实构建与部署后浏览器/smoke 验证，但反馈发生在较晚阶段、纠错成本高；必须补充**会话内、低延迟**的「运行—读错—修改」自纠正闭环（见 Tech-Spec §13.5）；
- `VerificationAgent` 现有 `compileall` + 起服务 + 端点探测之外，应补充 pytest / 项目测试命令能力，并把真实构建、测试与 smoke 失败可靠回灌至同一次生成会话，而非仅由下游恢复流程处理（见 Tech-Spec §13.5、§13.6）；
- 将 `agentic` 设为默认前，必须以隔离影子任务或小比例 canary 对照 artifact-backed 与 agentic，在预注册的代表性冻结任务集上比较成功率、成本、延迟、首次可运行率、修正轮次和人工介入率；门槛、样本量、停止规则与安全护栏必须在实验前冻结（见 Tech-Spec §13.7、Plan §13）；
- GQ-3 / M6 canary 必须经强制控制流启用：canary 仅当 `generation_strategy_canary_gate=True`（GQ-2 反馈闭环验证后）**且** `canary_percent>0` 时，按 `stable_canary_bucket(goal_id)` 对**具体 goal** 选 `agentic` 生成器；`canary_rollout_allowed` 在策略解析中强制 GQ-2→GQ-3 顺序；**Settings 代码默认** gate=False、percent=0（见 Tech-Spec §13.7、§13.4）；
- **M6 受控 canary（产品状态，2026-08-11）**：曾于 2026-08-01 打开小比例（5%）agentic canary；权威窗记录现为 **CLAMPED_PENDING_QUALIFICATION**，观察计划 **HALTED**（须先 Offline Qual；扩流禁止）。代码默认 `generation_strategy=agentic`、canary gate/percent 关闭；`artifact-backed` 为 kill-switch / scaffold fallback。**不等于 GQ-4**。soft-pass、单次 demo、开窗前历史或已 clamp 的开窗样本**不得**宣称 M6 出口达标。权威：`docs/m6-canary-window-2026-08-01.json`、`docs/m6-canary-watch-plan-2026-08-01.md`；
- **GQ-3 合规前置**：agent 工具循环必须在独立 sandbox 内执行（不得在持有 DB/Provider 凭据的 worker 宿主上 `subprocess` 跑白名单命令）。不满足则**禁止**打开 canary / 影子流量，即使控制流代码已就绪（见 Tech-Spec §13.8、统一计划 CD-0）；
- 固定 Hive 和动态团队应在真实经营循环中持续学习；成员数量和探索路径由团队在资源池内自主配置。系统呈现消耗、结果和学习，不以短期净收益不足自动禁止探索。
- GQ-4 默认切换必须经强制控制流：运行对照实验后，唯有 `gq4_default_switch_gate` 判定 `PROMOTE_AGENTIC_CANDIDATE` 且无 kill switch 时，`apply_gq4_promotion` 才允许晋级；未通过则 `DomainError` 阻止翻转 `generation_strategy`。运行时默认仍由 `generation_strategy` 驱动，kill switch 始终覆盖。晋级须形成 DecisionRecord（见 Tech-Spec §13.7、Plan §13）。
- **运维 `.env` 覆盖 `REGENT_GENERATION_STRATEGY=agentic` 不等于 GQ-4 晋级**，也不得替代上述沙箱前置与实验窗；打开小比例 canary 同样不等于默认策略翻转。
- **模型 thinking 模式**属运维/成本旋钮（默认关闭），不是面向用户的功能承诺；开启须可配置且预算可观测（见 Tech-Spec）。

---

## 11. 停止投资条件

出现任一条件应触发正式 STOP 投资评审：

1. P1 Graduation 矩阵在约定窗口内不可证伪或连续失败且无可信修复计划；
2. 生产或预发出现重复外部副作用且无法证明已修复；
3. 团队长期脱离真实反馈、无法解释资源去向，且多轮主动重组后仍不能恢复学习循环；
4. 内部 Smoke / 伪 Observation 再次进入产品 Gate；
5. 凭据泄露未轮换或安全红线被突破；
6. 文档仍为 CONDITIONAL 却开工 P2 编码。

---

## 12. 非目标

- 无审批的全自动生产发布。
- 无证据的「自治公司」叙事。
- 用投票替代事实验证。
- 把长上下文、更多 Token 或更多 Agent 数量作为成功指标。
- 为单个生成 App 向 Core 添加业务专用模型。
- 未经认证的开放 Agent/Tool 市场。
- 支付系统和完整商业化后台（除非作为独立 App Goal）。
- **Tauri 桌面端**：仓库中若保留骨架，视为探索性非目标；产品交付面仍为 Core + Web Console，桌面端不计入 P0/P1/P2 验收。
- **无边界现实权限**：自由拓扑和自主重组在思考、仿真、沙箱与可逆环境中默认开放；它们不能自动继承资金、生产、敏感数据、对外沟通或其他不可逆现实权限。现实权限随影响对象治理，不随组织形态一刀切禁止。
- 下列能力**已登记但未实现**（不得宣称验收）：

  - 持续团队 Eval Harness（仍实验骨架，尚未形成在线经营学习闭环）；
  - 自由拓扑的生产现实权限继承（`ROLLOUT_NOT_ALLOWED`；自由探索、沙箱组织演化与新角色创造不在此禁令内）；
  - GQ-4 默认切换（PENDING：DecisionRecord 未 ACCEPTED）；
  - G0 ExternalOperation **跨 provider 真实网络 query→resolve 全路径**（核心闭环已落地，仅此切片待合入）；
  - SelfImprovementRun 产品门禁（候选，`ROLLOUT_NOT_ALLOWED`）；
  - MAST 失败码生产接入（已定义 9 码，尚未接入生产分类路径）；
  - `agent_invocation_guard.check_cross_deployment_invocation` 接线（已实现，主链消费待接入）；
  - 组织模式运行中重评估（里程碑边界 / 目标修订后重选 org_mode，属自适应编排缺口）。

> **更正（2026-08-23 代码核查）**：原列于此的 **组织修复脚手架**（hub-and-spoke 守卫、目标分类、组织模式选择、运行时行为监测与修复环）与**"直达生成"旁路**，经核查已实现、单测并接入执行主链与 Worker（commit `40e5378` 起接线；行为修复环自动再调度与周期监测 tick 于 2026-08-23 补齐，护栏内运行，见 §0.5），已从本"未实现"清单移除，其剩余缺口以上面两行单列。本节此前滞后于实现，以本次核查为准。

> **更正（2026-08-01 代码核查）**：原列于此的 **P2-3 Impact Graph、P2-5 AgentEnvelope HMAC、G0 ExternalOperation 核心闭环** 经代码核查**均已实质实现并有单测**（见 `docs/registered-unimplemented-2026-07-30.md` 与 `core/src/regent/application/impact_graph_service.py` / `envelope_v1.py` / `external_operation_service.py`），已从本"未实现"清单移除。本节此前滞后于实现，以本次核查为准。

> **迭代登记（2026-08-03 代码核查）**：2026-08-02/03 落地的混合控制平面 H0–H2（abort/permission/ask 工具/只读时间线）、Session Work Plan（W0–W4，Step-0 门禁 + 计划审批）、控制台可观测性（SSE + ProgressEvent）、Agent 内核 W4 收口（CJK token、质量门、live golden lane）、交付缺口恢复与诊断交付，均已接线生产路径；其产品语义以各决策笔记与执行计划为准（见 `docs/decision-note-*`、`docs/execution-plan-*` 2026-08-02/03）。本节"未实现"清单（P2-4 / P2-5 自适应 / GQ-4 / EO 跨 provider / SelfImprovementRun 门禁 / MAST 生产接入）维持不变。

---

## 13. 迭代顺序

```text
经营目标合同 + 数据/权限边界
→ 组建最小经营团队
→ 完成一次“观察—发现—行动—验证—复盘”闭环
→ 持续运行并沉淀经营记忆
→ 根据真实缺口调整角色、能力和协作关系
→ 以影子/限额/可回滚方式验证新编成
→ 扩大经营指标、业务职能和行动权限
→ 扩展到多目标、多运行时和跨职能经营
```

团队持续保留探索日志、成本、结果和认知变化；不要求每次尝试都形成正式 DecisionRecord。只有重大经营方向变化、现实权限变化、资源池显著扩展和不可逆行动需要正式决策记录。安全、合规、资源上限或授权红线只暂停对应现实行动，不冻结团队的思考与进化。
---

## 附录 A：2026-07-16 历史快照（非权威）

以下内容仅供版本追溯，不构成当前需求、验收或发布依据；发生冲突时，以本文件附录之前的 CURRENT 正文为准。

# Regent 产品需求文档

> 状态：ARCHIVED 历史快照（非权威）
> 日期：2026-07-16

## 1. 产品定义

Regent 接收自然语言目标，在用户授权、资源、约束和治理边界内，自主解释目标、发现与补齐能力、组建人机组织、创建并运营独立应用，并根据外部证据持续调整计划与组织。

用户唯一必填输入是自然语言 Goal。附件、期限、资源、约束和偏好均为可选。系统内部可以生成 GoalSpec，但不要求用户定义组织结构或解决方案。

Regent 的 P0 产品核心是“可靠、受治理的目标执行内核”。系统默认使用单 Agent；动态组织是需要通过对照实验验证的候选增益机制，不是预设成立的产品前提。

## 2. 产品边界

```text
regent/
├─ core/   # 通用自治组织运行内核
└─ apps/   # Regent 创建和运营的独立应用
```

Core 只表达目标、能力、组织、工作、执行、证据、策略和资源。每个 App 拥有独立源码、依赖、数据、测试与部署，可以脱离 Core 运行。具体内容类型、订阅方式和业务指标不得成为 Core 领域对象。

## 3. P0 能力

1. 保存原始 Goal，生成版本化 GoalSpec，区分显式约束、系统推断和未知项；
2. 持久化 Goal、Work、Run、Artifact、Evidence、HumanTask 和 Audit，进程重启后恢复；
3. 从 Goal 与计划推导能力需求，区分能力、权限、资源、信息缺口和普通失败；
4. 通过复用、配置、组合、构建或请求人类补齐能力；
5. 根据能力覆盖、并行收益、隔离、成本与风险组建最小组织，并能收缩、替换和解散；
6. 在 `apps/<app-id>` 创建、构建和测试独立 App，不向 Core 引入业务模型；
7. 使用外部 Observation 和 Evidence 评价进展并重规划；
8. 所有副作用行动经过策略判断和一次性 ExecutionPermit；
9. 人工输入与审批使用独立 HumanTask，等待期间不占 Worker；
10. 支持暂停、恢复、取消、预算限制、无进展停止和完整审计。

P0 保持整体交付，不拆分为独立版本。开发顺序可以分切片推进，但 P0 只有在第 9 节全部条件满足后才算完成。

## 4. 状态语义

不同层级使用不同完成语义：

- Goal `ACHIEVED`：目标成功条件已有充分证据；
- Work `ACCEPTED`：逻辑工作成果已通过独立验收；
- Run `EXECUTED`：一次执行正常返回，但成果尚不一定被接受。

Goal 其他终态：

- `EXHAUSTED`：当前硬约束和资源上限内已无可行路径；
- `FAILED`：发生不可恢复的系统或状态完整性错误；
- `CANCELLED`：Goal Owner 主动终止。

`PAUSED`、`WAITING_HUMAN` 和 `BLOCKED` 是三种不同的可恢复状态：

- `PAUSED` 只能由用户 `resume`；
- `WAITING_HUMAN` 由 HumanTask 完成、超时或升级策略唤醒；
- `BLOCKED` 由资源、环境、授权变化或重规划唤醒。

## 5. Work 与 Run

Work 是计划中的逻辑工作单元，保存目的、输入引用、验收标准、依赖、优先级和预算。Run 是执行某个 Work 的一次不可变尝试，绑定实际执行者、模型、工具、输入版本和 Permit。

```text
Goal 1 ──* Work 1 ──* Run
```

一个 Work 可以产生多个 Run，但同一时刻最多一个活动 Run。重试或更换执行方式必须新建 Run，历史 Run 不覆盖。Run `EXECUTED` 后，Evaluator 接受 Evidence，Work 才进入 `ACCEPTED`。

## 6. 固定 P0 验收

```text
名称：CSV_SUMMARY_BASELINE
Goal：读取授权目录中的 orders.csv，生成 summary.json。
数据：1,12.50 / 2,7.50 / 3,INVALID / 4,10.00
约束：禁止联网；不得修改输入；只能写入 output/。
输出：{"row_count":4,"valid_count":3,"invalid_count":1,"total_amount":30.0}
```

自动验收必须证明：原始 Goal 保存；约束与推断分离；形成 Work 和 Run；输出逐字段相等；Evidence 包含输入与输出哈希；Worker 中断后恢复；幂等重放不产生第二份输出；Run 为 `EXECUTED`、Work 为 `ACCEPTED`、Goal 为 `ACHIEVED`。

## 7. 测量与产品决策

P0 必须执行冻结的对照实验，而不能仅以功能演示判断动态组织价值。详细实验协议与指标定义以 [Regent-Measurement-Decision-Framework.md](./Regent-Measurement-Decision-Framework.md) 为准。

比较三种执行模式：

- A：强单 Agent；
- B：固定组织模板；
- C：动态组织。

三组必须使用相同模型、初始能力、输入、预算、时间上限和验收标准。核心指标包括端到端成功率、独立质量、完成时间、模型与工具成本、人工负担、安全事件、能力缺口识别质量、协调开销和能力复用率。

产品决策规则：

- C 达到冻结的净收益门槛：继续投资动态组织；
- 能力补齐有效但 C 连续两轮不优于 A：转向“强单 Agent + 受治理能力工厂”；
- B 与 C 表现接近：采用少量固定组织模板；
- 能力缺口无法稳定识别、能力无法独立验证，或复用价值持续不足：停止通用化扩展，转向垂直产品。

这些是必须由数据验证的产品假设，不是 P0 功能存在即可证明的结论。

## 8. 首批长期目标

- 创建并运营面向 AI 从业者的产品，第一阶段达到 100 个有效付费用户；
- 创建并运营面向成年人的短篇童话网站，第一阶段达到 10,000 有效 DAU。

两者从同一 Goal API 进入。业务指标来自外部可验证数据，不作为 P0 编码完成条件，也不得反向固化 Core。

“有效付费用户”的权威来源是支付流水、退款状态、账号与有效使用事件；测试支付、已退款订单、内部账号和欺诈交易不计入。指标口径必须版本化。

“有效 DAU”必须是去重真实用户并满足最低有效阅读行为，排除爬虫、内部账号、自动流量、异常设备/IP 和流量农场。原始事件需要服务端签名或等价防篡改机制、幂等 ID 和 Bot 检测。

长期目标启动时必须另外冻结时间窗口、数据源、归因规则和阶段 Gate。童话网站在 100、1,000、3,000 有效 DAU Gate 完成质量、安全与留存复核后，才扩大到 10,000 有效 DAU。

## 9. P0 完成定义

P0 作为整体完成，必须同时满足：

1. Core 在空 Apps 条件下通过 `CSV_SUMMARY_BASELINE`；
2. 仅凭普通 Goal 形成可解释的最小组织，补齐至少一个能力缺口，并通过 `EVT_PARSER_GAP`；
3. 在独立 Apps 目录创建可运行产品候选，新 App 接入不改变 Core 领域模型；
4. 运行可恢复、副作用幂等、高风险行动受控，状态、Evidence、Permit 和审计可追溯；
5. 完成 A/B/C 冻结任务集的首轮对照实验，并依据预先冻结的门槛形成继续、转向或停止的产品 DecisionRecord。

P0 完成证明 Regent 具备受治理的目标执行、能力补齐和应用创建能力；它不自动证明动态组织优于单 Agent。动态组织是否成为核心卖点，必须服从对照实验结果。

## P1：Core 自主生成应用

P1 的产品目标是让 Regent Core 在未知具体产品实现的前提下，从 Goal 和治理约束出发，自主完成证据发现、产品假设比较、需求修订、能力解析、应用生成、隔离构建、发布和观测闭环。

### 强制边界

- Core 预置治理和生成机制，不预置各种垂直 App 功能。
- AI 业内人员 App 是首个验证对象，不是 Kernel 模块。
- 生成结果必须由版本化需求、证据引用、生成计划和文件变更集共同约束。
- 构建与发布均视为受治理副作用，必须可幂等、可恢复、可审计。
- 未知外部结果不得推定成功；必须进入 UNKNOWN 并通过查询或对账收敛。

### P1 整体验收

输入一个目标和约束后，Core 能生成可运行 App，在隔离环境通过验证，发布预览，采集真实使用信号，并输出 CONTINUE、REVISE 或 STOP 决策及完整证据链。P1 保持整体交付，不拆成子版本。
