# Regent Core 内核职责说明

> 状态：ACTIVE INTERNAL KERNEL
>
> 产品权威源：[Novel-Engine-PRD.md](./Novel-Engine-PRD.md)
>
> 本文件保留原文件名，仅用于兼容既有链接；不再定义独立商业产品。

## 1. 定位

Regent Core 是 Novel Engine 的内部执行与治理内核。它不面向经营负责人销售，不再承担互联网经营、智慧城市、景区运营或通用应用生成等独立产品使命。

当前唯一对外产品是面向 C 端用户的剧本式长篇小说创作与阅读系统。任何 Regent Core 演进都必须能够说明其如何服务 Novel Engine 的产品需求、可靠性、安全或成本目标。

## 2. 保留职责

- 持久化长任务、幂等调度、暂停、恢复和失败对账。
- 模型调用、预算与配额、成本记录和供应商适配。
- 确定性状态转换、权限边界、审计事件和不可变产物。
- 多 Agent 执行编排以及受约束的角色调用。
- 运行时观测、越界检测、重试和人工接管入口。
- 通知、对象存储、数据库与部署基础设施。

## 3. 小说领域不属于内核的职责

以下对象和规则属于 Novel Engine 产品层，不得反向固化为通用内核概念：

- `StoryGoal`、`CriticalPath`、`ScenarioSpec`、`PersonaSpec`。
- 场景卡、角色信息集、表演输出、Canon 和长篇一致性。
- 锚点作品、结构抽取、强制变异和作品相似度。
- 阅读、公开发布、广告、收益分成和创作者结算。
- 小说内容安全、AI 内容标识和版权投诉流程。

## 4. 复用原则

概念相似不等于同一实现。`GoalSpec` 与 `StoryGoal`、通用计划与 `CriticalPath`、hub-and-spoke 与角色有限视角只能通过明确接口映射，不得仅凭类比共用领域对象。

复用分为三类：

1. **直接复用**：队列、恢复、预算、审计、模型网关和存储基础设施。
2. **接口复用**：审批、通知、监测、重试和人工接管。
3. **产品专属**：全部小说领域模型与用户体验。

## 5. 变更门槛

新增内核能力须至少满足一项：

- 被 Novel Engine 当前里程碑明确依赖；
- 修复已验证的可靠性、安全、隐私或成本问题；
- 替换重复实现并有自动化回归证据。

不得再以旧 Regent 商业愿景、通用自治团队或未来行业场景为理由扩大当前范围。

## 6. 历史定义

旧 Regent 产品 PRD、定义、计划、评测和决策记录已移至 `docs/archive/legacy-regent-2026/`。它们不是当前执行依据。
