# Novel Engine 开发计划

> 版本：v7.6
>
> 更新：2026-09-09
>
> 状态：ACTIVE — 当前开发顺序唯一权威源

> 最新复核（v7.4）：导演式生成尚未完备。本轮 277 项定向回归、前端构建通过，迁移图单 head 0055；新增隔离探测复现纠错任务缺少纠错输入、DONE 作品任务无人领取、80% 扩卷绕过用户卷数限制、正式事实兑现后承诺仍 OPEN。详细证据及验收条件见 [完备性复核](docs/archive/novel-director-completeness-v7.4.md)。以下 v7.3 完成记录保留为历史开发记录，冲突处以本轮结论为准。
>
> **v7.5 进展**：C-01、C-02 落地。纠错内容进入重演运行的 `generation_context`；重复报错不再叠任务；DONE/PAUSED 如实说明「恢复后才执行」；80% 预规划不再扩卷（它会提前结束活动卷、跳过剩余节点），扩卷只在末节点完成时发生且共享用户终局约束。
>
> **v7.6 进展（C-01～C-05 全部闭合）**：C-01 补齐恢复衔接——新增 `resume_after_correction()` 与 `POST /works/{id}/resume-correction`，完结/暂停作品在用户明确恢复后其排队的重演才会真正执行（有排队任务才允许恢复，不空放回 RUNNING）。C-03：`VerifiedFact` 新增 `resolves`，兑现按**具体承诺**核验（同人两条只兑现一条时另一条保留；只给人物名的有歧义兑现不整批关闭）；六类视图投影接入生产——`perform` 拿角色视图、`weave` 拿叙述者视图、`direct` 拿导演视图（导演笔记不进正文、读者认知不给人物）。C-04：实体无交集不再自动认证为独立，改为 unknown，独立性只认创作输入的显式声明。C-05：终局判断请求带上已接受正文、已核验事实与**未兑现承诺**，节点只取当前路径版本，调用改走 CallBroker（幂等键 `ending:<卷>:<章>`，重试不重复计费）。新增 `test_c_batch.py` 共 13 例 + `test_c01_resume_api.py` 3 例（真实 HTTP 入口）；反证 `deploy/novel/mutate_check_c01.py` 退回旧行为后 23 例次变红（C-04 两方向互斥，分场景注入）。本地回归 212 passed + 22/22 B 反例。入口级测试带出并修复 3 个生产缺陷：鉴权时间比较 naive/aware 混用（500 应为 401）、事件序列裸 SQL 绑不上 UUID、`novel_events.id` 在 SQLite 不自增。追加：前端报错面板接线恢复按钮（构建通过）；**0055 真机认证通过**——全新库升级/回退/再升级全绿 + 并发崩溃 16/16，并抓出并修复 0048 对 `_LATER_TABLES` 表发 `DROP COLUMN` 致全新库无法安装的缺陷（本机单测发现不了，只有全新库真机升级能暴露）。详见 [完备性复核 v7.6](docs/archive/novel-director-completeness-v7.6.md)。**仍开放**：B-04/B-06（运行时认证）、M2/R4/M4。

### v7.4 优先待办（覆盖历史勾选）

- [x] **C-01**：报错内容进入导演请求；已完结/暂停作品按明确策略调度；重复报错不无限建任务。（已落地：`_queue_replay_run(correction=...)` 把 ticket/statement/subject 写进重演运行上下文；同章已有排队中的重演不再叠新任务；DONE/PAUSED_QUOTA/PAUSED_COST 返回 `resume_then_replay` 并如实说明、不自动改状态；新增 `resume_after_correction()` 与 API，用户明确恢复后排队任务才被执行，且无排队任务时拒绝恢复。重演章号按升序排队——依赖边只从早章指向晚章，升序即拓扑序。HTTP 入口层 3 例验证真实路由 + 鉴权 + 409/401。前端报错面板在返回 `resume_then_replay` 时显示「恢复创作并执行重演」按钮，构建通过（浏览器旅程验证属 M2）。）
- [x] **C-02**：所有扩卷入口共享用户终局约束；80% 预规划不能提前完成活动卷或绕过卷数限制。（已落地：`_maybe_expand_volume` 移除了 80% 触发——`expand_next_volume()` 会把活动卷标记完成，80% 调用等于跳过剩余节点；扩卷只在末节点完成时发生，且先过 `decide_ending` 的用户卷数约束。）
- [x] **C-03**：具体承诺的正式兑现与六类记忆投影贯通真实角色/Writer/导演请求。（已落地：`VerifiedFact.resolves` 让正文兑现关联到具体承诺，`resolve_items` 按 key/内容精确命中，按 subject 只在无歧义（唯一候选）时关闭；`_memory_view()` 把记忆按受众投影后分别进入 `perform`（角色）、`weave`（叙述者）、`direct`（导演）请求。）
- [x] **C-04**：依赖独立性须有来源证据，不能把实体无交集直接判为 independent。（已落地：`_link_dependencies` 不再因「没有共享实体」自动标 independent；无证据即 unknown 并保守重做。首批仍标独立——那是「此前无任何记忆」的结构性事实，不是启发式。`MemoryItem.declared_independent` 承载创作输入的显式声明。）
- [x] **C-05**：终局判断读取已接受产物与未兑现承诺，并纳入统一预算、调用持久化及恢复约束。（已落地：`generate_ending_verdict` 请求带 `accepted_text`/`verified_facts`/`open_promises`，节点只取当前路径版本；调用改走 `CallBroker`，幂等键 `ending:<卷>:<章>`。）
- [ ] B-04/B-06、M2/R4/M4：实际灰度、生产入口认证、浏览器旅程、真实盲评及长篇验收保持开放。

## 1. 执行原则

1. 持续导演 loop 是目标主流程；现有六步链路与可取得的 novelMaker 轨迹作为历史基线，不要求恢复旧拓扑后才能开发新协议。
2. Hive 只在局部步骤同时需要信息隔离和多线程处理时确定性启用；它不是实验变量。
3. UI、UX、前端和后端按纵向旅程切片协作，不按层整包交付。
4. 当前 MVP 只做原创创作、私有作品和定向试读。
5. 每个里程碑都有代码、体验、数据和恢复四类出口。
6. 阈值先 pilot、再冻结；不得开发后修改门槛以宣布通过。
7. 导演创作控制权是架构约束；具体调度策略、检索、重演与呈现机制通过同预算盲评优化，不能把导演退化为审校器。

开发顺序：

`R0 基线与底座缺口 → R1 单场景导演闭环 → R2 成章与恢复 → R3 长期记忆 → R4 盲评灰度 → M3 MVP 封测 → M4 长篇认证 → M-T 双环 → M5 商业化`

M2 用户旅程随 R1–R4 纵向接入。下列 M-1/M0 保留为底座检查清单，其历史迁移项不得阻塞已具备相应能力的新架构。

## 2. M-1：基线与决策

### 工作

- 冻结私有 `novelMaker` commit、依赖、运行说明和访问方式。
- 保存至少 3 条完整 loop 轨迹：正常推进、用户改意、失败恢复。
- 建立 baseline manifest：代码、模型、prompt、sampling、输入、事件、人工动作、成本和产物 hash。
- 对轨迹数据去敏并记录使用许可。
- 冻结当前 Regent 工作树快照；恢复规定 Python 版本下的可重复测试基线。
- 用 20–30 名目标作者执行 Wizard-of-Oz，测改稿诉求、澄清、方向复述和裁决行为。
- 确认 PRD D-01～D-03（正文已定）并拍板 D-04～D-06；未拍板项采用文档建议默认值进行原型，但不得进入生产。

### 出口

- [ ] novelMaker 指定 commit 可重复运行。
- [ ] 三类轨迹和 baseline manifest 完整。
- [ ] Core 测试基线可重复，已知失败有清单和责任人。
- [ ] Wizard-of-Oz 输出原始记录、指标和结论。
- [ ] 首批 1–2 个题材确定。

## 3. M0：安全底座与运行契约迁移

### 后端先行切片

1. 服务端身份、session 和 owner/tenant 资源授权；移除客户端 actor 的授权作用。
2. 金额迁移为整数最小单位；修复预算 settle 原子性；禁止非幂等成本旁路。
3. 新建 Novel Domain schema、migration、状态机和版本字段。
4. 建立持久事件/outbox/sequence/SSE replay。
5. 建立章+节拍 checkpoint、logical_call/attempt、幂等键和恢复契约。
6. 建立 DecisionRequest 与 durable timer 的竞争事务。

### loop 迁移切片

1. 建立 `core/src/regent/novel` 与 Core 端口。
2. 迁移 novelMaker loop，保留历史恢复和用户控制契约，创作拓扑按 Tech-Spec v5.0 重构。
3. 保留用户改意、暂停、继续、失败修正和人工 gate。
4. 跑通目标→方向→关键路径→场景→一章正文。
5. 完成故障注入：模型成功后崩溃、正文写入前后、Canon 前后、结算前后、SSE 断线、timer/用户并发。

### 出口

- [ ] 不存在一次调用生成整部作品的路径。
- [ ] 每一步有状态、事件、成本和不可变产物。
- [ ] 任意断点恢复不重复调用、计费、裁决或章节。
- [ ] 两设备/默认 timer 同时裁决仅一个结果成功。
- [ ] 修改 id/actor 无法读取他人作品、事件、账本或导出物。
- [ ] 迁移行为相对 novelMaker 基线无未解释退化。

## 4. M1：导演架构重构（当前主线）

执行顺序为 R0→R1→R2→R3→R4。

2026-09-08 重新校验：**R1 核心场景闭环已有实现与回归；R0、R2、R3、R4 和 M2 仍为部分完成，尚无完整里程碑出口获本轮认证。** 新增代码明显推进了恢复、运行契约、记忆和评估，但主链路仍有连接缺口。历史“完成”记录保留为当时的开发记录，以本节和 §10 的复核状态为准。

本次依据为 HEAD `b3d5818` 加当前未提交工作树。详见 [审计记录](docs/archive/novel-progress-2026-09-08.md)、[源码指纹](docs/archive/novel-progress-2026-09-08-manifest.json) 和 [可重复探测](docs/archive/novel-progress-2026-09-08-probe.py)。本轮仅更新计划及审计材料，没有修复业务代码或修改部署系统。

### 当前进度与证据（2026-09-10 复核，以此为准）

**B-01～B-05 已完成并验收；B-04/B-06 仍开放。** 本轮 196 项小说域定向回归与 22/22 离线反例全部通过。导演场景控制权、长期创作约束（六类视图）、依赖重演执行、同预算质量验证与自然完结均已接入生产并闭环；实际双策略灰度（B-04）与真实成章入口并发认证（B-06）保持开放。详见 [本轮审计](docs/archive/novel-progress-2026-09-10.md) 和 [离线反例](deploy/novel/mutate_check_b.py)。以下历史验证记录保留，不代表本轮重新认证。

| 范围 | 已确认成果 | 仍待完成 |
|---|---|---|
| R0 | UNKNOWN、预算/租约及跨 attempt 补账修复；存在 PostgreSQL 16/16 日志 | 全部源码指纹及真实成章入口并发认证，B-06 |
| R1 | 场景规划、调度、重演/改写、完整命令；用户/默认裁决语义已进入导演 | 持续创作质量与跨章意图验收 |
| R2 | v2 成章收尾已接扩卷、动态卷边界测试通过；自然完结按用户卷数/导演判定结束，扩卷失败不再套模板而是发出 `volume.expansion_failed` | 真实生产终局与扩卷失败的端到端触发仍属 B-06 范围 |
| R3 | 六类视图（world_fact/character_state/character_belief/reader_cognition/promise_and_foreshadow/director_memory）已落地；承诺带内容指纹不互相覆盖、关系变化与共现推断置信度区分、改意按依赖子图失效、兑现章号可查 | 依赖完整性真实重演已接入 `report_fact`；六类视图权限边界单测通过 |
| R4 | BudgetBand 成本/时延约束、逐臂绑定实际模型/正文/成本/运行；缺证据 HOLD、超预算/超门槛不得晋级、指纹重算检出漂移 | 真实双策略灰度 B-04、pilot 与人评 |
| M2 | 后端旅程定向测试及前端构建通过 | 浏览器、移动端、断网和无障碍完整验收 |

验证记录（2026-09-10）：Python 3.13.12 定向回归 **196 passed in 26.01s**（`tests/unit/novel` + `tests/unit/test_novel_generation.py`）；`deploy/novel/mutate_check_b.py` 离线反例 **22/22 成立**；迁移图单一 head 为 `20260909_0055`（链 0048→0055 连续）。新增迁移 `20260909_0055_ending_and_memory_views.py`：`novel_works.ending_target_volume`/`ending_statement`、`novel_memory_edges.edge_kind`、`novel_memory_items.resolved_chapter_no`/`basis`、记忆 kind 检查扩展为 7 类。本次未执行数据库升级或远程认证。回归命令：

```powershell
.venv/Scripts/python.exe -m pytest tests/unit/novel tests/unit/test_novel_generation.py -p no:cacheprovider -q
.venv/Scripts/python.exe deploy/novel/mutate_check_b.py
```

此结果不是全仓测试通过，也不证明真实模型质量或生产并发安全。旧定义冻结测试未纳入通过集合。历史计划记载 PostgreSQL 临时库 11/11 验证，本轮未重跑，未找到绑定当前源码指纹的原始结果，故保留历史记录但不认定当前工作树已全面认证。B-01～B-05 的接入与判定依据静态调用链与单测/反例，B-04/B-06 仍须新增入口级与生产环境证据。

验证记录（2026-09-09 追加）：A-04/A-05/A-06 修复后，`tests/unit/novel` + `test_novel_generation.py` + `test_worker.py` + `tests/integration` **221 passed**；改动源码 ruff（E,F,I,UP,B,SIM）除既有 E501 外无新增问题。反证脚本 `deploy/novel/mutate_check_a05.py`（6 例）与 `mutate_check_a06.py`（7 例）全部咬住。P0-5 已按源码指纹执行：git HEAD `b3d5818071c95b656109cb2b0209184e0d9314ff`、工作区差异 sha `399ec42cf73d9a85`、迁移 head `20260908_0054`、PostgreSQL 16.14 / Python 3.12.14；全新库升级→回退两级→再升级全绿，并发与崩溃注入 **16/16 通过**（新增并发章提交、用户/默认裁决竞争两个场景），原始结果见 `deploy/novel/pgverify-20260909.log`。仍未执行真实 pilot 人评与浏览器旅程矩阵。

### R0 基线与硬边界

- 冻结当前工作树、模型/提示词配置和可取得的真实三章样本；记录未提交代码，不覆盖。
- 核对权限、预算、恢复缺口；清除事实矛盾降级放行，后续上下文只选已接受章节。
- 定义 versioned DirectorCommand、SceneBrief、Take、事件、正文、ValidationReport 和提交包。
- 设计 architecture_version、旧章导入、shadow 分支和回退开关。
- 出口：基线可重跑，v1/v2 数据隔离，硬失败不可放行；尚未宣称新导演有效。

### R1 单场景纵向闭环

- 导演先构思场景，再发 ActorBrief；人物提出行动，Resolver 结算。
- 导演观察后继续/重演；Writer 呈现，导演观看正文并接受/重写。
- ContextCompiler 隔离人物、导演与叙述可见信息；所有命令由 Runtime 校验。
- 出口：一个冲突场景完整跑通，至少覆盖一次有证据且改变输入的重演；调用、take、决策可追踪且有预算上限。

### R2 成章、事实与恢复

- 工作状态贯穿同章多场景，已接受场景组章并最终审校、核验、提交。
- 建立角色认知、读者认知、承诺记录；取消 v2 固定三章映射。
- 接入现有后台 Worker 和用户进度；用户不逐场审核。
- 出口：连续三章可读；废弃 take 不进入事实；正文与事件一致；故障注入覆盖调用结果落盘、费用结算、take 选择、章提交前后和并发父版本冲突。

### R3 长期创作与纠错

**部分实现（2026-09-08 复核）**：`domain/memory.py`、`application/memory.py` 和迁移 `20260908_0053` 已有四类记忆及依赖边存储，Canon 写入与章上下文召回已接线，相关测试通过。但正式 `VerifiedFact.statement` 与抽取器字段/分类契约不兼容，正常事实探测生成 0 条记忆；`link_memory`/`plan_replay` 未接生产，缺边不能可靠识别，失效仍按类别而非依赖范围处理。现有单元测试不能证明长期记忆或最小子图重演可用。详见 A-04/A-05，后续验收见 §10。 2026-09-09 修复（A-04/A-05）：正式 `VerifiedFact.statement` 已进入抽取契约，人物共现作为分类的结构信号；`link_memory`/`plan_replay` 接入 `record_chapter_memory` 与 `update_critical_path`，一条边都没有即判图不完整并保守重做；改意失效范围由「按类别整批」改为「依赖子图」，退路如实写进事件。

- 实现稳定规则、人物弧线、伏笔与承诺、摘要加按需召回。
- 依赖完整后实现最小子图重演；此前保守重做当前章后续场景。
- 用户改意通过分支和版本失效，不修改既有事实链。
- 出口：跨章知识获取、误信、伏笔回收、关系变化均有来源；重大节点由用户决定，旧版上下文不污染新版。

### R4 质量验证与灰度

**部分实现，晋级能力未验收（2026-09-08 复核）**：`domain/evaluation.py`、`application/evaluation.py` 和迁移 `20260908_0054` 已提供配置、报告、盲评载荷和判定函数；灰度函数也有测试。补充探测证明：未注册样本、同一样本多名评者、缺成本或修改冻结配置均可能错误 PROMOTE；盲评私有映射与生产灰度选择尚未闭合。修复 A-06 和接通 A-05 前，验收结论保持 HOLD，不得据现有判定宣布胜出或扩大灰度。真实 pilot、预注册门槛和人评仍待执行，不能用单元测试替代。 2026-09-09 修复（A-06）：只接受已注册样本与预注册评者；A/B→arm 私有映射与正文引用落库但不对评者可见；样本量按独立样本计数；缺成本、延迟、模型、样本构成、正文引用或预注册评者时结论只能是 HOLD；配置与报告指纹一律重算，漂移与篡改不得晋级。反证脚本见 `deploy/novel/mutate_check_a06.py`。

- pilot 后冻结样本、rubric、预算、停止条件和晋级阈值；包含舒缓场景与单人场景，避免奖励机械冲突与多人对白。
- v1/v2 在相同模型和预算带下进行场景、三章和卷级盲评；隐藏导演自评。
- 报告阅读意愿、人物可信度、情绪与兑现、事实错误、成本、延迟和重演收益；样本不足不得宣布胜出。
- 出口：达到预注册人评与成本门槛，恢复/权限通过，新作品灰度；不兼容的运行中任务不得切换执行器。

新导演控制权不作为可删除功能；失败时重设计调度策略。知识隔离与硬门不关闭，Hive 保持确定性路由。

## 5. M2：端到端旅程薄切

`apps/novel-web` 已接入多项真实 API，当前重点改为补齐端到端行为和异常恢复。页面、接口存在或构建通过不等于用户旅程验收；优先完成 §10 的裁决闭环，再执行以下测试矩阵。

### 顺序

1. App shell、URL 路由、认证回跳和作品工作台。
2. 创建目标、有限澄清和方向卡。
3. 关键路径移动端卡片、版本冲突和影响预览。
4. Snapshot + durable SSE 进度、离开/回来和恢复说明。
5. 裁决深链、二次确认、截止时间和作品级默认策略。
6. 章节阅读、AI 标识和阅读位置。
7. 事实报错、局部重演说明。
8. 定向分享、撤回和首次导出告知（`ExportNotice` 校验）。
9. 内容审核状态、投诉与误判申诉入口，对作者可见。
10. PWA app shell；私有内容默认不离线缓存。

### 测试矩阵

- 视口：360、390、768、1440。
- 网络：offline、slow-3G、重连。
- 浏览器：Chromium、WebKit、微信内置浏览器 smoke。
- 状态：刷新恢复、后台回前台、SSE 重复/乱序/gap、409 路径冲突、重复/过期裁决、401 回跳、分享撤回、登出清缓存、SW 升级。
- 质量：状态 reducer、组件、API contract、Playwright E2E、axe、视觉回归、Lighthouse。

### 出口

- [ ] 完整旅程：目标→方向→路径→后台运行→离开/回来→裁决→恢复→首章阅读。
- [ ] 默认页面无 Agent、model、token、Canon、Scenario 等内部术语。
- [ ] 移动端三动作全部可用，关键路径不依赖拖拽。
- [ ] URL 刷新不丢上下文；事件缺口能 resync。
- [ ] 私有正文未授权不进入共享缓存。
- [ ] 审核待审状态对作者可见；投诉与申诉可提交且结果留痕。
- [ ] axe serious/critical 为 0。

## 6. M3：MVP 封闭测试

### 范围

原创创作、私有作品、最多 5 部并行、首章到首卷、定向试读；不做真实公共池、广告分成、锚点和原生 App。

### 实验

- 招募目标作者与其邀请的真实试读者。
- 明确展示 AI 标识，不在隐藏真实条件下测续读。
- 记录方向确认、正文等待、首章完读、D1/D7、第 3/10 章、首卷、第二部启动、裁决、改稿诉求和有效阅读章成本。
- 纠错功能保持开放；系统能力评测单独统计 untouched cohort。
- MVP 默认免费并记录影子价格、实际成本和支付意愿；真实收费由 D-06 决定。

### 出口

- [ ] 首次用户无帮助提交目标的成功率和耗时达标。
- [ ] 方向卡核心差异可复述率达标。
- [ ] 方向预览 P95≤15 分钟；正文 P50/P95 单独报告。
- [ ] 首个裁决完成率、错过率和默认策略接受度达标。
- [ ] 首章、D7、第 10 章及首卷继续率达到预注册门槛。
- [ ] 每个有效阅读章成本处于批准预算。
- [ ] 至少完成导演式交互闸门和带 AI 标识持续阅读闸门。

## 7. M4：长篇分级认证

建立 `NE-LongArc-v1`，按 20→50→100→150 章升级；每一级使用冻结 seed，至少 3 次模型采样，并按前/中/后段报告。

### 工作

- 卷级摘要、上下文预算与实体召回曲线；
- 快照、分支、回滚和未固化窗口 N=1/3/5；
- 真实用户跨日裁决和长时间暂停恢复；
- 完成率、介入曲线、质量、成本和时延分布；
- 模型/prompt/data/evaluator manifest、原始 run 和失败案例包。

### 出口

- [ ] 连续两个长度档通过才升级。
- [ ] 上下文和成本不随全文长度无界线性膨胀。
- [ ] 修改仅重算确定性依赖范围。
- [ ] 150 章通过后才允许宣称“30 万字长篇能力”。

## 8. M-T：创作—阅读双环

仅在 M3/M4 证明作者愿意持续创作、受邀读者愿意持续阅读后启动。

### 工作与出口

- 逐部显式公开授权和撤回，无默认勾选。
- `PublicWork` 不可变快照和独立只读服务。
- 阅读 10 万次 generation 调用数为 0。
- 读者行为驱动的质量门槛、推荐和公共池冷启动。
- 发布前复现检测、append-only 创作留痕和投诉入口。
- 他人作品阅读占比达到预注册门槛。
- 国内备案、资质、审核责任和法务书面结论完成。

原生 App 只在 Web 验证通过且渠道收益明确时另行立项，不与公共池上线强绑定。

## 9. M5：商业化

- 用真实流量标定 CPM、填充率、打赏退款、广告容忍度和治理成本。
- 建立 WorkLedger、QuotaLedger、收入确认、反刷、成本回收和分成。
- 免费额度与生成硬上限版本化；付费不能取消反灌水硬上限。
- 作品按 cohort 和完整分布计算聚合毛利，不用爆款均值。
- 收益表述必须披露典型结果与典型支出；无数据不作收益承诺。

出口：至少 100 部真实公开作品聚合毛利为正；账本可重放；未回本作品分成为 0；每一分钱可归因。

## 10. 下一开发批次（按本轮复核排序）

勾选仅代表该开发任务在所列证据范围内完成，不代表整个 R 里程碑通过。重新打开的任务保留既有实现；后续验证必须从生产入口贯穿状态、模型请求和账本，不能只调用 helper 或手工预置终态。

### 上一批任务复核

| 任务 | 当前状态 | 保留的成果与关闭边界 |
|---|---|---|
| P0-1 UNKNOWN 恢复 | [x] 实现及定向回归通过 | 对账计数、Worker 启动/周期清扫、查询复用和终止；生产并发证据另按 P0-5 |
| P0-2 每次尝试独立结算 | [x] 原缺陷已关闭 | top-up 消费键已带 attempt，新增回归通过 |
| P0-3 预算原子预留 | [x] 实现及定向回归通过 | 已结算/未结清预算核算、章上限和作品行锁；不以 SQLite 证明 PostgreSQL 并发 |
| P0-4 版本与租约隔离 | [x] 实现及定向回归通过 | owner/token/有效期复核、调用后回查版本、改意递增版本和配置冲突检查 |
| P0-5 PostgreSQL 认证 | [ ] 当前工作树认证待补 | 保留历史临时库 11/11 与迁移往返记录；需原始结果与源码指纹绑定，并补主入口场景；历史回退仅到 0052，不承诺无损 |
| P1-1 完整命令执行 | [x] 实现及定向回归通过 | Runtime 命令/产物绑定、完整 manifest、Hive 确定路由与仅模型调用并发 |
| P1-2 导演裁决闭环 | [x] 原缺陷已关闭 | 用户/默认选择语义、等待状态与消费清理已接入，入口回归通过 |
| P1-3 按卷记忆与路径终止 | [x] 实现及定向回归通过 | v2 扩卷和卷边界已接入；自然完结按用户卷数/导演判定结束、扩卷失败发 volume.expansion_failed（B-05 已闭） |
| P1-4 留痕与首章旅程 | [x] 后端范围通过 | 审核/申诉结论与无需逐场审核的首章回归通过；不包含 §5 浏览器验收 |

### 第一批：补账与导演持续创作闭环

- [x] **P0-2 / A-01：修复补账的 attempt 幂等维度。** 连续两个 attempt 均超预留，覆盖正常返回、供应商查回和 UNKNOWN 重试；验证实际费用等于消费账、每笔 reservation 结清、重复恢复不重记。保留本轮 3+4 应为 7 的反例作为回归。
- [x] **P1-2 / A-02：让裁决真实改变导演后续创作。** 将选择的完整语义绑定请求/版本并传入 WATCH_TAKE/WATCH_PROSE；消费并清除 pending，保持作品与章节等待状态一致。由导演实际请求启动，分别走用户选择和到期默认，断言下一次模型请求含所选内容、无重复提问、无未选分支执行；竞争与重启只应用一次。
- [x] **P1-3 / A-03：闭合 v2 完成节点后的终止或扩卷。** 从真实 CANON 接到路径进度判定：有后续卷时及时展开，无后续目标时完成作品且不多建空章。通过 advance_step→start_run 验证，不手工设 story_complete；覆盖动态章数偏离预计卷范围、重试与并发，保证不重复扩卷。（B-05 验收：decide_ending 三态、扩卷失败发 volume.expansion_failed、UNDECIDED 拒绝空章。）

出口：账本守恒、导演能带着用户决定继续创作、末节点完成后准确结束或转卷；导演继续拥有规划、表演调度、重演和正文取舍权。

### 第二批：长期记忆从正式事实贯通到纠错

- [x] **R3 / A-04：统一事实与记忆契约。** 从正式 VerifiedFact 输出进入 Canon，形成规则、弧线、承诺与关系记录，带来源/版本；下一章实际导演请求召回正确内容。分类信息必须有明确来源，不仅让测试手工补标签。（B-01 验收：分类依据可查、承诺带内容指纹不互相覆盖。）
- [x] **R3 / A-05：建立真实依赖与保守退路。** 在生产中记录依赖边及完整性依据，接入改意/纠错和重演入口；漏边、空图或未知来源不得误报完整。完整时只重做受影响范围，不完整时保守重做后续场景，保护已接受事实；避免按类别整批误伤无关记忆。（B-02 验收：coverage_of/replay_subgraph 区分 depends/independent，report_fact 排真实重演。）
- [x] **R3：补齐长期视图与跨章验收。** 明确事实、人物认知/误信、读者认知、承诺、人物关系/弧线及导演记忆的投影与权限边界，逐项对齐架构契约；覆盖知识获取、误信纠正、伏笔回收和改意后旧版本隔离。（B-01 验收：六类视图与 director_note/reader_knowledge 可见性边界单测通过。）

出口：真实章节能写入、召回和修正有来源的记忆；不能用四类表记录数量替代六类长期视图验收。

### 第三批：评估可信度、运行灰度与联合验收

- [x] **R4 / A-06（B-03 重新打开）：修复盲评数据和晋级硬门。** 只接受已注册样本/有效评者，持久化私有 A/B→arm 映射与正文引用；按独立样本而非评分行计数并处理重复评分。缺成本/模型/预算/样本构成等必需证据时 HOLD；重算冻结配置和报告指纹，漂移不得晋级。覆盖本轮零注册样本、多评者单样本、缺成本和阈值篡改反例。（B-03 验收：BudgetBand 三维度硬门、缺模型/运行绑定 HOLD、报告指纹随样本或评分变化失效，22/22 反例含。）
- [ ] **R4 / A-05（B-04 尚未完整验收）：接入生产灰度选择与回退。** 在作品/运行创建入口使用确定分桶，持久化执行器版本；验证新作品选择、shadow 产物隔离、回退和运行中禁止不兼容切换，不仅测试纯函数。
- [ ] **P0-5（B-06 证据需补齐）：绑定当前源码的 PostgreSQL 验证。** 保存源码/迁移/环境指纹、原始结果和失败记录；覆盖全新安装、声明范围的迁移往返，以及从实际入口触发的调用后崩溃、补账重试、租约接管、并发章提交、用户/默认裁决竞争。使用隔离测试库；当前单一 migration head 不等于迁移执行通过。
- [ ] **M2：执行 §5 浏览器旅程矩阵。** 重点覆盖裁决后继续生成、刷新/离开返回、断网重连和跨卷阅读；记录移动端与无障碍结果。
- [ ] **R4：真实 pilot 与三章同预算盲评。** 数据链和硬门修复后，先 pilot 再冻结样本、rubric、模型、预算带、停止条件和门槛；保存原始人评、成本、延迟与产物 manifest，再据证据决定新作品灰度。

以上出口通过后再进入 M3 封测。当前不提前启动公共池、分成或 150 章能力认证；190 项定向回归与前端构建不能替代真实创作质量验收。

### 本轮剩余任务与初始目标（v7.3，覆盖前述历史完成宣称）

- [x] **B-05 自然完结与扩卷失败恢复**：从正常模型/生产入口按用户终局意图结束，不以替换扩卷函数证明完结；失败不套用偏离题材的升级模板。实现：`domain/ending.py` 的 `decide_ending`（用户卷数/导演判定/未判定=UNDECIDED 三态）；扩卷失败不再套模板，发 `volume.expansion_failed`；`start_run` 在 UNDECIDED 时拒绝空章。单测与反例见 `test_last_node_and_volume.py`、`mutate_check_b.py`。
- [x] **B-01 正式长期记忆**：正式事实能形成规则、承诺及兑现记录；补齐误信、读者认知、导演记忆等六类视图，跨章来源可查。实现：`domain/memory.py` 7 类 kind、内容指纹键、共现/关系/误信/读者/导演信号分级置信、六视图投影与可见性边界、兑现章号。验收：`test_long_term_memory.py`、`test_memory_from_real_facts.py`、`mutate_check_b.py`。
- [x] **B-02 依赖完整性与重演执行**：部分图不得误报完整；实际按场景/产物依赖调度重演，未知时保守恢复，不只给记忆打失效标记。实现：`coverage_of`/`replay_subgraph` 区分 depends/independent 边，无覆盖记录即 unknown；`report_fact` 接 `plan_local_replay`+`invalidate_changed`+排重演运行。验收：`test_path_change_scoped_memory.py`、`mutate_check_b.py`。
- [x] **B-03 可信评估**：落实 BudgetBand 成本与时延约束，逐臂绑定实际模型/正文/成本；缺证据 HOLD，超预算不得晋级。实现：`evaluation.py` 的 `budget_violations`（场景/章/时延三维度）、`verdict` 同预算带优先级 ③、缺模型/运行绑定 HOLD、报告指纹随样本/评分变化失效。验收：`test_eval_and_canary.py`、`mutate_check_b.py`。**边界**：`build_report` 尚无生产/API 入口，硬门为库级就绪，真实生效仍待 R4 pilot 接入；按 fail-closed 设计，漏传只会 HOLD 不会误放行。
- [ ] **B-04 实际灰度**：提供两个真实且保留导演控制权的策略版本，验证分桶、shadow 隔离与回退；当前唯一允许值 director_v2 不构成双臂灰度。
- [ ] **B-06 生产恢复证据**：指纹包含未跟踪文件及实际上传内容；通过真实生成提交入口验证并发/故障，保留现有 16/16 日志的有限结论。
- [ ] **M2/R4/M3/M4 验收**：浏览器旅程 → pilot 与冻结 → 三章同预算人评 → 封测 → 分档长篇验收；现有 23 份章节产物不能替代这些证据。

初始目标的判断：导演已在场景创作中拥有控制权，整体“持续把控小说质量”尚未获证实。M-1 基线与用户研究等出口也未全部完成。公共阅读与商业化为有前置条件的后续阶段，保持未启动，不扩张本轮修复范围。

## 11. 修订记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-02 | v5.0 | 基于最新代码和 UI、UX、产品、前端、后端、AI 科学家复核重新生成；改为纵向旅程交付，新增安全/账本前置、durable SSE、分级评测、MVP 封测和局部机制 kill criterion。 |
| 2026-09-02 | v5.1 | 闭合性复核：M2 补内容审核/投诉/申诉工作项与出口条件（PRD FR-25 为 P0，原计划缺失）；决策项措辞与 PRD v4.1 的已定/待拍板划分对齐。 |
| 2026-09-03 | v5.2 | 固定 Agent loop 为主核心流程；Hive 仅在信息隔离与可并发双条件同时成立时启用，移出质量消融和架构讨论，只保留路由、隔离、收敛与故障测试。 |
| 2026-09-07 | v6.0 | 以 R0–R4 替换审核式导演建设路线，明确单场景闭环、成章提交、长期记忆、同预算盲评和渐进迁移。 |
| 2026-09-07 | v6.1 | R0 生产预算与调用恢复落地，更新 M1 进度与剩余边界。 |
| 2026-09-07 | v6.2 | 补记 R2 未完成项：Canon 事实按卷切分尚未实现。 |
| 2026-09-07 | v6.3 | R1 补齐命令 Runtime 校验与上下文编译，更新 M1 进度。 |
| 2026-09-07 | v6.4 | 重新核验当前工作树：记录 111 项定向回归、前端构建及迁移图证据；校正 R0/R1/R2/M2 完成边界，加入四个可复现缺口及 P0/P1 后续批次。 |
| 2026-09-08 | v6.5 | P0-1~P0-3 落地：新增迁移 `20260908_0052`（`reconcile_count`）、`recover_novel_calls()` 与 Worker 启动/周期恢复、失败异常保留 `request_id`、预算改按已结算金额校验并做按章原子预留。 |
| 2026-09-08 | v6.6 | P0-4 落地：租约 owner/fencing token/有效期复核、调用窗口后回查数据库判定失效、`bump_input_version()` 使用户改意与路径变更作废旧方向产出、调用配置指纹入幂等键。 |
| 2026-09-08 | v6.7 | P0-5 完成：真实 PostgreSQL 临时库认证（迁移链 + 11/11 并发崩溃场景），修掉并发预留越界、恢复清扫跨会话不可见、同步 Engine 造 AsyncSession 三个缺陷，并冻结 0048 使全新库可安装。 同版第二批记录：P1-1～P1-4 新增命令/manifest/Hive、裁决、卷记忆与终止、审核留痕及首章回归；当时记录小说域 138 例通过。完成边界以 v6.9 复核为准。 |
| 2026-09-08 | v6.8 | R3 长期记忆与 R4 盲评骨架落地：迁移 0053/0054、记忆抽取/召回/失效/最小子图重演、评估冻结/盲评/裁决与灰度分桶；R4 的真实 pilot 采样与人评仍为人工项。 |
| 2026-09-08 | v6.9 | 复核当前未提交工作树：190 项定向回归、前端构建、0054 单 head；补充探测与调用链审计发现 A-01～A-06。重新打开 P0-2/P1-2/P1-3，校正 R3/R4 为部分实现，区分历史 PostgreSQL 记录与本轮证据，并重排后续验收批次。 |
| 2026-09-09 | v7.0 | A-04/A-05/A-06 修复落地：正式事实抽取与依赖建图接入生产、按依赖范围失效、执行器灰度钉住与延期切换（不许切换≠停摆）、盲评注册校验与证据硬门、指纹重算检出漂移；221 项定向回归通过，13 个反例全部咬住。A-05/A-06 验收条目据此闭合；P0-5 与真实 pilot 仍未执行。 |
| 2026-09-09 | v7.1 | P0-5 按源码指纹完成：独立临时库全新安装与迁移往返全绿，并发/崩溃注入 16/16（含并发章提交与用户/默认裁决竞争两个新场景），指纹与原始结果入库。剩余为真实 pilot 人评、浏览器旅程矩阵与长篇回验。 |
| 2026-09-09 | v7.2 | 再复核：258 项定向回归和前端构建通过；关闭 A-01/A-02 原缺陷，确认 v2 扩卷接线；发现 B-01～B-06，重新打开评估预算带、实际灰度和完整认证任务，对照初始目标明确尚未全部完成。 |
| 2026-09-10 | v7.3 | B-01～B-05 落地验收：六类视图长期记忆与内容指纹键、依赖完整性真实重演（report_fact 排重演）、同预算带/证据硬门、自然完结三态与扩卷失败不套模板；新增迁移 0055；196 项定向回归 + 22/22 离线反例全绿。B-04/B-06 仍开放。 |
| 2026-09-10 | v7.4 | 完备性复核（未改业务代码）：277 项定向回归与前端构建通过，但隔离探测复现四处生产流程缺口——纠错任务未携带纠错输入、DONE 作品任务无人领取、80% 扩卷绕过用户卷数限制、正式事实兑现后承诺仍 OPEN。新立 C-01～C-05，并声明 v7.3 的完成记录不代表对应完整旅程已验收。 |
| 2026-09-10 | v7.5 | C-01/C-02 落地验收：纠错内容进入重演运行上下文（`correction`/`replay_reason`）、同章重复报错不再叠任务、DONE/PAUSED 如实说明「恢复后才执行」且不自动改状态；80% 预规划不再扩卷（避免提前结束活动卷跳过剩余节点），扩卷只在末节点完成时发生并共享用户终局约束。新增 `test_c_batch.py` 6 例与反证脚本 `mutate_check_c01.py`（退回旧行为 5 例变红）；本地 202 passed。 |
| 2026-09-10 | v7.6 | C-01～C-05 全部闭合：新增 `resume_after_correction()` + API 补上恢复衔接（无排队任务则拒绝恢复）；`VerifiedFact.resolves` 按具体承诺兑现（同人多承诺不整批关闭），六类视图投影分别进入 `perform`/`weave`/`direct`；依赖独立性不再由「实体无交集」推断，无证据即 unknown；终局判断带上已接受正文/已核验事实/未兑现承诺、节点只取当前路径、调用接入 CallBroker 幂等键。`test_c_batch.py` 共 13 例 + `test_c01_resume_api.py` 3 例（HTTP 入口），反证退回旧行为 23 例次变红；本地 212 passed + 22/22 B 反例。入口级测试带出并修复 3 个生产缺陷（鉴权 naive/aware 时间比较、事件序列裸 SQL 绑 UUID、`novel_events.id` SQLite 不自增）；前端接线恢复按钮；0055 真机认证通过并修复 0048 全新库安装缺陷。B-04/B-06（运行时认证）、M2/R4/M4 仍开放。 |
