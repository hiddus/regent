# Novel Engine 执行清单

> **版本**：v3.1 · **建立**：2026-08-31 · **状态**：DRAFT（M0 启动前置条件已满足）
> **文档定位**：唯一编码执行清单与开发切片。需求以 [PRD](./Novel-Engine-PRD.md) 为准，技术实现以 [Tech-Spec](./Novel-Engine-Tech-Spec.md) 为准，本文只规定**做什么、按什么顺序做、做完如何验收**。
> **v3.0 变更**：里程碑顺序调整（**M2 C 端可用先于 M3 长篇能力**，理由见 PRD §6.0）；新增引导层与关键路径切片；新增守卫 G-7~G-9。
> **v3.1 变更**：① 新增**锚点创作切片组**（S2.1A 锚点入口 / S2.1B 结构抽取器 / S2.1C 变异门与同源判定 / S2.1D 合规留痕）；② 新增守卫 **G-10**（抽取只出结构）与 **G-11**（`entry_path` 不可缺省）；③ 前端切片改为 **Web PWA** 并强制业务层/UI 层分离（为原生壳预留）；④ M2 出口检查新增 **A2.9（A/B 双路径分组，不可妥协）** 与 A2.10；⑤ 新增 **M-T 转正里程碑**（条件触发，含 ST.1–ST.5 与 AT.1–AT.4），**M4 商业化的前置依赖由 M3 改为 M-T**；⑥ 技术债纳入 T-11~T-13，并新增"业务层/UI 层未分离则转正期需重写"的高返工风险项。

---

## 0. 启动前置条件（v3.0：已满足）

四项原阻塞 M0 的未决项已收敛，M0 可进入编码：

| # | 决策 | 落地含义 |
|---|---|---|
| U-1 | 通用场景引擎，用户自建 | S0 须实现场景无关核心 + 场景包机制（FR-1.11/1.12）；首发以中文网文场景包为 starter |
| U-2 | 开源模型为主，DeepSeek 系列 | S0 须实现 `ModelRouter`（V3 默认 + R1 关键拍 + 自托管可选）；成本护栏降为优化项 |
| U-3 | fork `novelMaker` 作为参考基座 | 以 novelMaker 为起点 fork；与 Regent 内核同源承载（非桥接） |
| U-9 | 面向公众的 C 端网站 | **对本文影响最大**：C 端能力从「M3 外壳」上移为 **M2 产品闸口**；`StoryGoal` / `CriticalPath` 须在 M0 即建立（A0.5），不能推到 C 端阶段 |
| U-13 | 终局定位（创作工具 vs 阅读消费） | **尚未拍板**。不阻塞 M0–M3（阶段一两者路径一致），但**阻塞转正期规划与 M-T 的验收口径**。当前按"创作工具"执行 |
| U-14 | 锚点退出阈值 | 不阻塞开发，阻塞 M-T 的启动判定（PRD §0.8.4 三条初值，M3 标定） |
| **U-15** | **锚点作品范围**（公版/已授权 vs 允许在世作品但严格限在结构层） | **阻塞 S2.1A 锚点库选型与合规准备**。用户倾向后者（"测试阶段可以仿写任何小说"）；若采纳，PRD §9.2 列举的四条执行前提（确定性专名过滤 G-10 / 0.45 硬阻断 / 留痕随功能上线 / 保留切回能力）须在 S2.1A–S2.1D 逐条落实 |

**启动动作：**

1. Fork / 建立 `novel-engine` 包骨架（基于 novelMaker 改造，非另起炉灶）。
2. 接入 DeepSeek（V3 默认，R1 关键拍）作为主模型后端。
3. 跑通 novelMaker 现有链路（含 mock writer），确认基线可用。

---

## 1. 编码约定

| 项 | 约定 |
|---|---|
| 语言 / 类型 | Python 3.12+，严格类型标注（mypy） |
| 风格 | ruff |
| 测试 | 每个 P0 需求至少一个测试；架构测试守护核心不变式 |
| 提交 | 每个切片一个提交，提交信息含切片编号 |
| 破坏性变更 | 任何触及 PRD §0.3 恒定属性或 §0.2 三动作契约的改动，须先有 DecisionRecord |

**架构守卫测试（必须实现，随 M0 落地）：**

| 守卫 | 检查内容 | 守护条款 |
|---|---|---|
| G-1 | 不存在单 prompt 批量生成多角色台词的代码路径 | PRD §0.3-2 声纹分离 |
| G-2 | 上下文装配函数不由 LLM 执行信息集裁剪 | Tech-Spec §10.2 |
| G-3 | 导演层判定顺序中，信息泄露与规则违反先于 LLM 判定 | Tech-Spec §5 |
| G-4 | 重演存在次数上限，无无限重试路径 | FR-3.6 |
| G-5 | 引擎核心不直接引用 `KnowledgePack` / `ScenarioRule` 常量 | Tech-Spec §7.3 |
| G-6 | 引擎核心不含任何品类/场景硬编码规则（**含引导层** `scenario_proposer`） | FR-1.11 领域无关性 |
| G-7 | **主路径创建接口的必填参数只有自由文本 `intent`**；`ScenarioSpec` / `SceneCard` 的结构化写入接口不在主路径暴露 | PRD H6 零门槛不被侵蚀 |
| G-8 | 导演层、评审层、一致性检查**不读取** `StoryGoal.diversity_seed` | Tech-Spec §6.4.1 |
| G-9 | 作品仓储层的读接口不带 `owner_id` 时直接抛错，不存在"漏加过滤即全量可见"的路径 | FR-7.2 私有优先 |
| G-10 | `structure_extractor` 的输出经专名过滤与实体泛化后，不含源作品的角色名 / 地名 / 世界观专名 / 设定专名 / 原文片段 | R-0.8.1 锚点只贡献结构（合规底线，不可由 prompt 保证） |
| G-11 | 所有会话与埋点记录必须携带 `entry_path ∈ {anchor, freeform}`，创建时写入后不可变更；缺失即测试失败 | FR-0.15 分路径统计（数据一旦混合无法回溯分离） |

> G-7~G-9 是 v3.0 新增。它们守护的是**产品承诺**而非技术不变式——"任何人都能用"和"专属于自己"一旦被代码侵蚀，是静默失效的，只能靠测试拦住。

---

## 2. M0 表演内核

**目标**：一章由演员模型产出，跑通任意场景（不锁品类），且 `StoryGoal → CriticalPath → 场景卡` 链路成立。
**验收**：PRD §6 A0.1–A0.5

> **界面形态说明**：M0 允许以脚本/最简界面驱动（A0.5 明确"界面可为最简形态"）。引导层的完整用户体验在 M2 建设，但**对象与链路必须在 M0 就位**——否则 M2 会变成"给已有结构化流程套壳"，而套壳正是 v2.0 被否决的做法。

| 切片 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| **S0.1 骨架** | 包结构（Tech-Spec §2，基于 novelMaker fork）；配置；领域模型（**StoryGoal / CriticalPath / CriticalNode** / ScenarioSpec / PersonaSpec / SceneCard / InformationSet / Performance / VoiceSpec / ScenarioRule） | 可实例化的数据层 | 前置条件完成 |
| **S0.2 场景包机制** | 定义 `KnowledgePack` / `ScenarioRule` 接口（T-5）；实现中文网文 starter 包（复用 `webnovel_knowledge.py`）；确保引擎核心与引导层均不引用包常量（G-5/G-6） | 可插拔场景包骨架 + 中文网文 starter | S0.1 |
| **S0.3 目标与关键路径** | `StoryGoal` 定义与锁定（SG-1~SG-3）；`CriticalPath` 生成（10–20 节点，CP-1~CP-4）；`script.expand(critical_path)` 展开为卷/章/场景卡；节点编辑触发的**增量重生成**骨架 | 一条可锁定的关键路径 + 由其展开的章节骨架 | S0.1, S0.2 |
| **S0.4 剧本层** | ScenarioSpec 生成；场景卡生成；小传编辑器（含声纹字段）；锁定机制 | 一篇可用的 ScenarioSpec + 一章场景卡 | S0.3 |
| **S0.5 信息集** | `assemble_character_context` 确定性装配；信息集裁剪与排除逻辑；命中诊断 | 各角色上下文可打印比对 | S0.1 |
| **S0.6 角色表演** | Persona Agent 注册与执行；独立采样；结构化 `Performance` 输出；差异化采样参数 | 一场戏中各角色的表演输出 | S0.4, S0.5 |
| **S0.7 导演层** | 五项判定（信息泄露 / 场景规则违反 / 剧本偏离 / 关键节点 / OOC 抽样，规则来源 `ScenarioRule`）；打回重演；上限与挂起 | 判定结果与重演记录 | S0.6 |
| **S0.8 成文** | 叙述者按 POV 编织；不新增剧情的约束校验 | 一章正文 | S0.7 |
| **S0.9 ModelRouter** | DeepSeek V3 默认 + R1 关键拍路由（T-6 初版）；自托管后端可选接入（NFR-1.5） | 模型路由能力 | S0.6 |
| **S0.10 单章贯通** | 端到端跑通（中文网文 starter + 一个用户自建简单场景）；观测埋点（token / 成本 / 越界类型） | 一章成品 + 观测数据 | S0.8, S0.9 |
| **S0.11 声纹盲测** | 人工盲测辨别角色台词；产出混淆矩阵 | 声纹区分度初步数据 | S0.10 |

**M0 出口检查：**

- [ ] 同一场戏中不同角色的声纹人工可辨别（A0.2）
- [ ] 无信息泄露（A0.3）
- [ ] 剧本必达节点被达成（A0.4）
- [ ] **`StoryGoal` → `ScenarioSpec` → `CriticalPath` → 场景卡链路可跑通**（A0.5）
- [ ] 九条架构守卫测试全部通过（G-1~G-9）
- [ ] 引擎在不改核心代码的前提下，可加载中文网文 starter 与一个用户自建场景（验证领域无关）
- [ ] 声纹区分度阈值完成标定并写入 Tech-Spec §6.2

---

## 3. M1 一致性与质量（**技术成败闸口**）

**目标**：20 章规模验证（可用中文网文 starter 或任一用户自建场景）。
**验收**：PRD §6 A1.1–A1.4（**四项须同时达标**）

| 切片 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| **S1.1 长链推演** | 20 章连续推演；章摘要生成；滑动窗口 | 20 章成品 | M0 |
| **S1.2 一致性审查** | 设定 / 人物 / 时间线 / 规则体系四审接入 | 一致性报告 | S1.1 |
| **S1.3 读者评议** | 多视角读者画像；追读分 / 回报分 / 节奏分 | 评议报告 | S1.1 |
| **S1.4 声纹指标** | `voice_metrics.py`；阈值标定落地；逐章评分 | 声纹区分度曲线 | M0 |
| **S1.5 伏笔台账** | Hook Ledger 接入；状态流转；回收率统计 | 伏笔台账 | S1.1 |
| **S1.6 介入频次** | 人工介入计数与分类（P1–P7）；统计面板；**A1.4 阈值标定** | 介入频次数据 + 阈值 | S1.1 |
| **S1.7 对照组** | 单 prompt 写作对照组产出；Reader Council 对比 | 对照数据 | S1.3 |
| **S1.8 成本实测** | DeepSeek V3/R1 cache 命中率、在场角色数分布、重演率、R1 关键拍占比实测（T-1/T-6） | 成本实测报告 | S1.1 |
| **S1.9 关键路径粒度实测** | 10–20 节点展开为 20 章的质量实测（**U-11**）；节点编辑后增量重生成的正确性验证（CP-2） | 节点数量结论 + 增量重生成验证 | S1.1 |

**M1 出口检查：**

- [ ] 人设 / 伏笔 / 时间线无硬冲突（A1.1）
- [ ] 声纹区分度达标（A1.2，阈值不得事后放宽）
- [ ] Reader Council 评分不低于对照组（A1.3）
- [ ] 人的介入频次低于阈值（A1.4）
- [ ] 成本实测数据替换 Tech-Spec §11 的粗算值（验证 NFR-1.1 ≤¥100）
- [ ] `CriticalPath` 节点数量上限结论写入 Tech-Spec §4.3（U-11 收敛）
- [ ] **首作品启动率阈值完成标定**（供 M2 A2.3 使用）

> **未通过处置**：若 A1.1–A1.4 任一项未达标，允许最多两轮针对性改进；仍不达标则触发 PRD §10 停止投资条件 1。

---

## 4. M2 C 端可用（**产品成败闸口**）

**目标**：未经培训的外部用户可独立完成一部作品的启动与首卷推演。
**验收**：PRD §6 A2.1–A2.8

> **本里程碑是 v3.0 的核心改动。** 它不是"给引擎套个前端"，而是验证 **AS-4（普通用户能仅凭自然语言表达出可用目标）** 与 **AS-5（10–20 节点粒度足以让用户感到故事是自己的）** 两条产品级假设。这两条比任何技术假设风险更高。

| 切片 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| **S2.1 引导层（A 路）** | `goal_extractor`（自然语言 → StoryGoal）；`clarifier`（≤3 轮 × ≤3 问，超限即用默认值 + 显式假设，约束 O-1）；`goal_summary` 回述确认 | 一句话 → 可锁定 StoryGoal | M1 |
| **S2.1A 锚点选择入口（B 路）** | `AnchorWork` 列表与选择界面；与 A 路并列的双入口；**不引入任何结构化填写**；`entry_path` 字段落库（G-11）；**锚点库按授权状态标记，可整体下线未授权部分**（U-15 的快速切回前提） | 锚点入口可用（**锚点库取值范围待 U-15 拍板**） | M1 |
| **S2.1B 结构抽取器** | `structure_extractor`：从锚点作品抽 `StructureProfile`（事件类型序列 / 情绪弧 / 爽点密度 / 卷章节奏 / 冲突类型）；专名过滤与实体泛化两道确定性处理（**G-10**，约束 O-5）；抽取质量的覆盖度与保真度回归测试 | 结构抽取能力 + 可度量的抽取质量 | S2.1A |
| **S2.1C 变异门与同源判定** | `variation_gate`：强制变异 + 两级相似度判定（与源作品 0.45 合规判据 / 同锚点池 0.50 体验判据，Tech-Spec §6.4.5）；未过阈值继续变异（上限 2 次）或回落追问；锚点集中度监控（T-12） | **FR-0.13 与 FR-7.6 落地** | S2.1B |
| **S2.1D 合规留痕** | 记录锚点来源、抽取产物、变异操作、相似度判定结果（NFR-3.7 / FR-0.14） | 可审计留痕 | S2.1C |
| **S2.2 场景候选** | `scenario_proposer`（k=2..3 候选，约束 O-3 强制可度量相异）；候选选择界面；选定后 ScenarioSpec 锁定 | 候选选择链路 | S2.1 |
| **S2.3 关键路径界面** | 关键路径可视化（10–20 节点时间轴）；节点级操作（改 / 增 / 删 / 锁定 / 标记需裁决）；`diff_translator`（自然语言 → 结构化 diff，约束 O-2）；增量重生成落地 | **「调整关键路径」的完整落点** | S2.2, S1.9 |
| **S2.4 用户体系与专属性** | 注册 / 登录 / 作品列表；`owner_id` 仓储层强制隔离（G-9）；默认 `visibility=private`；用户协议与版权归属声明（FR-7.2/7.3） | 账户体系 + 专属性保障 | M1 |
| **S2.5 异步推演** | 后台任务队列；实时进度；暂停 / 恢复 / 重试；**首章可见时延达标**（NFR-2.5 ≤15min） | 任务中心 | S2.4 |
| **S2.6 配额与免费额度** | 总预算 / 单章预算双阈值；超限自动暂停；成本面板；**免费额度定档**（NFR-1.6 / U-12），按"引导层 + 首章"整体计价 | 成本与配额 | S2.4 |
| **S2.7 前端工作台（Web PWA）** | 三动作主路径（说目标 → 调路径 → 看进度）；结构化字段**默认折叠**；专业模式为可选入口（G-7）；裁决卡片；**Web PWA 形态**（NFR-5.1/5.2：响应式、可加主屏幕、离线缓存章节、全屏阅读）；**业务层与 UI 层严格分离**（NFR-5.3，为原生壳预留） | 完整前端（Web） | S2.3, S2.5 |
| **S2.8 站外通知与裁决** | 关键节点挂起的站外通知（**阶段一用邮件兜底**，渠道抽象为可切换接口 NFR-5.4）；裁决入口；**超时策略落地**（T-9） | 挂起不再是完成率杀手 | S2.7 |
| **S2.9 在线阅读与导出** | 在线阅读器（FR-6.7）；移动端可用（FR-6.9）；导出 Markdown / TXT / **EPUB**（FR-6.12）；导出包自洽可迁移（FR-7.4） | 阅读与导出能力 | S2.7 |
| **S2.10 多样性指标** | `diversity_metrics.py`（Tech-Spec §6.4）；索引只存嵌入与结构摘要不存正文（FR-7.5）；`CriticalPath` 定稿时与首 3 章后各判定一次；**阈值标定**（T-7） | 专属性可度量 | S2.3 |
| **S2.11 海外部署** | 海外云部署；隐私与数据声明；数据不用于训练的实现保障（FR-7.5） | 上线环境 | S2.4 |
| **S2.12 内容分级** | 敏感内容过滤；用户内容强度偏好（**U-6**） | 分级能力 | S2.7 |
| **S2.13 支付链路** | 海外支付方式接入（M2 可为测试模式，正式定价在 M4） | 收款链路 | S2.11 |
| **S2.14 外部用户可用性测试（A/B 双路径）** | 招募未经培训的外部用户；**按 `entry_path` 分组**且 B 路样本量足以独立得出结论；全程无协助观察；记录流失点与澄清轮数；漏斗埋点（Tech-Spec §12.1 / 12.1.1） | **A2.1/A2.2/A2.9 实证数据** | S2.7–S2.13 |

**并行提示**：S2.11 海外部署宜与 S2.1 同期启动（不依赖引导层），以便提前暴露合规与基础设施问题。

**⚠️ 启动前置（不可边做边定）**：S2.1A 动工前须先拍板 **U-15**（锚点作品范围）。理由：锚点库的选取规则、授权状态标记方式、以及是否需要预留"整体下线未授权部分"的开关，都取决于 U-15 的答案；若先按"仅公版"建库再改为允许在世作品，锚点库与合规留痕（S2.1D）都要返工。

**M2 出口检查：**

- [ ] **外部用户无协助完成全流程**（A2.1，**不得由内部人员代测**）
- [ ] **全程无强制填写结构化字段的步骤**（A2.2）
- [ ] 首作品启动率达到 M1 标定的阈值（A2.3，**A/B 两路分别标定**）
- [ ] 首章可见时延符合 NFR-2.5（A2.4）
- [ ] 配额护栏生效且免费额度足以完成首次体验（A2.5）
- [ ] 关键节点挂起可经站外通知完成裁决（A2.6，阶段一邮件兜底）
- [ ] 海外部署与支付链路跑通（A2.7）
- [ ] 同场景包下不同用户作品显著不同（A2.8），跨作品相似度阈值写入 Tech-Spec §6.4.3
- [ ] **A/B 两路指标可分路径查询，B 路样本量足以独立得出结论**（A2.9，**不可妥协**）
- [ ] **锚点产出与源作品的结构相似度低于 0.45**（A2.10 / FR-7.6）
- [ ] U-6 / U-10 / U-12 收敛并回写 PRD

> **未通过处置**：若 A2.1/A2.2 不达标，允许最多两轮**引导层**改进；仍不达标则触发 PRD §10 停止投资条件 2。
> **禁止的规避方式**：不得通过"增加一个让用户自己填的字段"来通过 A2.1——那是 H6 侵蚀，等于放弃 C 端定位。
>
> **A2.9 为什么不可妥协**：外部测试若只跑锚点路径，AS-4 就不是被证伪而是**被绕过**。等产品上线后用户走自由表达路径时，风险原封不动还在。B 路样本量须达到可独立得出结论的规模（M1 后标定）。

---

## 5. M3 长篇能力

**目标**：150 章规模可用。
**验收**：PRD §6 A3.1–A3.4

> 本里程碑投入最大，故置于 M2 之后——**先确认用户愿意用，再投长篇工程量**（PRD §6.0 理由 3）。

| 切片 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| **S3.1 卷级压缩** | L0.5 卷摘要；换卷压缩；上下文预算与篇幅解耦验证 | 预算曲线（证明不膨胀） | M2 |
| **S3.2 原子章** | 章节原子推演单元；状态持久化；断点续跑 | 中断恢复能力 | M2 |
| **S3.3 快照与回滚** | 快照 / 版本记录 / 按章节回滚 | 回滚能力 | S3.2 |
| **S3.4 重规划** | 回滚后受影响章节重规划与重跑；与 `CriticalPath` 增量重生成打通 | 重规划能力 | S3.3 |
| **S3.5 状态图谱** | 巡检、断链诊断、修复建议；项目级批量恢复 | 巡检面板 | S3.2 |
| **S3.6 自托管后端** | `ModelRouter` 接入自托管 vLLM（NFR-1.5）；数据不出域 | 合规与成本增强 | S3.2 |
| **S3.7 长篇压测** | 150 章连续推演；预算、成本、质量全程观测 | 压测报告 | S3.1–S3.5 |
| **S3.8 完成率测量** | 完成率（北极星之二）的稳定测量口径与面板 | 完成率数据 | S3.7 |

**M3 出口检查：**

- [ ] 150 章单章上下文预算不显著膨胀（A3.1）
- [ ] 任意章节中断可恢复且不丢内容（A3.2）
- [ ] 单部成本符合 NFR-1.1（A3.3）
- [ ] 完成率可稳定测量并达阈值（A3.4）
- [ ] **锚点退出的三条量化条件完成标定**（U-14 收敛，供 M-T 判定）

---

## 5.5 M-T 转正里程碑（条件触发 · v3.1 新增）

**目标**：移除锚点入口，切换为纯 AI 自由创作，并同步交付原生 App。
**验收**：AT.1–AT.4
**触发条件**：M3 结束后评估 PRD §0.8.4 的三条量化退出条件，**须同时满足**。

> **为什么单列一个里程碑**：转正是一次重大产品形态变更（入口移除 + 形态切换），有自己的失败模式（用户流失），不能挂在"商业化"里顺带做。
> **为什么不先商业化**：在一个注定要移除的功能上收费，用户付费后功能被砍是灾难。故转正须先于 M4 的正式定价。

| 切片 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| **ST.1 退出判定** | 按 U-14 标定值评估三条退出条件；出具判定报告 | 转正 / 不转正的决策依据 | M3 |
| **ST.2 锚点入口下线** | 移除锚点选择入口；A 路存量用户的迁移引导（第二次创作默认引导 B 路）；存量作品与留痕数据的归档处理 | 单入口产品 | ST.1 |
| **ST.3 原生 App 发布** | 复用阶段一前端代码经 Capacitor 类方案打包（NFR-5.3）；iOS / Android 双端上架（NFR-5.5）；推送由邮件切原生（NFR-5.4）；原生壳选型实测（T-13） | 原生 App 上架 | ST.2 |
| **ST.4 转正后复验** | 外部用户测试重跑（**仅 B 路**）；确认移除锚点后启动率与完成率未崩塌 | AT.2/AT.4 实证 | ST.3 |
| **ST.5 合规清理** | 源作品引用移除；留痕数据归档策略；用户协议与隐私声明更新 | 合规闭环 | ST.2 |

**M-T 出口检查：**

- [ ] 三条退出条件均满足（AT.1）
- [ ] **移除锚点后，B 路启动率不低于移除前 A 路的 80%**（AT.2，防用户大规模流失）
- [ ] 原生 App 上架并通过审核（AT.3）
- [ ] 转正后外部用户测试通过（AT.4）
- [ ] 合规清理完成，无残留的源作品内容引用

> **未通过处置**：若 AT.1 不满足，按 PRD §0.8.5 评估转为衍生创作平台 + 正版授权路径；若 AT.2 不满足（用户大规模流失），须暂停下线、保留锚点入口并重新评估退出阈值。
> **被动转正预案**：若因外部压力（下架通知 / 支付关停 / 法律主张）被迫提前转正，**不得因用户量未达标而拖延**——被动切换的代价高于主动切换（PRD §10 条件 7）。此时 ST.1 可跳过，直接进入 ST.2。

---

## 6. M4 商业化

**目标**：单部毛利为正。
**验收**：PRD §6 A4.1

> **执行顺序**：M4 的正式定价须在 **M-T 转正完成之后**。阶段一（M2/M3）的支付链路为测试模式（S2.13），不向用户就锚点功能收取长期费用。

| 切片 | 任务 | 依赖 |
|---|---|---|
| **S4.1 定价模型** | 基于 M3 实测成本（DeepSeek 档 ¥40–85/部）确定定价与免费额度（**U-5**） | M-T |
| **S4.2 计费与订阅** | 计费、订阅、发票（复用 S2.13 支付链路，切正式模式） | S4.1 |
| **S4.3 毛利验证** | 单部毛利为正的实证 | S4.2 |

---

## 7. 阶段二（预留，本阶段不投入）

仅在 PRD §1.1 阶段二触发条件满足后启动：

- 英语 `KnowledgePack` / `StyleTemplate` / `ReviewRubric` / `ReaderPersona` 建设
- 西方类型文学 / 通用场景包建设
- 英语母语用户获取与验证

**当前不得投入任何资源。**

---

## 8. 已知技术债与处置时点

| 债 | 处置时点 | 跟踪 |
|---|---|---|
| `store.py` 单文件 1549 行 | M2 前评估拆分（C 端多用户后压力上升） | Tech-Spec T-3 |
| 文件型持久化 | **M2 前**评估 PostgreSQL（多用户隔离要求提前） | Tech-Spec T-3 |
| 无外部消息队列 | M3 前评估引入 | Tech-Spec T-4 |
| `KnowledgePack` / `ScenarioRule` 接口签名未定 | M0 完成（S0.2） | Tech-Spec T-5 |
| `ModelRouter` 路由策略未定 | M1 完成（S1.8） | Tech-Spec T-6 |
| 跨作品相似度阈值未标定 + 索引选型未定 | M2 完成（S2.10） | Tech-Spec T-7 |
| `diff_translator` 表达能力边界未定 | M2 完成（S2.3） | Tech-Spec T-8 |
| 关键节点裁决超时策略未定 | M2 完成（S2.8） | Tech-Spec T-9 |
| 引导层放弃用户的成本回收策略未定 | M2 完成（S2.6） | Tech-Spec T-10 |
| `structure_extractor` 抽取粒度与跨品类可抽取性未定 | M2 完成（S2.1B） | Tech-Spec T-11 |
| 锚点集中度干预阈值未定（初值 20%） | M2 完成（S2.1C） | Tech-Spec T-12 |
| 转正期原生壳方案与代码复用率未定 | M-T 完成（ST.3） | Tech-Spec T-13 |
| 前端业务层/UI 层若未严格分离，转正期需重写 | **M2 完成（S2.7，NFR-5.3）** | **返工成本极高，须在阶段一落实** |

> **持久化时点前移说明**：v2.0 将 PostgreSQL 评估放在 M3 前，因当时 C 端在 M3。v3.0 C 端上移至 M2，多用户数据隔离（G-9）在文件型持久化上难以稳妥实现，故评估时点同步前移。

---

## 9. 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-31 | v1.0 | 首版建立。文档体系拆为 PRD / Tech-Spec / Plan 三件套，与 Regent 同构；原 Plan.md 中的架构内容迁移至 Tech-Spec，本文只保留执行清单 |
| 2026-08-31 | v1.1 | 文档体检修正：§0.3 U-3 拆解行数由约 780 行对齐为约 875 行（纳入 `canon.extract_chapter_update` 语义反转），与 Tech-Spec §9.2 一致 |
| 2026-08-31 | v2.0 | 重大迭代对齐 PRD/Tech-Spec v2.0：① §0 前置条件改为"已满足"，落地 U-1/U-2/U-3 三项决策；② 新增 S0.2 场景包机制、S0.8 ModelRouter、S3.9 自托管后端、G-6 领域无关守卫；③ M0 出口检查新增"领域无关验证"；④ 技术债表纳入 T-5/T-6；⑤ 成本依据改为 DeepSeek 档 |
| 2026-08-31 | v3.0 | 重大迭代对齐 PRD/Tech-Spec v3.0（C 端定位）：① **里程碑顺序调整**——原 M2 长篇能力与 M3 C 端外壳互换，新序为 M0 内核 → M1 一致性（技术闸口）→ **M2 C 端可用（产品闸口）** → M3 长篇能力 → M4 商业化；② M0 新增 **S0.3 目标与关键路径**切片，`StoryGoal` / `CriticalPath` 在 M0 即就位（A0.5），避免 M2 退化为套壳；③ M2 由 9 个切片扩为 **14 个**，新增引导层（S2.1）、场景候选（S2.2）、关键路径界面（S2.3）、站外通知与裁决（S2.8）、多样性指标（S2.10）、外部用户可用性测试（S2.14）；④ 新增守卫 **G-7 零门槛 / G-8 种子隔离 / G-9 owner 隔离**；⑤ M1 新增 S1.9 关键路径粒度实测（U-11）与启动率阈值标定；⑥ 技术债新增 T-7~T-10，**PostgreSQL 评估时点由 M3 前移至 M2 前**；⑦ 明确禁止以"加字段让用户自己填"的方式通过 A2.1 |
| 2026-08-31 | v3.1 | 重大迭代对齐 PRD/Tech-Spec v3.1（分阶段策略）：① 新增锚点创作切片组 **S2.1A–S2.1D**（锚点入口 / 结构抽取器 / 变异门与同源判定 / 合规留痕），全部为阶段一专属；② 新增守卫 **G-10**（抽取器只出结构，守 R-0.8.1 合规底线）与 **G-11**（`entry_path` 不可缺省，守 FR-0.15 分路径统计）；③ S2.7 前端改为 **Web PWA** 并强制业务层/UI 层分离（NFR-5.3），S2.8 通知改为邮件兜底 + 渠道抽象；④ M2 出口检查新增 **A2.9（A/B 双路径分组，不可妥协，否则 AS-4 是被绕过而非被证伪）** 与 A2.10（与源作品结构相似度 < 0.45）；⑤ 新增 **M-T 转正里程碑**（条件触发，ST.1–ST.5 / AT.1–AT.4），含被动转正预案；**M4 商业化前置依赖由 M3 改为 M-T**（不在注定要移除的功能上收费）；⑥ 技术债新增 T-11~T-13 与"业务层/UI 层未分离则转正期需重写"（高返工风险，须在阶段一落实）；⑦ §0 前置条件纳入 U-13/U-14，其中 **U-13（终局定位）未拍板，不阻塞 M0–M3 但阻塞转正期规划** |
| 2026-08-31 | v3.1.1 | 文档体检与补录（对齐 PRD v3.1.1）：① §0 前置条件表纳入 **U-15**（锚点作品范围），标明其阻塞 S2.1A 锚点库选型；② S2.1A 切片补充"锚点库按授权状态标记、可整体下线未授权部分"（U-15 采纳 B 方案时的快速切回前提）；③ **新增 S2.1A 动工前置警告**——U-15 不可边做边定，否则锚点库与合规留痕需返工；④ 校验三份文档的表格渲染完整性与跨文档引用一致性（PRD U-8 ↔ Tech-Spec T-11 ✓，U-13/U-14/U-15 ↔ Plan ✓） |
