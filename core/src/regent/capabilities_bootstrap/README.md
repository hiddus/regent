# core/src/regent/capabilities_bootstrap

引导期能力声明 JSON（打包进 wheel，由 Capability Pool 在引导时加载）。

## 目录内容

文件：
- `allowlisted_http_source_v1.json`
- `delivery_review_v1.json`
- `product_surface_v1.json`

> 本 README 由目录实际内容生成，反映当前结构；如用途有变请同步更新。
