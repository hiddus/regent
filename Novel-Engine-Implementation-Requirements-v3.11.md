# Novel Engine 落地需求文档（历史快照）

> **版本**：v3.11 · **建立**：2026-09-01 · **状态**：SUPERSEDED
>
> **替代关系（2026-09-02）**：本文件中的有效结论已经由六角色重新核对最新代码和竞品后，吸收到 `Novel-Engine-PRD.md` v4.0、`Novel-Engine-Tech-Spec.md` v4.0 与 `Novel-Engine-Plan.md` v5.0。本文件只保留为审查历史，不再覆盖主文档，不再充当开工前置或冲突裁决源。
> **来源**：由 UI / UX / 产品 / 技术 / 后端 / 前端 六个角色分别审查 `Novel-Engine-PRD.md` v3.11、`Novel-Engine-Tech-Spec.md` v3.11、`Novel-Engine-Plan.md` v4.7 后收敛而成。原始发现 73 条，去重归并为 **20 个问题簇**。
> **与 PRD 的关系**：PRD 定义"要什么"，本文件定义"**每一项做到什么程度算完成**"以及"**开工前必须先拍板的事**"。**本文件不改方向**，只补齐"从需求到可开工"之间缺失的那一层。
> **版本头说明**：方向已于 v3.11 锁定（阶段一目标用户 = 网文平台的非头部兼职作者；价值主张 = 多开几本）。本文件所有内容以该方向为前提。
>
> **编号体系已失效（2026-09-02 复核）**：本文件正文引用的 `FR-x.y`、`NFR-x.y`、`G-22`～`G-24`、`T-25`～`T-28`、`U-30`～`U-36`、`R-x.y.z` 全部属于旧编号体系，在 v4.0 / v5.0 主文档中已不存在。**不要按这些编号去主文档里找对应关系**。现行编号是 `FR-01`～`FR-25`、`G-01`～`G-23`、`D-01`～`D-06`。本文件只保留"当时为什么这样裁"的理由，不是现行结论。

---

## 0. 怎么用这份文档

| 层 | 内容 | 谁看 | 状态 |
|---|---|---|---|
| 第 1 层 | §1 审查结论汇总 | 全体 | 只读 |
| 第 2 层 | §2 必须先拍板的 12 件事 | 决策人 | **阻塞开工，须逐条勾掉** |
| 第 3 层 | §3 角色间的冲突与裁决 | 全体 | 已裁决，有异议在 §2 提，不在 §3 提 |
| 第 4 层 | §4 可开工硬契约 | 研发 | 拍板后即为实现依据 |
| 第 5 层 | §5 分角色开工顺序 | 研发 | 排期依据 |
| 第 6 层 | §6 本次新增未决项 | 决策人 | 追加到 PRD / Tech-Spec 表尾 |

**三条使用规则**：

1. **（已不适用）** 原文要求"§2 未勾完，§4 不得开工"。该前置已由 PRD v4.1 第 10 节的 D-01～D-06 取代，现行开工条件见 PRD 与 Plan v5.1。
2. **本文件与主文档冲突时，一律以主文档为准。** 本文件保留的是裁决当时的理由；结论已在主文档中重新编号落地。
3. **本文件不再作为任何需求的验收依据**，也不再新增方向性需求。

---

## 1. 六角色审查结论汇总

| 角色 | 审查范围 | 原始发现 | 归并后问题簇 | 一句话结论 |
|---|---|---|---|---|
| 产品 | 目标与人群、优先级、验收标准、非目标、未决项、停止投资条件 | 12 | C-01 C-02 C-03 C-07 C-08 C-09 C-12 | **地域锁定后全篇口径未改**，且二选一悬空项（是否做仿写起步、是否收费）直接卡住 15 条需求 |
| UX | 使用场景、用户旅程、引导层、目标与关键路径层 | 12 | C-03 C-14 C-15 C-20 | **"用户要改正文"这条必然高频路径没有出口**，文档自认是头号风险却没定义任何交互 |
| UI | C 端产品能力、交付形态、产出去向告知、引导层界面 | 10 | C-04 C-09 C-11 C-14 C-15 | **"多开几本"是核心价值主张，却没有任何多作品并行的界面信息架构** |
| 技术 | Tech-Spec 全文 + 21 条架构守卫 | 12 | C-05 C-06 C-08 C-16 C-17 C-18 | **主流程没有状态机、没有幂等键、没有恢复契约**；两条核心不变式（信息泄露判定、阅读不触发生成）无可实现的检测点 |
| 后端 | 商业模式与账本、非功能需求、埋点、技术待标定项 | 13 | C-05 C-06 C-07 C-08 C-19 | **钱和额度的流转写不出正确的表**：状态无转移条件、扣减无时机、结算无幂等键、冲销无路径 |
| 前端 | 交付形态、C 端能力、引导层、PWA 与移动端约束 | 14 | C-04 C-10 C-11 C-13 C-14 | **没有页面清单、没有路由表、没有状态同步机制**；且中国大陆环境下"手机上被通知回来裁决"这个主场景当前的唯一渠道是邮件，做不到 |

> **收敛时的一个观察**：六个角色各自独立审查，但在 **5 个问题上撞到了同一处**——多作品并行（产品/UI/前端）、改稿请求（产品/UX）、长任务状态机（技术/后端/前端）、分层字段（技术/后端）、产出去向告知（产品/UI/UX/技术）。**撞车处就是真正的阻塞点**，其余多为各角色的专业补漏。

---

## 2. 必须先拍板的 12 件事

> 排序依据：**不拍板的返工代价**（不是难度，不是重要性）。每条都给了我的建议与理由，勾掉即可。

### 2.1 【B-1】M2 要不要做"仿写起步"（文档内称锚点创作）

| 项 | 内容 |
|---|---|
| 问题（人话） | 用户开新书时，能不能选"照着某本热门作品的结构来"？ |
| 为什么现在必须定 | 这不是加一个开关。含锚点 = 需要结构抽取器 + 变异门 + 留痕三套独立链路 + 引导层双入口 + 15 条需求；不含 = §0.8 整节降级。**两条路的前端入口、引导层流程、埋点分组完全不同，做完再改等于重做引导层。** |
| 选项 | A. 阶段一做；B. 阶段一不做，降级为阶段二评估 |
| 我的建议 | **B（不做）**，三条理由 |
| 理由 ① | 锁定人群的瓶颈是**时间不是想法**（PRD §2.2.2.3 已锁，他有存货有开篇）。锚点解决的是"没有想法"，不是他的瓶颈 |
| 理由 ② | 锚点产出**不得变现**（硬约束 R-0.8.2），而锁定人群的动机里带着变现成分。**给他一条"能做但不能赚钱"的路径，引力方向是反的** |
| 理由 ③ | 成本：三套链路约 2–3 人周，且 PRD §8「不做原生 App」的**头号理由**依赖锚点存在（见 B-2 连带项），做锚点要连带重写那一节 |
| 反例条件（写死） | 若改稿率实测（B-2）中"用户第一句话给不出任何方向"的比例 > 50%，则重开此议题 |
| 不拍板会怎样 | 引导层 15 条需求 + 抽取/变异/留痕三链路整体白做或整体缺失 |
| 谁拍 / 何时 | 决策人 / M0 立项前 |

> **B-1 的连带项（选定 B 时必须同时改，否则文档自相矛盾）**：PRD §5.5「为什么阶段一不做原生 App」的第 1 条理由（上架审核与锚点功能冲突）**失效**，第 4 条「海外中文用户的传播链在微信内」**主语写错**，第 5 条「Web 可直连 Stripe 自由试价」**渠道写错**。**结论（阶段一做 Web）不变，但三条理由须重写。** 见 §3 冲突 8。

### 2.2 【B-2】改稿请求给不给通道（文档内称 U-30，标注阻塞 M0）

| 项 | 内容 |
|---|---|
| 问题（人话） | 他看到不满意的一段，一定会点"我要改这段文字"。而产品契约只许三个动作（① 定目标 ② 调关键路径 ③ 裁决关键节点），没有"改正文"。给不给？ |
| 为什么现在必须定 | **这是本次审查中唯一一个"人 × 契约"的直接对撞**，也是唯一一个**在没有产品的时候就能测出来**的关键假设。测法：找 20–30 个番茄长尾作者做纸面演示（Wizard-of-Oz），2–3 周，约 ¥2–3k，**零开发**。 |
| 选项 | A. 完全不给（只回落到三动作）；B. 只给"报错"（事实性错误），不给"改文字"；C. 给完整改稿 |
| 我的建议 | **先测，再定。但判定方向现在写死**——测出高改稿率时，**调整的是契约的话术与回落路径，不是开放改稿**。C 直接否决：一旦开改稿口子，长程一致性与声纹分离两条核心壁垒同时失效，且不可逆 |
| 我建议 B 的具体形态 | 入口叫**"报错"不叫"修改"**。先勾选错的类型（人物关系 / 时间 / 地点 / 数值 / 事件是否已发生），再填内容，系统回显"我将把 XX 改为 YY，并同步 N 处引用，确认吗"。判定为评价性意见（"这段太平淡"）时回显："这不是错误，我改不了文字。你可以：调整这一段的走向 / 留到下一个裁决点"，两个按钮直达对应界面 |
| 与已有文档的一处冲突（我的裁决） | PRD 已有纠错通道（FR-4.9），但**提防 ⑰ 禁止它在 M1 一致性测量期开放**（理由是会高估指标）。**我不同意用"关闭通道"来解决测量效度问题**——那等于让用户拿着明知写错的内容还不让改。正确解法：**通道照开，但把"是否经过纠错"记入分层字段，测量时只统计未纠错子集**。见 §3 冲突 9 |
| 不拍板会怎样 | 在验证人群与契约适配性之前就建引导层；测出高改稿率则引导层的拒绝交互与三动作边界全部返工 |
| 谁拍 / 何时 | 决策人拍"是否投这 ¥2–3k" / **排在全部开发之前**（Plan §11 已有 B0.0 条目） |

### 2.3 【B-3】地域口径的八处漏改（文档内称 U-31，阻塞 M0 前半段）

| 项 | 内容 |
|---|---|
| 问题（人话） | 阶段一地域已从"海外"锁定为"中国大陆"，但文档里还有一整节写着"部署于海外、不做国内合规" |
| 已核验属实（我逐行读过） | ① §5.3 标题「部署与合规（**海外**）」；② NFR-3.1「部署于**海外**基础设施，不依赖境内部署」；③ NFR-3.2「适用 **GDPR / CCPA**」；④ NFR-3.3「支付支持 **Stripe / Apple Pay**」；⑤ NFR-4.1 内容分级「适配**海外**发布平台要求」；⑥ §8 非目标第 9 行「面向**国内市场**的部署与合规 | 发布地在海外」；⑦ §5.5 理由 4「**海外中文**用户的传播链在微信内」；⑧ §5.5 理由 5「可直连 **Stripe** 自由试价」。另有验收标准与假设各 1 处待连带修订 |
| 我的建议 | **全部改为大陆口径**，并**补三条文档现在一句都没写的硬合规**：生成式 AI 服务备案与算法备案的责任方、ICP 与网络出版资质前提、内容审核机制（谁审、误判谁纠） |
| 不拍板会怎样 | 运维按海外买机房、支付按 Stripe 对接、测试按海外链路验收；到联调时备案 / 支付 / 广告三处全错。**这类错误属于"不可事后补"，越晚发现代价越大** |
| 谁拍 / 何时 | 决策人 + 法务 / **埋点与引导层设计之前** |

### 2.4 【B-4】阶段一接不接真实支付

| 项 | 内容 |
|---|---|
| 问题（人话） | 用户要不要掏钱？长尾作者付费能力存疑（已有未决项 U-23），但 §9.5.7 定的是"创作付费" |
| 我的建议 | **M2 内部测试期一律免费计量、不接真实支付**。付费与否由 U-23 在 M2 用户测试期实测后决定；据此把免费额度规模（U-18）的阻塞点从 M2 后移到 M3 |
| 理由 | 付费能力是**待实测的假设**，不是可以推定的前提。接了支付又发现收不上来，要同时改计费模块、额度规则、退坡曲线三处 |
| 不拍板会怎样 | 计费模块与额度退坡规则的形态都要重做 |

### 2.5 【B-5】手机上能不能"调整关键路径"

| 项 | 内容 |
|---|---|
| 问题（人话） | PRD 把移动端定为"只可阅读、看进度、做裁决"（FR-6.9，P1）——**三动作契约在手机上只剩两个**。而锁定人群的碎片时间主要在手机上 |
| 我的建议 | **必须支持，升 P0**。关键路径在手机上用纵向卡片列表 + 上移/下移按钮（不做拖拽），改/插/删/锁走底部动作条 |
| 理由 | 契约被截断 = 契约不成立。且移动端不做等于要维护两套组件树，违反 NFR-5.3（业务层与 UI 层分离、UI 层可整体替换）的预留原则 |
| 不拍板会怎样 | 关键路径组件出桌面 / 移动两套，或上线后契约缺动作 |

### 2.6 【B-6】用户分层字段：不可变快照 还是 事后推断

| 项 | 内容 |
|---|---|
| 问题（人话） | 文档要求"是否为专业作者"在会话创建时一次写入、不可变更；又要求它由行为信号推断。**创建时还没有任何行为信号**——两条不可能同时满足 |
| 我的建议 | **拆两张表**（见 §4.6）：不可变的**获客维度**（进入路径、注册渠道，创建时写死）+ 可变的**画像维度**（人群分层、风险等级、推断时间、推断规则版本，每次重算留版本快照） |
| 不拍板会怎样 | 分层数据一旦按错误口径落库，U-16（他人作品阅读占比）与"多开"验证数据**永久无法分离**（文档自己写过这条） |

### 2.7 【B-7】成本怎么记账、钱从哪来怎么分池

| 项 | 内容 |
|---|---|
| 问题（人话） | 成本按"每次模型调用"记还是"按章汇总"记？用户自己付的钱和平台给的免费额度，混在一个字段里，将来回收时归谁？ |
| 我的建议 | **append-only 成本流水 + 派生余额**（不要状态枚举）；资金来源分池（平台额度 / 用户自付），回收时分池回收，**平台池优先**，用户池回收部分进用户余额而非平台收入 |
| 不拍板会怎样 | **这是账上唯一不可逆的返工**：事后把汇总拆成流水 = 重建全部成本表 + 重算已结算账本 + 已分成金额需冲回 |
| 谁拍 / 何时 | 后端 + 决策人 / **建第一张表之前** |

### 2.8 【B-8】额度是生成前预扣还是生成后实扣

| 项 | 内容 |
|---|---|
| 问题（人话） | 重跑一章、或跑失败了，扣不扣钱？ |
| 我的建议 | **两段式**：`reserved`（预估冻结，乐观锁）+ `consumed`（按实际结算，释放差额）；终态为失败 / 取消时**全额释放**。每次模型调用记成本明细，账本按调用汇总、界面按章汇总 |
| 不拍板会怎样 | 并发下必然超发，超发部分是平台净损失且**事后追不回** |

### 2.9 【B-9】反套利上限 vs "多开几本"（同型问题第三次出现）

| 项 | 内容 |
|---|---|
| 问题（人话） | 反套利要求限制"同时在做的作品数"；而产品要考核的正向指标恰恰是"同时在跑几本"。**同一个数字既是成绩单又是警报器** |
| 我的建议 | **换口径，不调阈值**：限「月生成字数」+「月新开作品数」；**在制品并发数单列，阈值取 5**（显著高于典型多开数 2–3，让它成为真正的异常闸而不是日常天花板） |
| 提防（值得单独记一笔） | **同一个数值不得同时充当两个相反的判据。** 这是该型问题在本项目中的**第三次**出现（第一次：第二部作品启动率；第二次：价值主张里的产出速率；第三次：此处）。**解法永远是换判据，不是调阈值** |
| 不拍板会怎样 | 限额取值取不出来（单位经济未重算），且可能与核心价值主张直接互斥 |

### 2.10 【B-10】长任务状态机、幂等键粒度、超时策略

| 项 | 内容 |
|---|---|
| 问题（人话） | 一本书跑几天，中途断电 / 失败 / 用户关页面，从哪一拍续？续跑会不会重复扣费、重复裁决？ |
| 我的建议 | **三级状态机**（见 §4.2）：任务 → 章任务 → 章内节拍。断点落在"章 + 节拍序号"；每次模型调用写**带幂等键的尝试记录**，重试复用已成功调用、不重复计费；并发按用户级 / 全局级两级信号量排队；**挂起任务释放 worker，只占在制名额**，并配默认裁决时限与默认分支 |
| 不拍板会怎样 | 重试重复计费会在账本留下**无法甄别真假的双份成本**，只能人工核 |

### 2.11 【B-11】通知渠道怎么组合（中国大陆 + 微信内是硬约束）

| 项 | 内容 |
|---|---|
| 问题（人话） | 主场景是"用户在手机上被叫回来做裁决"。但当前唯一渠道是邮件，而**中国大陆 Web Push 依赖 FCM 不可达；微信内置浏览器既不支持 PWA 安装也不支持 Web Push** |
| 我的建议 | **三路并行**：① 邮件；② **站内持久"待裁决收件箱"（不依赖任何推送，升 P0，这是唯一可靠通道）**；③ 短信 / 公众号模板消息兜底。每条通知带一次性 token 深链，落地即裁决、免登录 |
| 不拍板会怎样 | "手机上被叫回裁决"这个主场景做不成，M2 相关切片无法验收 |

### 2.12 【B-12】前端框架与 PWA 工具链

| 项 | 内容 |
|---|---|
| 问题（人话） | 路由库、状态管理、PWA 方案现在一个都没定，而开工第一天就要选 |
| 我的建议 | **Vite + React + TypeScript + vite-plugin-PWA（Workbox）**。理由：NFR-5.3 要求业务层框架无关（React 生态下最容易守住）；阅读端需要独立 bundle（见 §4.3）；微信降级环境需要轻量 |
| 不拍板会怎样 | 路由与 Service Worker 两处返工 |

---

## 3. 角色之间的冲突与我的裁决

> 下列冲突是六个角色各自提出、彼此互斥的。**已裁决，不再讨论**；有异议请回到 §2 提拍板，不要在 §3 提。

| # | 冲突 | 我的裁决 | 理由 |
|---|---|---|---|
| 1 | 承诺"用户可完整导出全文"（FR-7.4）vs 产出去向约束（R-2.2.1，产出不得流向设限平台） | **完整性 = 内容完整，不包**含**去除标识的自由**。导出文件一律携带 AI 生成标识；U-26（是否提供去标识导出）给临时结论：**阶段一默认带标识、不提供去标识导出** | 二选一会让研发自行取舍。写死后两边都成立 |
| 2 | 反套利"在制品数上限" vs 价值主张"多开几本" | 见 B-9：**换口径**，并发数单列且阈值 5 | 提防：同一个数不得同时是成绩单和警报器 |
| 3 | 分层字段要求"创建时不可变" vs "由行为信号推断" | 见 B-6：**拆两张表** | 不可变字段承担不了"事后识别并隔离"的职责 |
| 4 | 移动端只给裁决（FR-6.9 P1）vs 兼职作者的碎片时间在手机 | 见 B-5：**调整关键路径升 P0** | 契约被截断 = 契约不成立 |
| 5 | §8 非目标写"专业模式可选提供"，但 §4 全文没有该模块的任何需求 | **阶段一不做专业模式**。§8 两行的"可选"改为"阶段一不提供，留待阶段二评估"；FR-1.2 / 1.3 / 1.13（声纹独立可编辑、结构化设定编辑）降为阶段二 | 悬空的"可选"是绕过三动作契约的通用口子；评审时无法判定"这个能不能做" |
| 6 | 产出去向告知要求"首次导出前写入"（G-21）vs 告知记录里存的是"本次导出的去向" | **拆成两张结构**：`ExportGate`（用户 × 作品级，含告知版本与满足时间）+ `ExportNotice`（每次导出一条）。拦截查 gate，**"未告知"定义为"未满足 或 版本已升级"** | 否则第二次导出没有写入路径，且版本升级后不会重新告知 |
| 7 | G-12 要求阅读链路"熔断并降级为纯只读分发" | **语义自相矛盾，改为直接抛错不降级**。阅读路径本就不该有"增强"可拒绝；熔断后请求成功但计数非零，与"非零即生产事故"冲突 | 降级意味着承认阅读路径上有生成能力 |
| 8 | 「不做原生 App」的头号理由依赖锚点存在，而 B-1 建议不做锚点 | **结论不变，理由重写**。新三条理由：① 迭代速度（M2 需多轮外部用户测试，App 发版审核 1–7 天）；② 样本成本（下载摩擦直接推高样本成本）；③ 支付不被渠道扭曲（国内为应用商店 IAP 抽成，Web 可直连微信 / 支付宝自由试价） | Web 优先是正确结论，但不能靠一条即将失效的理由支撑 |
| 9 | 纠错通道 vs 提防 ⑰（禁止在一致性测量期开放纠错，否则指标被高估） | **通道照开**，把"是否经过纠错"记入分层字段，**测量时只统计未纠错子集** | 用"关闭功能"换取"测量效度"是错的取舍——牺牲的是用户，保住的是指标 |
| 10 | 首章 15 分钟硬指标（NFR-2.5）vs 单章多拍串行的事实 | **首章拆两阶段**：先出设定与关键路径预览（同步返回），后台补正文。到点返回预览并续跑；超时按已完成节拍的**实际成本**记账，用户侧单章成本展示区分"部分产出" | 15 分钟是硬指标，但部分产出的 token 已真实消耗，既不能不计账也不能按整章计 |
| 11 | 用户撤回公开（FR-7.7）后账本怎么办，文档全程未写 | **公开状态与账本解耦，账本永不清零**。撤回仅停止新增收入入账，已确认收入保留、成本继续挂账 | 反复公开 / 撤回会产生对不上的账 |
| 12 | 引导层要用户确认"爽点密度 / 节奏分布"（FR-0.10/0.11），而这正是明令禁止的"让用户维护结构化字段" | **回述只留一句人话**（"这本大致是：每 3 章一个小高潮，前 30 章压着主角，中段翻盘"），配 2–3 张**节奏方案卡**供选，不提供数值输入。用户说"再密一点"时系统换成下一张卡，**不暴露参数** | 用自然语言回述一个密度值，用户无法调整；加一句"再密一点"就是填空的变体，直接违反三动作契约 |

---

## 4. 可开工硬契约（拍板后即为实现依据）

> 本节是**草案**，§2 勾完后生效。每一条都是"两个人不商量也会写出一样的代码"所需的粒度。

### 4.1 作品状态枚举 `WorkStatus`

| 状态 | 含义 | 需用户动作 | 界面呈现 |
|---|---|---|---|
| `onboarding` | 引导层会话中，作品对象未确立 | 是 | 引导流 |
| `running` | 推演进行中 | 否 | 进度条 + 当前章 |
| `pending_decision` | 关键节点挂起等人裁决 | **是** | 待裁决卡片 + 倒计时 |
| `paused_quota` | 免费额度耗尽 | **是** | 暂停页 + 额度重置时间 |
| `paused_cost` | 预算超限 | **是** | 暂停页 + 三个按钮（提高额度 / 缩短篇幅 / 暂停） |
| `recomputing` | 关键路径变更后重算受影响章节 | 否 | 重算进度 |
| `failed` | 失败 | **是** | 错误一句话 + 重试 + 从最近完成章恢复 |
| `done` | 已完成 | 否 | 完本态 |

> **配套**：每个挂起态必须同时返回 `reason`（一句话）、`blockedSince`（时间戳）、`availableActions`（动作列表）。**前端只渲染后端返回的动作，不自行判断状态**——后端加一态，前端不崩。
> **成本/进度**：另有 `public_state`（私有 / 公开中 / 已撤回），**与账本解耦**（见 §3 冲突 11）。

### 4.2 章任务三级状态机

| 层 | 结构 | 幂等键 | 失败处理 |
|---|---|---|---|
| 任务（Task） | `StoryRun{work_id, status, cursor}` | `work_id` | 挂起 / 终止 |
| 章任务 | `ChapterRun{work_id, chapter, step, status, attempt, checkpoint_ref}` | `work_id + chapter + attempt` | 分类：可重试 / 需人 / 终止 |
| 章内节拍 | `step ∈ {assemble, perform, direct, weave, review, canon}` | `work_id + chapter + step + attempt` | 从最近完成节拍续 |

**规则**：
1. 断点落在"章 + 节拍序号"。
2. 每次模型调用写带幂等键的尝试记录；**重试复用已成功调用，不重复计费**。
3. 挂起任务**释放 worker，只占在制名额**。
4. 关键节点挂起**必须有默认裁决时限**（建议 48 小时）与默认分支，到期显示"已按 XX 继续，可回退"，回退入口挂在时间轴上。
5. 守卫测试：`kill -9` 后重放同一章，断言**无重复扣费、无重复裁决**。

### 4.3 页面 × 路由表

| 路由 | 页面 | 说明 |
|---|---|---|
| `/` | **工作台（默认落地页）** | 一级信息是四栏：待我裁决 / 进行中 / 已暂停 / 已完成 + 全局待办计数。作品列表降为二级 |
| `/new/:sessionId` | 引导流 | 澄清 / 候选 / 微调全挂 session；服务端持久化草稿 + 本地镜像，可跨设备续接 |
| `/works/:id` | 作品详情 | 分段：进度 / 关键路径 / 章节 / 账本 |
| `/decide/:nodeId` | 裁决深链 | 通知直达，带一次性 token，免登录 |
| `/read/:workId/:chapter` | **阅读端（独立入口与独立 bundle）** | 见下 |
| `/me` | 账户 | 额度、账本、公开设置 |

**三条硬规则**：
1. **工作台是默认落地页，不是作品列表。** "多开几本"是核心价值主张，首页必须先回答"我这几本现在都怎么了"。
2. **阅读端 `/read/*` 用独立入口与独立 bundle**，构建期不含任何生成类 action；运行时断言阅读路由的网络层禁止命中生成接口（与后端守卫 G-12 对齐）。
3. **微信内置浏览器单列为"降级环境"**：功能子集 = 读 + 看进度 + 裁决；明确告知"安装到桌面与消息推送在此环境不可用"，引导用系统浏览器打开。

### 4.4 事件流契约（长任务进度同步）

| 项 | 约定 |
|---|---|
| 默认通道 | SSE `/works/:id/events` |
| 降级 | 微信内 / 弱网降级为 30 秒轮询 |
| 事件类型 | `chapter_done` / `pending_decision` / `quota_paused` / `cost_paused` / `recompute_progress` / `failed` |
| 去重 | 每条事件带**单调递增 `seq`**，前端按 seq 去重与断线补帧 |
| 首章等待 | 确认启动后**立刻跳 `/works/:id` 并后台推演**，首章前该页显示设定摘要 + 路径只读预览 + 进度条，允许离开，回来靠事件流续上 |

### 4.5 金额与额度规则

| 项 | 规则 |
|---|---|
| 精度 | **整数分**，带货币字段 |
| 成本 | append-only 流水，**不设状态枚举**；派生余额 = 累计成本 − 累计已回收 |
| 资金来源 | 分池：`platform_grant`（平台额度）/ `user_paid`（用户自付）/ `onboarding`（引导层，作品创建前） |
| 额度 | 两段式 `reserved` / `consumed`：章任务启动按预估冻结（乐观锁），结束按实际结算并释放差额；失败 / 取消**全额释放** |
| 引导层成本 | 作品创建前挂 `session_id`；作品创建后归集到作品；未成作品的会话成本进平台成本表并照扣用户额度 |
| 结算 | 以「作品 + 结算周期」为**唯一幂等键**，一周期只允许一个已提交批次；金额先进冻结余额，过账期转可提现 |
| 分成比例 | 按**收益产生日**锁定当时的比例与策略版本快照，不得事后下调已产生的收益 |
| 反刷冲销 | 收入流水带 `pending / confirmed / reversed` 状态；**分成只对超过观察窗的已确认收入结算**；冲销写负向流水并回退已回收额与未回收余额，优先冲冻结余额，不足记用户欠账 |
| 打赏 | 流水带状态（待结算 / 已结算 / 已退款 / 拒付）与**结算冻结窗**，冻结期内不计入可分成基数；退款按逆序冲销：先冲未发分成，再回退已回收额，不足记欠账 |

### 4.6 分层与埋点（两张表，不可混淆）

| 表 | 内容 | 可变性 |
|---|---|---|
| `SessionSegment` | 获客维度：进入路径、注册渠道、地域 | **创建时写死，不可变**，可为空 |
| `BehaviorRollup` | 画像维度：人群分层、风险等级、推断时间、推断规则版本 | **可重放重算**，每次变更留版本快照 |

**规则**：
1. 告知触发条件写成"**未告知 或 告知版本已升级**"，**不依赖分层字段**（否则分层推断不出来时守卫就悬空）。
2. 埋点落**明细事件**（作品创建 / 章节生成 / 关键路径调整并标来源=用户或系统 / 完本 / 弃坑 / 纠错），**聚合口径后置可重算**。
3. "在跑本数"定义写死：**近 7 日有章节生成 且 生命周期为进行中**。
4. "每部作品的关键路径调整次数"**严格按作品维度取均值，不得按账号求和**。
5. 断言：rollup 可由事件流重放复现。

### 4.7 新增三条架构守卫（原 21 条 → 24 条）

| ID | 守卫 | 依据 |
|---|---|---|
| **G-22** | 声纹区分度检测**下沉到场景展开的唯一入口**执行，每章必跑，低于阈值阻断入 canon；不得存在绕过它的正常代码路径（当前"专业模式"直接写场景卡即是一条旁路） | 技术审查 T-6 |
| **G-23** | 每次模型调用必带 `cost_scope ∈ {onboarding, generation, anchor}`；`generation` 类调用中 `work_id` 缺失数必须为 0；`QuotaLedger` 消费记录带 scope 维度 | 技术审查 T-4 |
| **G-24** | 导出接口参数为**枚举白名单**，且导出物字节流**不经过任何 LLM**（把"不得存在去 AI 味 / 改写以通过审核的 prompt 分支"换成可判定形式） | 技术审查 T-8 |

### 4.8 新增可验收需求条目

> 编号 `IMP-x`，用于本文件；并入 PRD 时按 §6 的规则追加。

| ID | 需求 | 验收方式 | 优先级 |
|---|---|---|---|
| IMP-1 | 首页默认为多作品工作台，按"待我裁决 / 进行中 / 已暂停 / 已完成"四栏分组，跨作品聚合待办计数 | 5 本同时跑时，一屏内可见全部待裁决，点击直达 | P0 |
| IMP-2 | 每个挂起态须返回 reason / blockedSince / availableActions；前端只渲染后端返回的动作 | 后端新增一个挂起原因，前端不改代码即可正确呈现 | P0 |
| IMP-3 | 关键节点挂起须有默认时限与默认分支；超时后按默认分支继续并提供可回退入口 | 模拟用户 48 小时不响应，任务继续且回退可用 | P0 |
| IMP-4 | 站内"待裁决收件箱"为不依赖任何推送通道的持久通道 | 关闭全部推送渠道后，用户重新登录仍可见全部待裁决 | P0 |
| IMP-5 | 移动端须支持关键路径的调整（改 / 插 / 删 / 锁 / 调序），不得只支持阅读与裁决 | 在 6.1 寸屏上完成一次节点调序并触发重算 | P0 |
| IMP-6 | 重跑与失败不得重复计费：幂等键到"章 + 节拍 + 尝试"粒度 | `kill -9` 后重放同一章，成本流水无新增 | P0 |
| IMP-7 | 产出去向告知须由 `ExportGate`（用户 × 作品级，含版本）判定，未满足或版本升级即拦截；拦截点在接口层，不得仅由 UI 前置提示实现 | 直接调用导出接口（绕过 UI）被拒绝 | P0 |
| IMP-8 | 导出文件一律携带 AI 生成标识；不提供去标识导出；导出参数白名单；导出字节流不经 LLM | 检查导出物含标识；检查代码库中无去标识分支 | P0 |
| IMP-9 | 纠错通道入口标识为"报错"而非"修改"；评价性输入须回落到"调整走向"与"留到下一个裁决点"两个具体动作 | 输入"这段太平淡"，看到两个直达按钮而非拒绝提示 | P0 |
| IMP-10 | 每次关键路径调整须展示受影响章节数 / 预计耗时 / 预计成本并确认；未固化窗口内的变更免确认但须给即时反馈 | 窗口外调整弹确认卡；窗口内调整出 toast | P0 |
| IMP-11 | 首章分两阶段：同步返回设定与关键路径预览，后台补正文；超时按已完成节拍实际成本记账 | 15 分钟内看到预览；用户侧成本区分"部分产出" | P0 |
| IMP-12 | 用户撤回公开后账本不清零：停止新增收入入账，已确认收入保留、成本继续挂账 | 公开→撤回→再公开，账目自洽 | P1 |
| IMP-13 | 埋点须落明细事件并可按事件流重放复现聚合结果 | 重放后 rollup 与线上一致 | P1 |
| IMP-14 | 阅读端为独立入口与独立 bundle，构建期不含生成类 action | 检查阅读 bundle 无生成接口引用 | P1 |
| IMP-15 | 大文本渲染性能：阅读器单章渲染 + 预取前后各 1 章；目录与成本面板虚拟化；4G 下 LCP ≤ 2.5s | Lighthouse 移动端实测 | P1 |
| IMP-16 | PWA：App Shell 预缓存 + 已入 canon 章节按章存 IndexedDB + 写操作进 outbox 队列（不做乐观更新，恢复后按 seq 重放，冲突由服务端 409 收敛）；任务运行期间禁止 SW 自动 reload | 离线中点裁决，恢复联网后正确同步 | P1 |
| IMP-17 | 分层字段拆为不可变获客维度与可重放画像维度，画像每次重算留版本快照 | 重放事件流可复现任一历史版本的 rollup | P0 |
| IMP-18 | 通知三路并行：邮件 + 站内收件箱 + 短信/公众号模板消息；通知带一次性 token 深链，落地即裁决、免登录 | 在微信内点击通知直达裁决页且无需登录 | P0 |

---

## 5. 分角色开工顺序与阻塞关系

| 顺序 | 角色 | 可开工的前提 | 产出 |
|---|---|---|---|
| **0** | 决策人 | — | 拍掉 §2 的 B-1 ~ B-12 |
| **0.5** | 用户研究 | B-2 拍"投这 ¥2–3k" | 改稿率实测（20–30 人 / 2–3 周 / 零开发），判定方向已预先写死 |
| **1** | 后端 | B-3 B-6 B-7 B-8 B-9 B-10 | 数据模型、账本、任务状态机、`WorkStatus` 与 `availableActions` |
| **2** | 前端 | B-5 B-11 B-12 **+ 后端给出 `WorkStatus` 枚举** | 工程骨架、页面 × 路由表（§4.3）、事件流（§4.4） |
| **3** | 技术 | B-1（是否做锚点决定执行拓扑的分支）+ 后端数据模型 | 章执行器状态机、信息集与泄露判定（§6 新增项）、守卫 G-22 ~ G-24 |
| **4** | UX | B-1 B-2 + 后端状态枚举 | 交互稿，优先做改稿回落路径（IMP-9）与多开工作台（IMP-1） |
| **5** | UI | UX 交互稿 + 前端页面表 | 组件清单与视觉稿，优先做产出去向告知页（IMP-7/8）与关键路径移动端（IMP-5） |

> **一处提醒**：前端**不要**等 UX/UI 全做完才动。§4.1 状态枚举、§4.3 路由表、§4.4 事件流是工程骨架，可以先于视觉稿落地；反过来，UX/UI 依赖前端先定页面边界。

---

## 6. 本次审查新增的未决项

> **追加规则**：编号按既有表**表尾追加**，不得插队；不得在表格行之间插入空行或说明块。

### 追加到 PRD §9.2 未决项表尾（U-16 之后依次）

| ID | 内容 | 阻塞 |
|---|---|---|
| U-32 | 生成式 AI 服务备案与算法备案的责任方、ICP 与网络出版资质前提、内容审核机制（谁审、误判谁纠） | **M2 立项前**（不可事后补） |
| U-33 | 定向分享的人数上限与可索引性（超过某规模即实质成为公共池，触发分发侧合规） | M2 |
| U-34 | 单作品日产出字数目标、单用户月成本上限、在制品并发上限（三个数互相关联，须同时定） | M2 |
| U-35 | 未固化窗口 N 的取值（初值 3 章）与是否对用户可见 | M1 |
| U-36 | 关键裁决的默认时限（初值 48 小时）与默认分支策略 | M1 |

### 追加到 Tech-Spec §13 技术待标定项表尾

| ID | 内容 | 阻塞 |
|---|---|---|
| T-25 | 事实记录（canon）与信息集的数据模型，及泄露比对的确定性签名 | **M0**（否则导演层第一道判定只能用模型兜底） |
| T-26 | 并行角色采样的展开 / 收敛契约与结果排序规则（保证同一 seed 两次跑出相同 canon 哈希） | M0 |
| T-27 | 复现检测（G-17）的比对库结构、版本与覆盖率口径，及检测结果的落库结构 | M1 |
| T-28 | `ModelRouter` 的调用上下文契约（`path_kind` + `cost_scope` + `work_id` 的传递机制） | **M0**（G-12 与"成本必挂作品"两条护栏都依赖它） |

### 追加到 Plan §9 待拍板

| 项 | 阻塞 |
|---|---|
| M2 是否含锚点创作（B-1） | **M0 立项前** |
| 阶段一是否接真实支付（B-4） | M2 定价前 |
| 前端框架与 PWA 工具链（B-12） | **建前端工程之前** |

---

## 7. 本次未做的事（诚实声明）

1. **没有设计任何视觉稿**——UI 角色只做到"界面由哪些区块构成、缺数据时显示什么"，配色与字体不在本轮范围。
2. **没有重算单位经济**——地域改到中国大陆后，广告单价与作者分成的取值需要重算，本文件只改了口径没改数值（依赖 U-17 实测）。
3. **没有验证法务前提**——U-32 三项国内合规是**推断出来的清单**，不是法律意见，须由法务确认。
4. **已回写的部分只限于「文档硬伤」，不是全量回写**——本轮已把 §1 审查中属于硬伤的部分回写进三份主文档（PRD v3.11→**v3.12**：地域口径八处漏改、NFR-3.8~3.10、FR 优先级调整；Tech-Spec v3.11→**v3.12**：`ExportGate` 拆分、T-25~T-28；Plan v4.7→**v4.8**：守卫 G-22~G-24、M0 出口二十三条、3 项立项级待拍板）。**§3 的 12 条冲突裁决与 §6 的追加未决项尚未回写**——它们依赖 §2 的拍板结果，拍板后须一并回写，届时三份文档版本号再升一版。**此处存在短期两处维护**：拍板前，硬伤类以主文档为准，裁决与契约类以本文件 §3 / §4 为准。
