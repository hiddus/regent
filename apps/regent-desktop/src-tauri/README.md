# apps/regent-desktop/src-tauri

Tauri 应用配置与 Rust 后端（tauri.conf.json、Cargo.toml、src/ 入口）。

## 目录内容

子目录：
- `icons/`
- `src/`

文件：
- `Cargo.toml`
- `build.rs`
- `tauri.conf.json`

> 本 README 由目录实际内容生成，反映当前结构；如用途有变请同步更新。
