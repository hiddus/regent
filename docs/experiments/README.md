# docs/experiments

实验任务集与实验产物存档。

- `p0-task-set-v1.json` — P0 冻结任务集。
- `p0-v1-artifacts/` — P0 v1 实验产物与 `VERIFICATION.md`。

> 区分：**Graduation 证据包**（签名报告 + DecisionRecord）在 [`../graduation-evidence/`](../graduation-evidence/)；**评测输入固定数据**在仓库根 [`fixtures/`](../../fixtures/)。三者不要混放。

## 目录内容

文件：
- `p0-task-set-v1.json`

> 本 README 由目录实际内容生成，反映当前结构；如用途有变请同步更新。
