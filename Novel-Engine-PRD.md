# Novel Engine 产品需求文档

> **版本**：v3.2 · **建立**：2026-08-31 · **状态**：DRAFT（U-13 已收敛，可进入 M0 启动）
> **文档定位**：Novel Engine 的**需求权威源**。需求以本文档为准，技术实现以 Tech-Spec 为准，执行顺序以 Plan 为准；冲突须通过 DecisionRecord 解决。
> **v3.0 重大变更**：产品定位由「领域无关引擎（小说站为首个部署）」修正为 **面向公众的 C 端创作网站**——引擎是内核，网站是产品本体。新增引导层（FR-0.x）、`StoryGoal` 与 `CriticalPath` 两个一等对象、专属性需求（FR-7.x）；C 端能力由 P1 升为 P0；里程碑顺序调整为 M2 C 端可用先于 M3 长篇能力（理由见 §6.0）。
> **v3.1 重大变更**：确立**分阶段策略**（§0.8）——阶段一引入**锚点创作**（`AnchorWork`，即从既有热门作品抽取结构作为创作起点）作为起量手段，转正期按三条量化条件退出，回归纯 AI 自由创作；交付形态确定为**阶段一 Web PWA、转正期原生 App**（§5.5）。新增 FR-0.10–0.15 与 FR-7.6（FR 总数 72 → 79）、NFR-3.7 / NFR-5.1–5.5、A2.9 / A2.10、AS-6、U-13 / U-14、停止投资条件 6–7。

---

## 0. 产品定义

### 0.1 一句话定位

一个**面向公众的 AI 长篇小说创作网站**——任何人只需说出想看什么样的故事，即可设定初始目标、调整关键路径，由系统推演出一部 30 万字以上、人设不漂移、信息不泄露、声纹不趋同的专属长篇。

**引擎层**为领域无关的场景推演内核（在既定剧本框架内推演，非自由生成）；**产品层**为面向海外中文用户的 C 端创作网站。

> **分阶段说明（v3.1）**：阶段一（M2–M3）额外提供**锚点创作**入口——用户可从既有热门作品中选一个作为起点，系统抽取其结构与节奏经强制变异后作为创作起点。这是**起量手段而非终局形态**，转正期按三条量化条件退出（详见 §0.8）。锚点不贡献内容，只贡献结构（R-0.8.1）。
>
> **终局形态（v3.2 · U-13 已收敛）：创作 + 阅读双环。** 转正期启用公共内容池：用户可**自愿公开**自己的作品（FR-7.7），其他用户即可阅读（FR-7.8）。消费侧的经济性完全建立在「**已生成内容的复用边际成本为零**」之上，因此阅读绝不触发重新生成（FR-7.10）。
> **不做纯消费产品**——其单位经济在算术上不成立（月成本 ≈¥180 vs 市场可收 ≈¥30），决策依据见 §9.1 U-13。

### 0.2 C 端产品契约（v3.0 核心）

**用户只做三个动作，其余全部由系统承担。**

| # | 用户动作 | 用户实际做的事 | 系统随后承担 |
|---|---|---|---|
| ① | **设定初始目标** | 用自然语言说想看什么故事（一句话即可） | 追问澄清 → 生成世界观、人物小传、规则 → 给候选供选择 |
| ② | **调整关键路径** | 在 10–20 个主线关键节点上改、删、插、锁 | 由关键路径展开为卷/章/场景卡的完整剧本 |
| ③ | **关键节点拍板** | 在死亡 / 背叛 / 揭露 / 开战等节点上二选一 | 推演 150 章、导演校验、成文、评审、入 canon |

> **R-0.2.1（产品硬约束）**：任何设计若要求用户执行第四类动作（填写结构化字段、审核章级剧本、维护关系图、逐章改文），即为**产品缺陷**，须在设计评审阶段驳回。

### 0.3 核心范式（不可分割）

**剧本定死，Agent 是演员。**

- 场景设定（`ScenarioSpec`：世界观 / 人物小传 / 规则 / 禁忌）与剧本（关键路径 / 卷 / 章 / 场景卡）先行锁定，**不是 Agent 涌现的产物**。
- 每个角色由一个独立 Agent 扮演。**Agent 只决定"怎么演"，不决定"演什么"。**
- 剧情走向由剧本决定，不由角色 Agent 自由推演。

> **根本洞察**：自由度与长程一致性存在根本矛盾。解法是先把框架定死、再让 Agent 在框内发挥——Regent 用 GoalSpec + Plan 解决同一问题，本引擎用 StoryGoal + ScenarioSpec + 剧本解决。**这是本引擎与 Regent 同构的原因，也是系统能扛住 150 章不散焦的唯一机制。**

产品必须同时拒绝两种退化：

| 退化形态 | 表现 | 为什么拒绝 |
|---|---|---|
| 退回全知写作 | 一个 Writer 写完所有角色 | 声纹必然趋同、信息必然泄露（结构性缺陷） |
| 滑向自由推演 | Agent 自主涌现剧情 | 150 章必然散焦 |

### 0.4 不可分割的恒定属性

以下属性不可为工期或成本妥协，妥协即产品不成立：

1. **零专业门槛**：用户的输入始终是自然语言或选择操作；结构化编辑仅在可选的专业模式中提供。
2. **剧本先行**：场景卡先于表演存在，且表演不得改变剧本设定的剧情结果。
3. **角色独立采样**：每个在场角色的表演由独立 Agent 独立采样产出，禁止单 prompt 批量生成多个角色的台词。
4. **信息集裁剪**：角色 Agent 的上下文只包含该角色应当知道的信息。
5. **导演层确定性**：越界判定（规则违反 / 偏离剧本 / 人设 OOC / 信息泄露）由确定性规则优先执行，不得交由 LLM 自行判断其输出是否合规。
6. **人在关键节点**：死亡、背叛、身份揭露、势力开战、主线方向变更须挂起等待人的裁决。

### 0.5 产品边界

| 是 | 不是 |
|---|---|
| 面向公众的 C 端创作网站 | 面向专业作者的设定编辑器 |
| 人指导方向、AI 完成其余 | 人逐章改稿的协同编辑器 |
| 用户在 10–20 个关键节点上调整走向 | 用户逐章审批 150 章大纲 |
| 用户可自建任意场景（引擎领域无关） | 仅内置固定品类（玄幻 / 都市 / 言情…） |
| 角色表演与长程一致性守护 | 单段落润色 / 续写工具 |
| 独立引擎（与 Regent 内核同源） | Regent 的一个 Goal 类型 |

> 引擎是**领域无关**的：小说、商战模拟、养成经营、跑团叙事均可由同一套核心承载，差异仅在"场景包 / 知识包"。Regent 原定义的"经济实体运营"在架构上等同于一个未来场景包。

### 0.6 文档体系

| 文档 | 用途 |
|---|---|
| `Novel-Engine-PRD.md`（本文） | 需求、用户、验收、指标、非目标 |
| `Novel-Engine-Tech-Spec.md` | 架构、数据模型、技术选型、与 Regent 的合并关系 |
| `Novel-Engine-Plan.md` | 唯一编码执行清单与开发切片 |

### 0.7 与 Regent / novelMaker 的关系

三者为"**同一内核、分层实例化**"：

| 项 | 定位 |
|---|---|
| **Regent** | 其多 Agent 编排、hub-and-spoke 纪律、ExecutionPermit、行为监测/修复环 **即是本引擎的治理内核**，非"借鉴"而是同一套原语。`Regent-PRD.md` 转为上游架构参考（同一内核在"经济实体运营"场景的实例化描述） |
| **novelMaker** | 本引擎的**参考基座（fork）**，贡献世界推演/演员模型的具体链路（详见 Tech-Spec §9） |
| **本三件套** | "场景推演引擎 + C 端创作网站"这一统一需求的权威源 |

### 0.8 分阶段策略：锚点创作的引入与退出（v3.1 新增）

**本产品的创作入口分两阶段演进。** 锚点创作是阶段一的起量手段，不是产品的终局形态。

| | 阶段一（起量验证期） | 阶段二（转正期） |
|---|---|---|
| 起点 | M2 | 退出条件满足后（见下） |
| 创作入口 | **双入口**：锚点创作（预期为主）+ 自由表达 | **单入口**：自由表达 |
| 交付形态 | **Web PWA** | 原生 App（Web 保留） |
| 核心目标 | 验证「任何人都能用」+ 积累用户与分路径行为数据 | 由独立 AI 创作能力承接全部需求 |

#### 0.8.1 锚点创作的定义

用户从既有热门作品中选一个作为**起点锚**，系统抽取其**结构与节奏**（事件类型序列、情绪弧、爽点密度、卷章分布），经强制变异后作为 `StoryGoal` 与 `CriticalPath` 的候选来源。

> **R-0.8.1（锚点边界硬约束）**：锚点只贡献**结构**，不贡献**内容**。系统不得将源作品的角色名、地名、世界观专名、设定专名或原文片段带入产出；不得模仿在世作者的可识别文风。抽取产物仅为结构参数向量。

> **术语**：本三件套统一称锚点作品为 `AnchorWork`，抽取器为 `structure_extractor`，强制变异关卡为 `variation_gate`。**对外用户侧不使用"仿写"话术**，统一表述为"以你喜欢的作品为起点"——这既是合规考量，也因该入口在产品上确实是起点锚而非仿写对象。

#### 0.8.2 为什么引入（它降低的是冷启动难度，不是引擎难度）

1. **冷启动障碍是认知负担，不是输入能力。** 让一个人凭空说出想要什么故事，多数人会愣住；给一个锚点后，任务由 recall（创造）变为 recognition（挑选）。这是数量级差异。
2. **抽象模板 ≠ 具体作品。** 抽象模板是多个实例的质心（低维平均值），从质心生成会向质心聚集；具体作品是高维实例，从实例出发做变异，输出天然分散。因此锚点路径**不恶化** §6.4 多样性，反而因起点分散而改善——**前提是锚点本身分散**；若用户集中于头部少数作品，则须在同锚点池内做定向变异（见 Tech-Spec §6.4.2）。
3. **剧本层质量由不可度量变为可度量。** 结构抽取有 ground truth（源作品本身），可用覆盖度与保真度做自动化回归测试；生成模式没有 ground truth，只能靠 Reader Council 间接打分、滞后且噪声大。

#### 0.8.3 必须显式承担的代价与风险

| 风险 | 说明 | 缓解 |
|---|---|---|
| 版权合规 | 具体情节序列可能构成实质相似；角色名/专名/在世作者文风为高风险 | R-0.8.1 + 变异门强制阈值 + 全程留痕（FR-0.14） |
| 期望基线抬升 | 用户拿产出与源作品比，而非与"AI 能写"比 | 获客变易、留存变难；须在 M2 监测完成率而非仅看启动率 |
| **路径依赖** | **锚点越成功，取消锚点的代价越高** | 退出机制在 M2 第一天即建设，见下 |

> **路径依赖悖论（必须正视）**：锚点阶段越成功，取消锚点的代价越高——用户心智、获客渠道、品牌认知都会固化在"写一本像《X》的小说"上。**因此退出机制必须与锚点功能同时建设，不得推迟到"用户量起来再说"。**

#### 0.8.4 退出机制（三项建设，缺一不可）

1. **分路径埋点与分路径分析（FR-0.15）**：A 路（锚点）与 B 路（自由表达）的转化、质量、留存指标必须分开统计、分开优化。
   - **禁止**用 A 路的高转化掩盖 B 路的问题；
   - **禁止**用 A 路数据优化面向 B 路的系统——用锚点用户的行为数据训练系统，只会优化出"更适合锚点"的系统，与退出目标背道而驰。
   - **数据架构类需求必须第一天就位**：数据一旦混合便无法回溯分离，故 FR-0.15 为 P0，而功能类的留痕（FR-0.14）可为 P1。
2. **主动迁移引导**：A 路用户完成首部作品后，第二次创作默认引导 B 路。不作被动等待——被动等待的结果是永远等不到。
3. **量化退出条件**（初值见 U-14，三条须**同时**满足方可移除锚点入口）：
   - B 路首作品启动率 ≥ A 路的 80%
   - B 路首章 Reader Council 评分 ≥ A 路
   - A 路用户第二次创作选择 B 路的比例 ≥ 40%

> **退出判据是能力达标，不是用户量达标。** 用户量达标但能力不足时取消锚点等于自杀；能力达标即使用户量未完全达标，也应开始收缩。
> 另需注意：外部压力可能先于内部条件到来——用户量越小越无人关注，量越大越是靶子。**取消时点未必由我们决定，须有被动预案。**

#### 0.8.5 若退出失败（备选路径）

若 M3 结束时三条退出条件均未满足，说明产品为**锚点依赖型**。此时最诚实的选项是承认其为**衍生创作平台**，转而寻求正版授权合作（与平台 / 作者分成），将灰区转为白区，而非强行摘除入口造成用户流失。

> **不做备选的后果**：强行摘除入口 = 同时失去获客渠道、用户心智与收入来源，且新产品定位尚未验证。这是最差的组合。

---

## 1. 产品使命与分阶段市场路径

### 1.1 两阶段路径

| | 阶段一（验证期，当前） | 阶段二（扩展期，条件触发） |
|---|---|---|
| **目标用户** | **海外中文用户（公众，非专业作者）** | 英语母语用户 |
| **输出语种** | **中文** | 英语 |
| **场景包** | **中文网文场景包（starter）+ 用户自建任意场景** | 西方类型文学 / 通用场景包 |
| **发布地** | 海外 | 海外 |
| **核心目标** | 验证「任何人都能用」+ 引擎能力 + 走通海外发布链路 | 打开本地化市场 |
| **触发条件** | — | 阶段一 M2 验收通过且留存达标 |

### 1.2 阶段一为何从海外中文用户切入

**战略价值：把「引擎能力验证」与「内容 / 场景本地化」解耦。**

1. **引擎零改造**——领域无关的通用核心无需为任何品类特化；中文网文场景包作为 starter 直接提供，用户也可自建任意场景。
2. **目标用户存在既有消费习惯**——已知存在面向海外中文读者的网文平台（如起点海外版 Webnovel、Wuxiaworld 等公开可查的既有平台）。**但其用户规模、付费能力与品类偏好均无内部数据支撑，须经 U-7 外部验证后方可作为商业决策依据。**
3. **海外链路提前跑通**——部署、合规、支付、内容分级等海外发布环节在阶段一即完整走通，阶段二只需替换场景包/语言层。

### 1.3 策略约定：不过早铺场景包

**阶段一不投入大量英文 / 西方场景包的建设工作。**

> **R-1.3.1（架构硬约束）**：场景包、套路知识库、文风模板、评审标准必须实现为**可插拔、按语种/品类隔离的独立模块**，其与引擎的耦合仅限明确定义的接口。

目的：**既不为阶段二提前投入，也不锁死阶段二。** 验收见 Tech-Spec §7.3。

---

## 2. 目标用户

### 2.1 阶段一 ICP

**海外中文互联网用户 —— 想看到"属于自己的那个故事"的普通人**，身处海外、习惯海外应用的使用与付费方式。

| 特征 | 说明 |
|---|---|
| 内容语言 | 中文 |
| 内容口味 | 熟悉中文网文的节奏、爽点、追读机制（网文为首发 starter 场景，非唯一场景） |
| **专业能力** | **不假设任何写作或设定能力**；不理解"世界观架构""人物弧光"等术语 |
| 产品习惯 | 习惯海外应用的交互、订阅与支付方式 |
| 所在地区 | 北美、东南亚、欧洲、大洋洲等（分布需 U-7 验证） |

> **v3.0 关键修正**：v2.x 的 ICP 表述为"想自建世界/场景的人"，隐含了设定能力假设。C 端定位下，ICP 必须覆盖**无任何创作经验的普通用户**。

### 2.2 用户画像

| 画像 | 描述 | 核心诉求 | 阶段一优先级 |
|---|---|---|---|
| **A. 有构思但写不动** | 有故事想法，受限于时间或写作能力 | "把我脑子里的想法变成一部完整长篇" | **首要** |
| **B. 追更读者转创作** | 长期阅读网文，想要"我想看的那种故事" | "写一部正好合我口味、能追下去的故事" | **首要** |
| **C. 世界/模拟爱好者** | 想搭建设定世界并看角色自主演化 | "我的世界能自己跑、不崩" | 次要 |
| **D. 内容创业者** | 希望持续产出内容获取收益 | "稳定、量大、不崩的内容供给" | 阶段二 |

**画像 B 是 C 端最大的池子，也是门槛最低的入口**——他们不想"创作"，只想"点单"。产品的引导层必须优先服务这类用户。

### 2.3 核心使用场景

| 场景 | 描述 | 人的介入程度 |
|---|---|---|
| **S1 一句话点单** | 用户说一句想看的故事 → 系统补全设定与关键路径 → 用户确认 → 自动推演至完成 | 一句话 + 若干选择 + 关键节点 |
| **S2 关键路径调整** | 用户在关键节点序列上改写/插入/删除节点 → 系统重生成受影响章节 | 节点级操作 |
| **S3 中途改向** | 已推演若干章，用户注入自然语言方向（如"让女主黑化"）→ 系统修订后续路径 | 自然语言指令 |
| **S4 长周期托管** | 设定后后台持续推演，用户定期查看进度、被动接收待裁决通知 | 周期性查看 + 异常裁决 |
| **S5 专业模式自建** | 进阶用户直接编辑结构化设定与规则 | 场景定义 + 规则设定（可选路径） |

> **S5 是可选路径，不得成为 S1 的前置。** 若普通用户必须经过 S5 才能起书，即违反 R-0.2.1。

### 2.4 阶段二用户（预留，本阶段不投入）

英语母语的类型小说读者与通用场景用户。目标平台与画像待阶段一验证通过后另行定义。

---

## 3. 用户旅程

### 3.1 成功路径（S1 一句话点单）

| 步骤 | 用户动作 | 系统动作 | 用户所见 |
|---|---|---|---|
| 1 | 一句话描述想看的故事 | — | 输入框 + 若干示例灵感 |
| 2 | 回答 ≤3 轮澄清问题（每轮 ≤3 问，均为选择或短句） | 补全缺失要素 | 选项卡片 |
| 3 | 确认目标摘要 | 固化为 `StoryGoal` | **自然语言摘要**（非字段表） |
| 4 | 从 2–3 个候选中选一个 | 生成 `ScenarioSpec` 候选（世界观 / 主要人物 / 规则） | 候选卡片（含人物一句话简介） |
| 5 | 可选：用自然语言微调（"女主再狠一点"） | 翻译为结构化变更并重生成 | 变更前后对比 |
| 6 | 在关键路径上改 / 删 / 插 / 锁节点 | 生成 `CriticalPath`（10–20 节点） | **关键路径时间轴** |
| 7 | 确认预估耗时与成本，启动 | 由关键路径展开卷/章/场景卡剧本 | 预估卡片 + 启动按钮 |
| 8 | — | 后台推演：场景卡 → 角色独立表演 → 导演校验 → 叙述者成文 → 评审 → 入 canon | 实时进度 + 可读已完成章节 |
| 9 | 裁决关键节点（收到通知后） | 挂起等待 → 按裁决继续 | 裁决卡片（选项 + 后果说明） |
| 10 | 导出 | 每卷结束压缩摘要 | 完整长篇 / 分卷导出 |

> **步骤 6 是"调整关键路径"的唯一界面。章级剧本与场景卡默认不呈现给用户，仅在专业模式下可查看。**

### 3.2 人的介入点

| 介入点 | 对应动作 | 时机 | 产品形态 |
|---|---|---|---|
| **P1 说出目标** | ① | 起点 | 自然语言输入框 + 示例灵感 |
| **P2 澄清应答** | ① | 追问阶段 | 选项卡片（≤3 轮 × ≤3 问） |
| **P3 选设定** | ① | ScenarioSpec 候选产出后 | 候选卡片选择 + 自然语言微调 |
| **P4 调关键路径** | ② | 关键路径产出后 / 推演中任意时刻 | **关键路径时间轴**（改 / 删 / 插 / 锁） |
| **P5 注入方向** | ② | 推演中任意时刻 | 自然语言指令，作用于后续推演 |
| **P6 关键裁决** | ③ | 触发关键节点 | 待裁决卡片（选项 + 影响范围预览） |
| **P7 异常处置** | ③ | 质量不达标 / 治理阻断 / 成本超限 | 恢复建议卡片（默认选项须可一键采纳） |

**人明确不做的事（产品不得引导用户去做）：**

- 填写结构化设定字段（世界观条目、规则表达式、声纹参数）
- 审核章级剧本与场景卡
- 逐章改文字、改台词、润色正文
- 手工维护人物关系图、时间线

> 若阶段一验证中普通用户高度依赖上述任一项才能完成作品，则视为**产品失败**——说明"任何人都能用"与"人只指导方向"的承诺均未达成。

### 3.3 失败与例外路径

| 情形 | 系统行为 | 用户可见 |
|---|---|---|
| 用户输入过于模糊，追问 3 轮后仍不足 | 以场景包默认值补齐并明确告知假设 | "我按…理解，可随时改" |
| 表演越界（OOC / 规则违反 / 信息泄露） | 自动打回重演；超上限则挂起 | 仅在超限时提示，重演过程不打扰 |
| 单章评审不通过 | 自动重写（上限内）；超限挂起 | 待处置项 |
| 关键节点触发 | 挂起等待裁决，并主动通知用户 | 裁决卡片 + 站外通知 |
| 长任务中断（模型超时 / 配额耗尽 / 重启） | 从最近完成章节恢复，不丢内容 | 断点续跑状态 |
| 成本超预算 | 暂停并告警 | 成本面板 + 处置选项 |
| 用户中途改向 | 从当前章重规划；已写章节标记"待复核"而非丢弃 | 影响范围预览 |

---

## 4. 功能需求

优先级：**P0 = 不做则产品不成立 · P1 = 不做则体验残缺 · P2 = 增强项**

### 4.0 引导层（v3.0 新增，「任何人」的实现层）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-0.1 | 用户应能以纯自然语言（一句话即可）表达初始目标；系统不得要求用户预先填写任何结构化字段 | P0 |
| FR-0.2 | 系统应以澄清式追问补全缺失要素，**追问 ≤3 轮、每轮 ≤3 问**，且问题应以选项形式呈现、允许跳过 | P0 |
| FR-0.3 | 系统应将用户意图固化为 `StoryGoal` 一等对象，并以**自然语言摘要**（非字段表）向用户回述确认 | P0 |
| FR-0.4 | 系统应生成 **2–3 个 `ScenarioSpec` 候选**，用户通过"选择"而非"填空"完成设定 | P0 |
| FR-0.5 | 用户的所有修改动作应支持自然语言表达，由系统翻译为结构化变更并展示变更前后差异 | P0 |
| FR-0.6 | 结构化字段（世界观条目、规则、声纹参数）**默认折叠**，仅在「专业模式」下展开编辑 | P0 |
| FR-0.7 | 启动推演前应展示**预估耗时与预估成本**，并需用户显式确认 | P0 |
| FR-0.8 | 应提供零输入即可体验的示例作品（预置 `StoryGoal` + 关键路径），供冷启动试用 | P1 |
| FR-0.9 | 用户放弃或中断设定流程时应保存草稿，可从断点继续 | P1 |
| FR-0.10 | 用户应能从既有作品列表中选择一个**锚点作品**作为创作起点；该入口与"自由表达"入口并列，均为纯选择或自然语言操作，**不引入结构化填写** | P0 |
| FR-0.11 | 系统应能从锚点作品抽取**结构参数**（事件类型序列、情绪弧、爽点密度、卷章节奏分布），并以自然语言向用户回述，供其确认与调整 | P0 |
| FR-0.12 | **锚点只贡献结构、不贡献内容**（R-0.8.1）：抽取产物不得包含源作品的角色名、地名、世界观专名、设定专名或原文片段，不得模仿在世作者的可识别文风 | P0 |
| FR-0.13 | 抽取结果须经**强制变异**并过相似度阈值后，方可展开为剧本；未通过须继续变异或回落至澄清追问 | P0 |
| FR-0.14 | 系统应记录每次锚点创作的锚点来源、变异操作与相似度判定结果，作为合规留痕 | P1 |
| FR-0.15 | A 路（锚点）与 B 路（自由表达）的漏斗与质量指标应**分路径统计、可分别查询**（退出机制的数据基础，见 §0.8.4） | P0 |

> **FR-0.10~0.15 为阶段一需求**，转正期移除锚点入口后自然失效（FR-0.15 的分路径统计能力转为长期保留，用于持续监测）。
> **优先级说明**：FR-0.15 定为 P0 而 FR-0.14 定为 P1，理由是**数据架构类需求无法后置**——路径数据一旦混合便无法回溯分离；而留痕属功能类，可在锚点功能上线后补齐。

### 4.1 目标与关键路径层

| ID | 需求 | 优先级 |
|---|---|---|
| FR-1.0 | 系统应将用户初始目标固化为 **`StoryGoal`** 一等对象（创意、主角意向、期望结局、核心情感、目标篇幅、禁忌清单），作为设定与剧本生成的唯一上游 | P0 |
| FR-1.1 | 系统应能由 `StoryGoal` 生成 `ScenarioSpec`：世界观、人物小传、规则、禁忌 | P0 |
| FR-1.2 | 人物小传应包含**声纹特征**字段（用词、句式、口癖），作为独立可编辑项 | P0 |
| FR-1.3 | `ScenarioSpec` 应支持版本记录，确认后锁定；编辑入口在专业模式，普通模式经自然语言修改 | P0 |
| FR-1.4 | 系统应能生成 **`CriticalPath`**（10–20 个主线关键节点的有序序列），作为用户调整剧情走向的**唯一界面** | P0 |
| FR-1.5 | `CriticalPath` 每节点应含：预计位置（第 N 章附近）、事件类型、一句话描述、影响范围、锁定状态 | P0 |
| FR-1.6 | 用户应能对 `CriticalPath` 执行：改写 / 删除 / 插入 / 调序 / 锁定节点 | P0 |
| FR-1.7 | 调整 `CriticalPath` 后系统应自动重生成受影响的章级剧本，并展示受影响章节范围 | P0 |
| FR-1.8 | 系统应能由 `CriticalPath` 展开卷 → 章 → 场景卡的层级剧本 | P0 |
| FR-1.9 | 场景卡应至少包含：地点、时间、在场者、剧情目标、必达节点、情绪弧、POV 角色、伏笔指令 | P0 |
| FR-1.10 | **章级剧本与场景卡不得作为推演前置审核项**；用户可在专业模式查看，但推演不以其审核为条件 | P0 |
| FR-1.11 | **引擎应领域无关**：用户可自建任意场景，核心逻辑不因场景类型改变（规则由场景包/用户定义，引擎只消费通用规则类型） | P0 |
| FR-1.12 | 应提供**场景包 / 知识包**机制，允许以配置方式新增场景类型，引擎代码不变 | P0 |
| FR-1.13 | 专业模式应支持章节级剧本操作：通过 / 修改 / 重生成 / 删除 | P1 |
| FR-1.14 | 剧本层应支持独立的预期质量评审（成文前的剧本打分） | P1 |

### 4.2 表演层（L2）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-2.1 | 每个在场角色应由**独立 Agent 独立采样**产出表演，禁止单 prompt 批量生成多角色台词 | P0 |
| FR-2.2 | 角色 Agent 的上下文应只包含该角色的信息集（亲身经历 / 被告知 / 当场观察） | P0 |
| FR-2.3 | 表演输出应结构化：台词（含语气）、动作与神态、内心活动、即时反应 | P0 |
| FR-2.4 | 表演不得宣称剧本外的既定结果（角色不能自称成功） | P0 |
| FR-2.5 | 角色 Agent 的 prompt 中，小传部分应作为稳定前缀以利用 prompt cache | P0 |
| FR-2.6 | 应支持为每个角色配置差异化采样参数，以降低声纹趋同风险 | P1 |
| FR-2.7 | 应记录每次表演的 token 消耗与 cache 命中诊断 | P1 |

### 4.3 导演层（L3）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-3.1 | 应校验表演是否违反**场景定义的规则**（引擎内置通用规则类型：数值 / 资源 / 地理 / 禁忌 / 自定义断言） | P0 |
| FR-3.2 | 应校验表演是否偏离剧本（未达必达节点、改变剧情目标） | P0 |
| FR-3.3 | 应校验人设 OOC（触碰禁忌、声纹偏离） | P0 |
| FR-3.4 | 应通过**信息集比对**确定性检测信息泄露 | P0 |
| FR-3.5 | 越界应打回重演并记录原因；重演过程不得干扰用户 | P0 |
| FR-3.6 | 重演次数应设上限，超限转为挂起等待处置（禁止无限重试） | P0 |
| FR-3.7 | 关键节点（死亡 / 背叛 / 揭露 / 开战 / 主线变更）应挂起等待裁决，并**提供选项与后果说明** | P0 |
| FR-3.8 | 裁决界面应展示受影响章节范围预览 | P0 |

### 4.4 成文与评审（L4）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-4.1 | 叙述者应把各角色表演按 POV 视角编织成正文，**不得新增剧本外剧情** | P0 |
| FR-4.2 | 成文后应执行一致性审查（设定 / 人物 / 时间线 / 规则体系四类） | P0 |
| FR-4.3 | 应执行多视角读者评议（主线推进 / 节奏密度 / 情绪代入），输出追读分 | P0 |
| FR-4.4 | 应维护伏笔台账：创建章、状态、最近触达章、预期回收线 | P0 |
| FR-4.5 | 章节评审应由系统自动执行；**人工审核为可选项，不得作为入 canon 的前置** | P0 |
| FR-4.6 | 应生成章节摘要，供后续章节检索与上下文装配 | P0 |
| FR-4.7 | 应输出**声纹区分度**指标（本章各角色语言特征的可区分程度） | P0 |
| FR-4.8 | 应支持自动重写不达标章节，并形成 revision 链（旧稿评审不得污染 canon） | P1 |

> **FR-4.5 相较 v2.0 的变更**：v2.0 为"章节应支持人工审核通过 / 驳回，通过后方入 canon"。C 端下 150 章逐章人工审核不可行，且与 R-0.2.1 冲突，故改为自动评审 + 可选人工。

### 4.5 长篇工程（30 万字目标）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-5.1 | 每章应是原子推演单元，状态持久化后方可进入下一章 | P0 |
| FR-5.2 | 应支持从任意已完成章节恢复推演 | P0 |
| FR-5.3 | 应实现卷级摘要压缩：已完结卷保留极简摘要，当前卷保留章级细节 | P0 |
| FR-5.4 | 上下文预算应与总篇幅解耦（150 章时单章上下文不显著膨胀） | P0 |
| FR-5.5 | 应支持快照、版本记录与按章节回滚 | P0 |
| FR-5.6 | 回滚后应能重规划受影响章节并重跑 | P1 |
| FR-5.7 | 状态图谱应支持巡检、断链诊断与修复建议 | P1 |
| FR-5.8 | 应支持项目级批量恢复（多条断链时给出统一恢复窗口） | P2 |

### 4.6 C 端产品能力（v3.0：由「外壳」升级为本体）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-6.1 | 用户注册、登录、作品列表与权限隔离 | P0 |
| FR-6.2 | 推演任务应后台异步执行，前端实时查看进度（当前章 / 已完成字数 / 预计剩余） | P0 |
| FR-6.3 | 应实现配额与成本护栏：总预算、单章预算、超限自动暂停 | P0 |
| FR-6.4 | 应支持导出 Markdown / TXT，长篇支持分卷导出 | P0 |
| FR-6.5 | 应支持推演任务暂停 / 恢复 / 重试 | P0 |
| FR-6.6 | 关键节点挂起、推演完成、成本告警应通过站外渠道（邮件等）通知用户 | P0 |
| FR-6.7 | 应提供在线阅读器，可读已完成章节（推演进行中即可阅读） | P0 |
| FR-6.8 | 应提供成本与质量面板（每章 token / 成本 / 质量分 / 伏笔回收率） | P1 |
| FR-6.9 | 移动端应可阅读、查看进度、执行关键裁决 | P1 |
| FR-6.10 | 内容分级与敏感内容过滤 | P1 |
| FR-6.11 | 计费与订阅（定价模型待 U-5 确认） | P1 |
| FR-6.12 | 导出 EPUB | P2 |

> **FR-6.6 为长任务 C 端的必需项**：150 章推演历时数小时至数天，用户不可能在线守候；关键节点挂起而用户不知情，等同于任务永久卡死。

### 4.7 专属性与所有权（v3.0 新增，「专属于客户自己」的实现层）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-7.1 | 相同场景包下不同用户的作品应显著不同：设定生成须引入用户目标差异与随机种子；跨作品相似度不得超过阈值（定义见 Tech-Spec） | P0 |
| FR-7.2 | 作品默认私有，未经用户显式操作不得公开 | P0 |
| FR-7.3 | 生成内容的权利归属应在用户协议中明示归用户所有 | P0 |
| FR-7.4 | 用户应能完整导出作品全文与设定，不得以格式或功能限制形成锁定 | P0 |
| FR-7.5 | 用户内容默认不用于模型训练；如需使用须显式取得同意 | P0 |
| FR-7.6 | 锚点创作（阶段一）的跨作品相似度判定池应为**同源锚点池**而非全局池，且阈值较 FR-7.1 加严（定义见 Tech-Spec §6.4.5） | P0 |
| FR-7.7 | 作品公开须为用户**逐部显式授权**，不得默认公开或批量授权；用户可随时撤回，撤回后停止新增分发 | P0 |
| FR-7.8 | 已授权公开的作品应进入**公共内容池**，其他用户可阅读、收藏、追更 | P0 |
| FR-7.9 | 公共内容池应有**质量筛选门槛**，未达门槛的作品不进入推荐位 | P1 |
| FR-7.10 | **阅读已生成内容不得触发重新生成**；阅读侧成本须维持存储与分发成本量级，而非生成成本量级 | P0 |

> **FR-7.7–7.10 的必要性（v3.2 · U-13 收敛结果）**：终局定位为**创作 + 阅读双环**（决策依据见 §9.1 U-13）。关键洞察是**已生成内容的复用边际成本为零**——生成成本已由创作者承担，因此公共池能同时解开消费侧的两个死结：供给算术（1 个重度读者月耗约 3 部，纯 UGC 供给缺口 3–5 倍）与成本算术（专属生成模式月成本 ≈¥180，而市场可收 ≈¥30）。
>
> **FR-7.10 尤其不可退让**：一旦阅读路径触发重新生成（哪怕只是"按我的口味重出一版"），成本立刻回到消费产品的亏损结构。消费侧的经济性**完全建立**在"只读已生成内容"之上，由 Tech-Spec 守卫 **G-12** 落地为代码断言。
>
> **边界**：分发是**单向**的——用户可读他人作品，但**不做评论、社交与 UGC 互动**（§8 非目标），避免社区运营投入与内容治理负担。

> **FR-7.6 的必要性**：锚点阶段用户主动要求"像某一本作品"，全局池判定会系统性放宽（因为不同锚点间的差异掩盖了同锚点内的雷同）。同锚点池是唯一能查出"同一个锚点下每个人都写得很像"的判定维度——而这恰恰是用户最先感知到的"不专属"。

### 4.8 优先级汇总

| 优先级 | 需求数 | 对应里程碑 |
|---|---|---|
| P0 | 66 | M0–M3 |
| P1 | 15 | M0–M4 |
| P2 | 2 | M3 / M4 |

**合计 83 条 FR。** 分节分布：

| 节 | 模块 | 条数 |
|---|---|---|
| 4.0 | 引导层 | 15 |
| 4.1 | 目标与关键路径层 | 15 |
| 4.2 | 表演层 | 7 |
| 4.3 | 导演层 | 8 |
| 4.4 | 成文与评审 | 8 |
| 4.5 | 长篇工程 | 8 |
| 4.6 | C 端产品能力 | 12 |
| 4.7 | 专属性与所有权 | 10 |

> v2.0 为 32/15/2 = 49 条；v3.0 为 57/13/2 = 72 条；v3.1 为 63/14/2 = 79 条；**v3.2 为 66/15/2 = 83 条**，净增 4 条：创作 + 阅读双环 4 条（FR-7.7–7.10）。
> 其中 FR-0.10–0.15 与 FR-7.6 为**阶段一专属需求**，转正期移除锚点入口后自然失效；**FR-7.7–7.10 为转正期生效需求**，阶段一不投入（消费侧在阶段一无内容来源）。
>
> **计数纪律**：本表数字一律由 `grep "^| FR-" Novel-Engine-PRD.md | grep -o "| P[0-2] |" | sort | uniq -c` 重数得出，不得凭印象累加。v1.1 曾误写 51（实际 47）、v3.0 初稿误写 74（实际 72）。

---

## 5. 非功能需求

### 5.1 成本（开源模型为主）

模型以开源系列（DeepSeek 等）为主，可经 API 调用或自托管。

| ID | 需求 | 目标值 |
|---|---|---|
| NFR-1.1 | 单部 30 万字长篇的推理成本（DeepSeek 档，开启 prompt cache 后） | ≤ ¥100（粗算约 ¥40–85/部，M1 后以实测修订） |
| NFR-1.2 | prompt cache 命中率（按角色维度的稳定前缀） | ≥ 60% |
| NFR-1.3 | 成本超预算时应自动暂停并告警，不得静默继续 | 强制 |
| NFR-1.4 | 单章成本应对用户可见 | 强制 |
| NFR-1.5 | 应支持开源模型自托管部署路径（数据不出域），作为 API 调用的可替换后端 | P1 |
| NFR-1.6 | **免费额度**应足以让新用户完成一次完整的"设定目标 → 关键路径 → 试跑数章"体验 | P0 |

> **成本影响判断**：相较海外闭源模型档（原估 ¥230–280/部），开源模型将单部成本压降约 3–6 倍。NFR-1.6 是 C 端转化的前置条件——若用户必须先付费才能看到第一章，首作品启动率无法达标。

### 5.2 长任务与性能

| ID | 需求 | 目标值 |
|---|---|---|
| NFR-2.1 | 150 章连续推演过程中，单章上下文预算增长 | 不显著增长（与篇幅解耦） |
| NFR-2.2 | 中断后恢复不应丢失已完成章节 | 强制 |
| NFR-2.3 | 单章推演耗时（不含人工等待） | 待 M1 实测后定义 |
| NFR-2.4 | 进度应对用户实时可见 | 强制 |
| NFR-2.5 | **从用户提交目标到看到第一章正文的时长** | ≤ 15 分钟（C 端首体验硬指标，M1 后校准） |

> **NFR-2.5 是 C 端特有的约束**：用户不会为看到第一章等待数小时。若首章时延过长，须以"先出设定与关键路径预览"填补等待体验。

### 5.3 部署与合规（海外）

| ID | 需求 |
|---|---|
| NFR-3.1 | 系统应部署于海外基础设施，不依赖境内部署 |
| NFR-3.2 | 用户数据存储位置应明确声明，并符合目标市场隐私法规（视用户分布适用 GDPR / CCPA 等） |
| NFR-3.3 | 支付应支持海外主流支付方式（如 Stripe / 信用卡 / Apple Pay） |
| NFR-3.4 | 应提供符合海外惯例的用户协议、隐私政策与服务条款 |
| NFR-3.5 | 生成内容的权利归属与使用条款应在用户协议中明示（对应 FR-7.3） |
| NFR-3.6 | 开源模型自托管时，推理数据不离开自有基础设施，作为合规与数据主权的可选增强 |
| NFR-3.7 | 锚点创作（阶段一）的抽取与变异过程应留痕可审计：记录锚点来源、抽取产物、变异操作、相似度判定结果 | 对应 FR-0.14 |

### 5.4 内容分级与安全

| ID | 需求 |
|---|---|
| NFR-4.1 | 应对生成内容执行分级与敏感内容过滤，标准适配海外发布平台要求 |
| NFR-4.2 | 过滤规则应与场景包 / 知识库同样可插拔 |
| NFR-4.3 | 用户应可设置内容强度偏好（如全年龄 / 成人） |

### 5.5 交付形态（v3.1 新增）

**阶段一以 Web PWA 交付，转正期交付原生 App。** 两者基于同一前端代码库。

| ID | 需求 |
|---|---|
| NFR-5.1 | 阶段一以 **Web** 为唯一交付形态，不发布原生 App（理由见下） |
| NFR-5.2 | Web 端移动端体验应按原生标准实现：响应式布局、可添加至主屏幕、已生成章节离线缓存、全屏阅读模式 |
| NFR-5.3 | 前端业务层与 UI 层须严格分离，UI 层可整体替换（为转正期套原生壳预留，目标为同一代码库经 Capacitor 类方案打包） |
| NFR-5.4 | 通知机制须抽象为可切换接口（邮件 / Web Push / 原生推送），不得在业务代码中写死渠道 |
| NFR-5.5 | 转正期应发布 iOS 与 Android 原生 App，Web 端保留 |

**为什么阶段一不做原生 App：**

1. **上架审核与锚点功能存在结构性冲突（决定性）**。锚点创作属 UGC 版权风险功能，App Store / Google Play 在**上架审核环节**即可能驳回或要求整改——这不是"量大了才被查"的远期风险，而是第一个版本就会遇到的当下风险。Web 无此环节。
2. **迭代速度是测试阶段的生命线**。M2 含 14+ 切片并需多轮外部用户测试；App 每次改动须发版 + 审核（1–7 天）且存在版本碎片化，Web 可随时部署，用户永远使用最新版。
3. **下载摩擦直接推高样本成本**。验证 AS-4 / AS-5 需要大量样本，而"下载 App 试一下"在点击 → 安装 → 注册链路上流失极高；Web 点链接即用。
4. **海外中文用户的传播链在微信内**。海外华人重度使用微信，Web 链接可在微信内直接打开与转发；App 下载链接在微信内的转化极差。
5. **支付数据不被渠道扭曲**。App 虚拟商品强制走 IAP 且抽成 30%（小企业计划 15%），会严重扭曲阶段一对付费意愿（AS-1）的验证；Web 可直连 Stripe 自由试价。

> **两个时点宜重合**：取消锚点入口（转正）与发布原生 App 应是**同一个产品事件**。这样转型有明确的事件承载（"发布全新 App 版本"），比在 Web 上静默下线功能的用户接受度高得多，也顺带让上架审核的版权问题自然消解。

---

## 6. 验收标准

### 6.0 里程碑顺序说明（v3.0 调整）

**v2.0 顺序**：M0 内核 → M1 一致性 → M2 长篇能力 → M3 C 端外壳。
**v3.0 顺序**：M0 内核 → M1 一致性 → **M2 C 端可用** → **M3 长篇能力** → M4 商业化。

调整理由：

1. **「任何人都能用」的风险高于「150 章不膨胀」的风险。** 前者一旦证伪产品即不成立，后者是可解的工程量问题。高风险假设应先验证。
2. **A1.4（介入频次阈值）无法在内部脚本环境中校准**，必须由真实用户在真实界面上跑出来。
3. **长篇能力投入最大**，应在确认用户愿意使用之后再投。

> 前置里程碑未通过不得进入下一里程碑。**M1 仍是技术成败闸口，M2 是产品成败闸口。**

#### 6.0.1 阶段划分与交付形态（v3.1 新增）

| 里程碑 | 所处阶段 | 创作入口 | 消费入口 | 交付形态 |
|---|---|---|---|---|
| M0 / M1 | 引擎建设期 | 无 C 端入口 | 无 | 无（脚本 / 内部界面） |
| **M2 / M3** | **阶段一：起量验证期** | **双入口**（锚点 + 自由表达） | 无（作品默认私有） | **Web PWA** |
| **M-T 及之后** | **阶段二：转正期**（退出条件满足后） | **单入口**（自由表达） | **公共内容池**（用户显式公开的作品） | **原生 App**（Web 保留） |

> 阶段一到阶段二的切换由 §0.8.4 的三条量化退出条件触发，**不由用户量单独触发**。
>
> **三个时点必须重合为同一个产品事件（v3.2 · NFR-5.5 / U-13）**：① 锚点入口移除；② 公共内容池启用；③ 原生 App 发布。
> 重合理由：转型需要有事件承载——"发新版 App，现在也能读别人的作品了"比在 Web 上静默下线锚点入口更容易被接受；且锚点引发的上架审核版权问题在锚点移除后自然消解。这同时缓解 §0.8 的路径依赖悖论。

### M0 表演内核

| # | 验收项 |
|---|---|
| A0.1 | 一章由演员模型产出：场景卡 → 多角色独立表演 → 导演校验 → 叙述者成文 |
| A0.2 | 同一场戏中不同角色的声纹可区分（人工盲测可辨别） |
| A0.3 | 无信息泄露（角色未说出其信息集外的内容） |
| A0.4 | 剧本设定的必达节点被达成 |
| A0.5 | `StoryGoal` → `ScenarioSpec` → `CriticalPath` → 场景卡的生成链路可跑通（界面可为最简形态） |

### M1 一致性与质量（**技术成败闸口**）

| # | 验收项 |
|---|---|
| A1.1 | 20 章连续推演后，人设、伏笔、时间线无硬冲突 |
| A1.2 | 声纹区分度达标（指标定义见 Tech-Spec，阈值在 M0 实测后标定，不得事后放宽） |
| A1.3 | Reader Council 评分不低于单 prompt 写作对照组 |
| A1.4 | 人的介入频次低于阈值（阈值在 M0 后标定） |

> **A1.1–A1.4 须同时达标。仅通过一致性不算通过。** 可用任意场景包验证，不锁定品类。

### M2 C 端可用（**产品成败闸口**）

| # | 验收项 |
|---|---|
| A2.1 | **未经培训的外部用户可独立完成**：说出目标 → 调整关键路径 → 启动推演 → 查看进度 → 导出，全程无人工协助 |
| A2.2 | 全程用户输入均为自然语言或选择操作，**未出现要求填写结构化字段的强制步骤** |
| A2.3 | 首作品启动率达标（阈值在 M1 后标定） |
| A2.4 | 首章可见时延符合 NFR-2.5 |
| A2.5 | 配额与成本护栏生效，超限自动暂停；免费额度足以完成首次体验（NFR-1.6） |
| A2.6 | 关键节点挂起时用户能通过站外通知获知并完成裁决 |
| A2.7 | 海外部署与支付链路跑通（支付可为测试模式） |
| A2.8 | 同一场景包下不同用户产出的作品显著不同（FR-7.1） |
| A2.9 | **A 路（锚点）与 B 路（自由表达）的指标可分路径查询**（FR-0.15），且外部用户测试中 B 路样本量足以支撑独立结论 |
| A2.10 | 锚点创作的产出经抽取与变异后，与源作品的结构相似度低于阈值（FR-0.13 / FR-7.6） |

> **A2.1 与 A2.2 是「任何人都能用」的直接度量，不得由内部人员代测。**
>
> **A2.9 是不可妥协项（v3.1 新增）**：外部用户测试必须**同时覆盖 A 路与 B 路并分组统计**。若只测锚点路径，AS-4 就不是被证伪而是**被绕过**——等产品上线后用户走自由表达路径时，风险原封不动还在。B 路样本须达到可独立得出结论的规模（具体量在 M1 后标定）。
>
> **期望基线提醒（v3.1）**：锚点路径的用户会拿产出与源作品比较，而非与"AI 能写小说"比较。故 A2.3 的启动率在 A 路易达标、在 B 路难达标，两路阈值应分别标定；完成率（北极星之二）在 A 路的压力大于 B 路。

### M3 长篇能力

| # | 验收项 |
|---|---|
| A3.1 | 150 章连续推演，单章上下文预算不显著膨胀 |
| A3.2 | 从任意章节中断后均可恢复，不丢已完成内容 |
| A3.3 | 单部成本符合 NFR-1.1 |
| A3.4 | 完成率（北极星）可被稳定测量并达到阈值 |

### M-T 转正里程碑（条件触发 · v3.2 补齐）

> **v3.1 遗漏说明**：M-T 里程碑此前仅在 Plan §5.5 定义，PRD §6 缺对应验收标准，形成"Plan 引用 PRD §6 但 PRD 无此节"的悬空引用。本节补齐，AT 编号以本节为准。

| # | 验收项 |
|---|---|
| AT.1 | PRD §0.8.4 的三条量化退出条件**均满足**（须同时满足，不由用户量单独触发） |
| AT.2 | 移除锚点后，B 路启动率不低于移除前 A 路的 80%（防用户大规模流失） |
| AT.3 | 原生 App 上架并通过审核 |
| AT.4 | 转正后外部用户测试通过（**仅 B 路**） |
| **AT.5** | **公共池阅读链路的模型调用计数为 0**（G-12）；公开授权为逐部显式操作，无默认勾选态与批量入口（G-13 / H7） |
| **AT.6** | **他人作品阅读占比达标**（双环真伪判据，§7.1.1）——若用户主要读自己写的作品，本质仍是"创作工具 + 阅读器"，双环未成立 |

> **AT.5 为何是验收项而非实现细节**：消费侧的经济性完全建立在"只读已生成内容"之上（Tech-Spec §6.5）。阅读链路一旦出现生成调用，成本即从 ≈¥0 回到 ≈¥180/月/人，整个双环不成立。这是存在性前提，必须在验收时显式检查。
> **AT.6 为何单独设立**：总阅读时长会被"用户反复读自己的作品"污染，无法区分双环真伪。达标阈值须在 M-T 前标定（**U-16**）。

### M4 商业化

| # | 验收项 |
|---|---|
| A4.1 | 定价模型上线，单部毛利为正 |

---

## 7. 北极星指标与护栏

### 7.1 指标体系（v3.0：双指标）

| 类型 | 指标 | 定义 | 理由 |
|---|---|---|---|
| **门槛指标** | **首作品启动率** | 注册用户中完成"说出目标 → 启动推演"的比例 | 「任何人都能用」的直接度量。若用户在设定阶段即流失，完成率再高也无意义 |
| **价值指标** | **完成率** | 启动推演的作品中，完成 ≥ 30 万字且通过 M1 等效质量标准的比例 | 产品价值主张（扛完一部长篇而不崩）的直接度量 |

> 两者必须同时被追踪。**只看完成率会掩盖门槛过高的问题；只看启动率会掩盖质量崩坏。**

#### 7.1.1 转正期消费侧指标（v3.2 · U-13 双环）

转正期启用公共内容池后追加追踪：

| 类型 | 指标 | 定义 | 理由 |
|---|---|---|---|
| **消费侧核心** | **他人作品阅读占比** | 用户阅读时长中来自**他人公开作品**的比例 | **双环是否真正成立的唯一判据**。若用户主要读自己写的，本质仍是"创作工具 + 阅读器" |
| 消费侧规模 | 人均阅读时长 / 次月留存 | 常规消费指标 | 衡量供给与质量是否满足"打发时间"诉求 |
| 供给侧 | 作品公开率 | 完成作品中用户选择公开的比例 | 公共池的内容来源充足度；过低则消费侧无米下锅 |

> **为何不用总阅读时长作为消费侧核心指标**：它会被"用户反复读自己的作品"污染，无法区分双环真伪。必须单独度量"读他人作品"这一行为。

### 7.2 护栏（不可被收益抵消）

| 护栏 | 说明 |
|---|---|
| **H1 声纹不趋同** | 任何成本优化不得以合并角色采样为代价 |
| **H2 信息不泄露** | 任何上下文压缩不得以取消信息集裁剪为代价 |
| **H3 场景规则不被绕过** | 表演层不得产出改变剧本/场景规则既定结果的剧情 |
| **H4 关键节点有人** | 不得为提升完成率而自动通过关键节点 |
| **H5 成本可预期** | 用户不得遭遇未预期的成本消耗 |
| **H6 零门槛不被侵蚀** | 不得为提升生成质量而要求用户填写结构化字段、审核章级剧本或使用专业术语 |
| **H7 公开出于自愿**（v3.2） | 不得为充实公共内容池而以默认勾选、奖励诱导、流程障碍等方式促使用户公开作品；公开须为逐部显式授权（FR-7.7） |

> **H1–H7 为产品级不变量，与 Plan §1 的架构守卫测试 G-1~G-13 属于不同层级**：前者约束产品行为（不可被收益抵消），后者是代码级断言。两者编号体系独立，勿混用。
>
> **对应关系**：H6 由 Plan 的 **G-7**（主路径只收自由文本）落地为代码断言；H1 由 **G-1** 落地；H2 由 **G-2/G-3** 落地；**H7 由 G-13**（公开授权无默认勾选态、无批量入口）落地。产品护栏若无对应守卫，则只是口头承诺——新增护栏时须同步新增守卫。

---

## 8. 非目标

阶段一明确**不做**以下事项：

| 非目标 | 原因 |
|---|---|
| 英文输出与西方场景包建设 | 阶段二事项，见 §1.3 |
| 锁死固定品类（如只做玄幻） | 引擎领域无关，用户自建任意场景 |
| 把章级审批作为必经流程 | 违反 R-0.2.1 与 H6；专业模式可选提供 |
| 多人协作编辑 | 与"人只指导方向"冲突 |
| 社区、评论、社交互动 | 分散核心能力验证；**转正期亦不做**——分发为单向（FR-7.8），不含任何互动形态 |
| 面向专业作者的设定编辑器 | C 端定位下为可选的专业模式，不作为主路径投入 |
| 短篇 / 中篇优化 | 与 30 万字目标不同，会诱导架构妥协 |
| 多模态（插图、有声） | 与核心能力无关 |
| 面向国内市场的部署与合规 | 发布地在海外 |
| 通用写作助手能力（润色、改写、翻译） | 产品边界外 |
| **原生 App（仅阶段一不做）** | 上架审核与锚点功能结构性冲突；发版审核拖慢迭代；下载摩擦推高样本成本；IAP 抽成扭曲付费验证。转正期再做（NFR-5.1 / NFR-5.5） |
| **把锚点创作作为长期形态** | 与终局定位冲突；锚点是起量手段，须按 §0.8.4 退出 |
| **作品分发与公共阅读（仅阶段一不做）** | 转正期按 U-13「创作 + 阅读双环」解禁：作品经用户**显式授权公开**（FR-7.7）后进入公共池供阅读（FR-7.8），含榜单与推荐位（FR-7.9）。**分发为单向**——不含评论、社交与 UGC 互动，不投入社区运营 |

---

## 9. 未决项与假设

### 9.1 已收敛的决策

| # | 决策 | 结论 |
|---|---|---|
| U-1 | 首发品类 | **通用场景引擎，用户自建**；首发提供中文网文场景包作为 starter，不锁死品类 |
| U-2 | 模型选型 | **开源模型为主，DeepSeek 系列**（V3 默认，R1 用于关键拍）；支持自托管 |
| U-3 | 代码处置 | **fork `novelMaker` 作为参考基座**；与 Regent 内核合并为同一系统（Tech-Spec §9） |
| U-9 | 产品形态 | **面向公众的 C 端网站**，用户只做三个动作（§0.2）；专业模式为可选路径 |
| **U-13** | **终局定位** | **创作 + 阅读双环**（2026-09-01 收敛）：阶段一为创作工具，转正期增加「作品可选公开 → 公共内容池」接消费侧。**不做纯消费产品**——其单位经济在算术上不成立 |

> **U-13 决策依据（v3.2 存档）**
>
> | 维度 | 创作工具 | 消费产品 |
> |---|---|---|
> | 用户来做什么 | 产出属于自己的作品 | 消费内容、打发时间 |
> | 竞争对手 | Sudowrite / NovelAI | 起点 / 番茄 / Webnovel |
> | 北极星 | 完成率 | 阅读时长 / 留存 |
> | **质量门槛** | 低——禀赋效应，用户对"自己参与创作的"容忍度高 | **极高**——与人类作家数百万本存量竞争，换书成本为 0 |
> | **供给算术** | 1 用户 = 1 部作品，自给自足 | 1 个重度读者月耗约 3 部（500 字/分钟 × 1 小时/天），**供给缺口 3–5 倍** |
> | **单位经济** | 月成本 ≈¥60，可收 ≈¥150 → **成立** | 月成本 ≈¥180，市场可收 ≈¥30 → **亏约 6 倍** |
> | FR-7.x 专属性 | 核心卖点 | **反而成为成本负担** |
>
> 成本基数取 NFR-1.1（¥40–85/部）中位 ¥60；市场价按起点千字 3–5 分估算，番茄等免费模式更低。**消费产品的价格锚由人类作家的边际成本（≈0）定死，AI 的每次生成无法与之竞争**——除非放弃专属性、让多人复用同一份已生成内容。
>
> **不做纯消费产品的三条理由**：
> 1. **算术不成立**：保持 FR-7.x 专属性意味着每人读的内容都要单独生成，成本无法摊薄（¥180 vs ¥30）。
> 2. **验证断层**：创作工具阶段验证不了消费产品的任何一条核心假设——用户是否愿读 AI 内容、质量是否达"不换书"门槛、成本能否被覆盖。这与 A2.9 同构：**只验证创作工具，消费产品的核心假设是被绕过而非被验证**。
> 3. **差异化丧失**：若为摊薄成本而放弃专属性，产品即退化为网文平台，与 Novel Engine 的核心主张冲突。
>
> **双环为何能同时成立**：**已生成内容的复用边际成本为零**。作品默认私有、可选公开（FR-7.7），公开后进入公共池供阅读（FR-7.8），N 个创作者的作品可供 N×k 个读者消费而不再产生生成成本。代价是三处调整：FR-7.2 由"默认私有"细化出"显式授权可公开"、§8 非目标解禁**单向**分发、需建设质量筛选（FR-7.9）与推荐位。
>
> **必须提防的陷阱**：若用户主要仍读自己写的作品，则本质仍是"创作工具 + 阅读器"而非消费产品。故转正期须单独度量**「用户读他人作品」这一行为**，而非总阅读时长——后者会被"读自己的作品"污染（§7.1 消费侧指标）。

### 9.2 其他未决项

| # | 未决项 | 阻塞 |
|---|---|---|
| U-4 | 阶段一目标地区与用户规模 | M1（Reader Council 画像） |
| U-5 | 定价模型与付费方式 | M4 |
| U-6 | 内容分级标准与敏感内容过滤方案 | M2 |
| U-7 | 海外中文市场的规模与平台分布（需外部数据验证） | M1（市场判断） |
| U-8 | 阶段一场景包范围（仅中文网文，还是同时提供 1–2 个跨品类 starter 以验证领域无关性） | M0（场景包设计） |
| U-10 | 引导层追问轮次与候选数量的最优值（初始假设 ≤3 轮 / 2–3 候选） | M2（用户测试标定） |
| U-11 | `CriticalPath` 节点数量上限（初始假设 10–20） | M1（剧本展开质量实测） |
| U-12 | 免费额度的具体规模（NFR-1.6 的量化） | M2 |
| **U-14** | 锚点入口退出的三条量化阈值初值（B 路启动率 ≥ A 路 80% / B 路首章评分 ≥ A 路 / A 路用户二次创作选 B 路 ≥ 40%） | M3（标定） |
| **U-15** | **锚点作品范围**：仅公版 / 已授权作品，还是允许在世作品但严格限在结构层 | **M2（阻塞锚点功能设计与合规准备）** |
| **U-16** | **他人作品阅读占比的达标阈值**（AT.6 / §7.1.1）：读到多少比例才算双环真正成立 | **M-T（转正判定的前置标定）** |

> **U-8 的收敛方法（v3.1 补充）**：锚点路径提供了比人工争论更好的收敛方式——用 `structure_extractor` 对玄幻 / 都市 / 悬疑 / 言情等不同品类的源作品各抽一遍，**若抽取器不加品类特定代码即可处理，FR-1.11 领域无关性即为实证**，U-8 可随之收敛（不必再纠结选哪几个 starter 包）。见 Tech-Spec T-11。

> **U-15 说明（v3.1 新增，商业判断非技术判断）**：用户已明确"测试阶段可以仿写任何小说"，即倾向后者。但两种选择的影响差异很大，须显式记录以便事后追溯决策依据：
>
> | 方案 | 吸引力 | 风险 | 说明 |
> |---|---|---|---|
> | **A. 仅公版 / 已授权作品** | 低——公版作品热度远低于当代爆款，用户"想写像《X》"的动机大部分落空 | 低 | 锚点库的吸引力直接决定起量效果，此方案可能使锚点路径失去意义 |
> | **B. 允许在世作品，严格限在结构层**（用户倾向） | 高——锚点即用户真实阅读过的爆款 | 中高 | 依赖 R-0.8.1 与变异门（FR-0.13）两条硬约束真正生效；一旦抽取泄漏内容或变异不足，即构成实质相似 |
>
> **若采纳 B，以下为不可退让的执行前提**（否则应退回 A）：
> 1. `structure_extractor` 的专名过滤与实体泛化必须为**确定性代码**，不依赖 prompt 自觉（守卫 G-10）。
> 2. 变异门阈值（与源作品结构相似度 < 0.45）为**硬阻断**，不得为提升生成质量而放宽。
> 3. 合规留痕（FR-0.14 / NFR-3.7）须在锚点功能上线时即具备，而非事后补齐——这是被质疑时唯一可举证的物料。
> 4. 保留快速切回方案 A 的能力（锚点库按授权状态标记，可整体下线未授权部分）。

### 9.3 关键假设（须在对应里程碑前验证）

| # | 假设 | 验证时点 | 若假设不成立的后果 |
|---|---|---|---|
| AS-1 | 海外中文用户对长篇 AI 生成内容存在付费意愿 | M2 | 商业模式需重估，产品可能转为工具定位 |
| AS-2 | 中文长篇创作可由 DeepSeek 等开源模型稳定完成 | M1 | 需评估闭源模型做关键拍混合，成本与合规路径重估 |
| AS-3 | 现有中文网文知识资产可直接用于海外中文读者 | M1 | 需重建知识库，阶段一成本上升 |
| AS-4 | **普通用户能仅凭自然语言表达出足以生成可用设定的初始目标** | M2 | 若仅锚点路径成立而自由表达路径不成立，则产品须长期依赖锚点，退化为"衍生创作工具"，个性化与合规性双重受限（见 AS-6） |
| AS-5 | **10–20 个关键节点的粒度足以让用户感到"故事是我的"** | M2 | 需增加中间粒度（如卷级摘要审核），介入频次上升，A1.4 面临压力 |
| **AS-6** | **锚点入口可被退出**：独立 AI 创作能力能承接全部需求（三条退出条件，§0.8.4） | **M3** | 产品为锚点依赖型，须按 §0.8.5 转为衍生创作平台并寻求正版授权，而非强行摘除入口 |

> AS-4 与 AS-5 是 v3.0 新增的**产品级核心假设**，其风险高于技术假设——它们决定 C 端定位是否成立。

> **v3.1 对 AS-4 的重要修订（验证方式，非风险等级）**：锚点入口（FR-0.10）显著降低了冷启动难度，但**它不是 AS-4 的替代验证**。AS-4 问的是"用户能否凭自然语言表达出可用目标"——只有 B 路（自由表达）的数据能回答这个问题。
> **若 M2 的外部用户测试只跑锚点路径，AS-4 就不是被证伪，而是被绕过。** 这是 v3.1 最需要警惕的测试设计陷阱，已写入 A2.9 作为不可妥协项。

---

## 10. 停止投资条件

出现以下任一情形，应停止当前路线并重新评估：

1. **M1 未通过**：A1.1–A1.4 任一项未达标，且经两轮针对性改进后仍不达标。
2. **M2 未通过**：外部用户无法在无协助情况下完成首个作品（A2.1/A2.2 不达标），且经两轮引导层改进后仍不达标。
3. **人的介入频次持续超标**：说明"人只指导方向"的承诺未达成，产品退化为协同编辑工具。
4. **成本不可控**：实测单部成本持续超过 NFR-1.1 目标的两倍，且优化空间已穷尽。
5. **AS-1 被证伪**：目标用户无付费意愿，且工具化转型亦不成立。
6. **AS-6 被证伪且不接受转型（v3.1 新增）**：M3 结束时三条退出条件均未满足，产品为锚点依赖型，且不采纳 §0.8.5 的衍生创作平台 + 正版授权路径。
7. **合规被迫中断（v3.1 新增）**：因锚点创作收到下架通知、支付渠道关停或法律主张。此时须立即执行 §0.8.4 退出，**不得因用户量未达标而拖延**——被动切换的代价高于主动切换。

---

## 附录 A：需求与 Regent 的关系

本系统是 **Regent 多 Agent 内核与 novelMaker 世界推演参考实现的统一需求**。

- **内核同源**：Regent 的 Hive 多 Agent 编排、hub-and-spoke 纪律、ExecutionPermit、行为监测/修复环，**即是本引擎的治理内核**。本引擎不是"借鉴"Regent，而是与 Regent 共享同一套原语，仅实例化场景不同（小说/世界 vs 经济实体运营）。
- **对象层对应**：`StoryGoal` ↔ Regent `GoalSpec`；`CriticalPath` ↔ Regent `Plan` 的关键节点序列；关键节点裁决 ↔ `ExecutionPermit`。
- **参考基座**：`novelMaker` 作为 fork 后的参考实现，贡献世界推演/演员模型的具体链路（Tech-Spec §8/§9）。
- **文档分工**：`Regent-PRD.md` 转为上游架构参考，本三件套是"场景推演引擎 + C 端创作网站"统一需求的权威源。冲突时以"同一内核、不同实例化"原则调和，必要时写 DecisionRecord。

## 附录 B：修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-31 | v1.0 | 首版建立。确立演员模型范式；明确阶段一为海外中文读者、阶段二为英语市场；确立"不过早切换模板"策略约定与可插拔架构硬约束 |
| 2026-08-31 | v1.1 | 文档体检修正：§4.7 优先级汇总由误写的 32/16/3=51 修正为 30/15/2（共 47 条 FR）；产品护栏由 G1–G5 重命名为 H1–H5，规避与 Plan 架构守卫的命名碰撞；§0.1 增补「非自由生成」消歧 |
| 2026-08-31 | v2.0 | 重大迭代：① 产品重定位为领域无关的通用场景推演引擎（用户自建任意场景）；② 模型策略改为开源为主（DeepSeek 系列），成本模型重算（¥40–85/部，NFR-1.1 ≤¥100）；③ Regent 与 novelMaker 合并为同一需求（内核同源而非借鉴）；④ 新增场景无关 + 场景包机制两项 P0；⑤ U-1/U-2/U-3 收敛 |
| 2026-08-31 | v3.0 | 重大迭代（C 端定位）：① 定位修正为**面向公众的 C 端创作网站**，确立三动作产品契约与硬约束 R-0.2.1；② 新增**引导层 FR-0.x**（9 条），落地「任何人都能用」——自然语言输入、澄清追问上限、候选选择、结构化字段默认折叠；③ 新增 **`StoryGoal`** 与 **`CriticalPath`** 两个一等对象，「调整关键路径」自此有明确落点，章级剧本不再作为审核前置（FR-1.10、FR-4.5 语义反转）；④ 新增**专属性需求 FR-7.x**（5 条：多样性、私有、版权、可导出、不用于训练）；⑤ C 端能力由「外壳/P1」升为**本体/P0**（7 项升级，新增站外通知 FR-6.6、在线阅读 FR-6.7）；⑥ **里程碑顺序调整**：M2 C 端可用先于 M3 长篇能力（理由见 §6.0），M2 为产品成败闸口；⑦ 北极星改为**双指标**（首作品启动率 + 完成率）；⑧ 新增护栏 **H6 零门槛不被侵蚀**；⑨ 新增 NFR-1.6 免费额度、NFR-2.5 首章时延；⑩ 新增假设 AS-4/AS-5 与未决项 U-10/U-11/U-12 |
| 2026-08-31 | v3.1 | 重大迭代（分阶段策略）：① 新增 **§0.8 分阶段策略**——阶段一引入**锚点创作**（`AnchorWork`）作为起量手段，转正期退出，回归纯 AI 自由创作；确立 **R-0.8.1**（锚点只贡献结构不贡献内容）；② 正视并显式承担**路径依赖悖论**（锚点越成功越难取消），确立三项退出机制：分路径埋点、主动迁移引导、三条量化退出条件；③ 给出**退出失败的备选路径**（转为衍生创作平台 + 正版授权）；④ 新增 **FR-0.10–0.15**（锚点选择/结构抽取/内容隔离/强制变异/合规留痕/分路径统计）与 **FR-7.6**（同源锚点判定池），FR 总数 72 → **79**（P0=63/P1=14/P2=2，已 grep 重数）；⑤ 交付形态定为**阶段一 Web PWA、转正期原生 App**，新增 **NFR-3.7** 与 **NFR-5.1–5.5**（含五条不做 App 的理由，首条为上架审核与锚点功能的结构性冲突）；⑥ 新增 **§6.0.1 阶段划分表**与 **A2.9/A2.10**（A2.9 为不可妥协项：外部测试必须 A/B 双路径分组，否则 AS-4 是被绕过而非被证伪）；⑦ 新增 **AS-6 锚点可退出性**并修订 AS-4 的验证方式；⑧ 新增 **U-13**（终局定位是创作工具还是阅读消费产品，**阻塞转正期规划，待用户确认**）与 **U-14**（退出阈值标定）；⑨ 停止投资条件新增第 6、7 条；⑩ §8 非目标新增「原生 App（阶段一不做）」「锚点创作作为长期形态」 |
| 2026-08-31 | v3.1.1 | 文档体检与补录：① 新增 **U-15**（锚点作品范围：公版/已授权 vs 允许在世作品但严格限在结构层），附 A/B 双方案对比与采纳 B 时的四条不可退让执行前提——此项原仅在对话中提出，未落文档，属遗漏补录；② 补 **U-8 的收敛方法**（用 `structure_extractor` 跨品类抽取作为 FR-1.11 领域无关性的实证，链接 Tech-Spec T-11）；③ 修复 U-8 说明块插在表格行中间导致的 **Markdown 表格渲染中断**（U-10 之后各行丢失表头）；④ 全量重数 FR 与分节分布，确认 63/14/2 = 79 且分节 15/15/7/8/8/8/12/6 与 §4.8 表格一致 |
| 2026-09-01 | v3.2 | 重大迭代（**U-13 收敛：终局定位为创作 + 阅读双环**）：① §9.1 收录 U-13 决策结论与完整依据——补入此前未计入的三个维度（质量门槛差一个数量级 / 供给缺口 3–5 倍 / **单位经济亏约 6 倍**），并给出不做纯消费产品的三条理由；② §4.7 新增 **FR-7.7 公开授权可撤回 / FR-7.8 公共内容池 / FR-7.9 质量门槛 / FR-7.10 阅读不触发生成**，FR 总数 79 → **83（P0=66/P1=15/P2=2，已 grep 重数）**；③ §7.1 新增 **§7.1.1 转正期消费侧指标**，核心指标为**「他人作品阅读占比」**而非总阅读时长（后者会被读自己的作品污染）；④ §7.2 新增护栏 **H7 公开出于自愿**；⑤ §8 非目标将「社区/榜单/作品分发」修订为「作品分发与公共阅读（仅阶段一不做）」，明确**分发单向**、不含评论与社交互动；⑥ §6.0.1 阶段表新增**消费入口**列，确立**锚点移除 / 公共池启用 / App 发布三时点重合**；⑦ **§6 补齐 M-T 验收标准 AT.1–AT.6**（v3.1 遗漏，此前 PRD 无此节导致 Plan 悬空引用）；⑧ 新增未决项 **U-16**（他人作品阅读占比阈值）；⑨ §0.1 补充终局形态说明；⑩ 修正 §7.2 中"G-1~G-9"的编号漂移（实为 G-1~G-13） |
