# Novel Engine 产品需求文档

> 版本：v4.2
>
> 更新：2026-09-03
>
> 状态：ACTIVE — 当前产品需求唯一权威源

## 1. 产品定义

Novel Engine 是面向中国大陆非头部兼职网文作者的“小说导演”产品。它通过持续 Agent loop 把用户的构思变成可持续连载的原创网文。

系统持续执行规划、角色推演、成文、检查、局部修复和续写；用户只在高杠杆位置介入：

1. 说出故事目标；
2. 调整主线关键路径；
3. 裁决不可逆或高影响剧情节点。

复杂小说不是一次模型调用或一次 Agent 输出。Agent loop 是主核心流程；Hive 不是候选架构，也不是待验证功能。只有某个局部步骤同时需要**信息隔离**与**多线程处理**时才启用 Hive，其余步骤一律由 Agent loop 顺序推进。

### 1.1 核心价值

帮助已有创作经验但时间不足、开坑多于完本的兼职作者，以更少维护工作同时稳定推进多部原创作品。

对用户承诺：更多、更稳、能持续推进；不承诺写得超过头部作者、一键生成整书、通过外部平台 AI 检测或必然获得收益。

### 1.2 阶段划分

| 阶段 | 范围 | 明确不做 |
|---|---|---|
| MVP | 原创创作、私有作品、定向试读、首章至首卷 | 公共池、广告分成、锚点仿写、原生 App、专业编辑器 |
| 长篇认证 | 20→50→100→150 章逐级认证 | 未通过前不宣称 30 万字能力 |
| 双环转正 | 用户显式公开、公共阅读、质量筛选 | 评论社区、阅读触发生成 |
| 商业化 | 免费阅读、广告收入、成本回收后分成 | 收益噱头、无限生成、锚点变现 |

锚点仿写不进入 MVP；是否重新启用属于后续独立决策，必须同时满足数据合法性、结构隔离、复现检测和不可公开变现约束。

## 2. 用户与任务

### 2.1 目标用户

必须同时满足：

- 在网文平台有真实创作行为；
- 位于长尾且没有稳定读者群或成熟作品资产；
- 有题材、套路或开篇想法，但时间不足导致开坑多、完本少；
- 接受 AI 显著标识和产品内定向试读；
- 不以批量灌水、规避平台规则或套利为目标。

### 2.2 核心 JTBD

- 当我有题材或梗但产能不稳定时，让我用少量方向决策启动并持续推进作品。
- 当人物泄密、设定冲突或剧情跑偏时，让系统自动发现并局部修复，只把高影响选择交给我。
- 当我同时推进多部作品时，让我知道每部书的状态、下一步、待裁决事项和真实成本。
- 当作品可读时，让受邀读者试读，并用真实阅读行为验证作品价值。

### 2.3 非目标用户

- 需要逐句精修、全文编辑器或完整 Story Bible 工作台的专业作者；
- 要把 AI 内容伪装成人工内容发布到限制平台的人；
- 批量生成、刷量或收益套利者；
- 对 AI 参与创作或显著标识不可接受的人。

## 3. 产品原则

### 3.1 三动作契约

主路径必填字段只有自然语言故事目标。用户不维护 `Canon`（作品事实库：已确认的人物关系、时间、地点、设定，只追加不修改）、`ScenarioSpec`、角色知识图、场景卡、prompt 或 token 参数。

用户可以“报错”，但不进入逐句改稿：

- 事实错误：关系、时间、地点、数值、事件状态，可提交纠正；系统显示影响范围后局部重演。
- 审美意见：例如“太平淡”，必须回落到“调整后续走向”或“留到下个裁决点”，不得只显示拒绝。

### 3.2 持续 loop

- 页面关闭、换设备或网络中断不等于任务停止。
- 人工等待是正常状态，释放执行资源但保留作品运行态。
- 恢复不得重复模型调用、重复计费、重复裁决或生成重复章节。
- 每次修改先展示受影响章节、预计时间区间和费用上限。

### 3.3 角色知识边界

角色不得使用其在当前时间点尚未获知的信息。未来事实、缺席事件和私密事实泄露属于严重错误，必须阻断正文接受并触发最小范围重演。

### 3.4 移动端等权

360×640 视口下必须完成目标输入、关键路径调整和重大裁决。桌面端可提高信息密度，但不得拥有移动端没有的核心创作能力。

## 4. MVP 用户旅程

1. 用户进入作品工作台，先看到待我裁决、进行中、已暂停、已完成作品。
2. 提交一句话或一段构思；首次提交应在 60 秒内完成。
3. 系统最多澄清 1 轮、最多 3 个问题；信息不足时给出可编辑默认假设。
4. 系统给出 2–3 张方向卡，说明主角欲望、核心冲突、题材承诺和节奏差异。
5. 用户选择方向并调整 10–20 个主线节点；移动端提供上移、下移、替换、增、删、锁，不以拖拽为唯一手段。
6. 系统立即进入作品页并后台运行。先展示已锁定方向和路径预览，再生成可读正文。
7. 运行页展示当前阶段、最近完成结果、下一里程碑、预计时间区间、是否需要用户；不展示虚假精确百分比。
8. 仅在死亡、背叛、身份揭露、开战或主线改变等节点挂起。
9. 裁决卡解释发生了什么、为何必须由人决定、2–3 个互斥选项、未来 1–3 章影响、默认项、截止时间和可逆性。
10. 正文完成后进入沉浸阅读；章末仅提供继续运行、调整后续路径、稍后三个主动作。
11. 首卷结束展示本卷兑现、未解悬念和下卷方向，允许调整未固化节点。
12. 用户可邀请指定读者试读；公共发布与收益不属于 MVP。

## 5. 页面级需求

| 路由 | 唯一主任务 | 核心要求 | 禁止元素 |
|---|---|---|---|
| `/` | 落地并分流 | 未登录进介绍页，已登录重定向到 `/works` | 内部运维入口、未登录直入控制台 |
| `/works` | 管理多部作品 | 四栏状态、跨作品待裁决、继续入口 | Agent/工具日志 |
| `/create` | 提交并确认方向 | 自由文本、有限澄清、方向卡 | 结构化设定表 |
| `/works/:id/path` | 调整关键路径 | 版本、影响范围、移动端全功能 | 章级剧本、逐句正文 |
| `/works/:id/progress` | 理解后台进度 | 用户态、最近结果、ETA 区间、安全离开提示 | token、内部 step、无依据百分比 |
| `/works/:id/decisions/:decisionId` | 完成重大裁决 | 后果、默认项、截止时间、二次确认 | 技术日志 |
| `/works/:id/read/:chapter` | 阅读章节 | 目录、主题、位置同步、AI 标识 | 生成、改写按钮 |
| `/works/:id/share` | 管理定向试读 | 邀请、失效、撤回、范围说明 | 默认公开 |
| `/works/:id/export` | 导出 | 目的地告知、格式白名单、AI 标识 | 去标识、过审话术 |

现有 Regent 控制台属于内部运维界面，不得换皮作为用户端。

### 5.1 通用状态

每页必须覆盖：初始、加载、刷新、空、部分完成、成功、离线、陈旧、可恢复失败、致命失败、无权限、资源已撤回/过期。

### 5.2 无障碍

达到 WCAG 2.2 AA；键盘顺序、可见焦点、语义化标题和对话框、44×44 触控目标、非颜色唯一信号、动态进度 `aria-live`、减少动画均为验收项。

## 6. 功能需求

### P0 — MVP 必须具备

| ID | 需求 |
|---|---|
| FR-01 | 自然语言目标输入，首次提交必填仅目标文本 |
| FR-02 | 最多 1 轮澄清，每轮最多 3 问，允许默认假设继续 |
| FR-03 | 2–3 个可复述差异的故事方向候选 |
| FR-04 | 10–20 节点关键路径，支持改/插/删/锁/调序 |
| FR-05 | 路径修改显示影响章节、ETA 和费用上限，并使用版本冲突保护 |
| FR-06 | 持续 Agent loop 支持暂停、恢复、重试和局部重演 |
| FR-07 | 每个角色使用时间点信息集独立表演 |
| FR-08 | 导演先执行确定性硬规则，再执行软质量判断 |
| FR-09 | 模型不能直接修改 Canon；须 extract→verify→append-only commit |
| FR-10 | 重大节点裁决支持深链、截止时间、作品级默认策略和回退说明 |
| FR-11 | 事实“报错”入口与审美意见的可行动回落路径 |
| FR-12 | 多作品工作台与持久待裁决收件箱 |
| FR-13 | 进度、最近产物、下一里程碑、ETA 区间和恢复信息 |
| FR-14 | 手机端三动作完整可用 |
| FR-15 | 章节阅读、阅读位置、AI 显著标识 |
| FR-16 | 私有作品、owner 隔离、完整内容导出 |
| FR-17 | 定向分享、可撤回、不可被搜索引擎索引 |
| FR-18 | 模型调用前预算预留，失败释放，成功按实际成本结算 |
| FR-19 | 每笔成本可归因至作品、章、节拍、模型调用和资金来源 |
| FR-20 | 任意断点恢复不重复调用、计费、裁决或副作用 |
| FR-21 | 所有模型、prompt、sampling、上下文快照和版本可追溯 |
| FR-22 | 阅读、分享和导出不得触发生成 |
| FR-23 | 首次导出前显式告知产出去向；导出物带 AI 标识 |
| FR-24 | 服务端认证与资源授权，不采信客户端传入 actor |
| FR-25 | 国内内容审核、投诉、删除和误判申诉入口 |

### Next — 长篇认证后

- 50/100/150 章能力逐档开放；
- 定向试读数据驱动的作品质量晋级；
- 系列作品共享世界观的受控能力；
- 用户显式开启的单设备离线阅读；
- 公共池、收藏、追更和只读推荐。

### Later — 双环转正后

- 用户显式公开、公共阅读与质量筛选之外的晋级能力；
- 授权前提下的锚点或衍生创作；
- 其他 IP 形态。

### 商业化 — 独立于双环转正的第四阶段

与 §1.2 阶段划分一致，**不得与双环转正合并验收**（先证明有人持续读，再证明能赚钱）：

- 广告、打赏、作品账本、先回收后分成；
- 原生 App，仅在 Web 验证通过且渠道收益明确时另行立项；
- 海外版本。

## 7. 非功能需求

| 领域 | 要求 |
|---|---|
| 首次方向 | 目标用户提交目标 P95 ≤60 秒；方向卡可复述率 ≥80% |
| 方向预览 | 提交后 P95 ≤15 分钟；不得称作“首章完成” |
| 正文 SLA | 首章正文 P50/P95 经真实模型试运行后冻结 |
| 恢复 | 任意步骤杀进程后恢复，无重复模型调用和副作用 |
| 金额 | 整数最小货币单位 + ISO 4217 币种，不使用浮点 |
| 并发 | 同一作品使用乐观版本；同一裁决只允许一个有效结果 |
| 事件 | 持久序列、断线补帧、缺口检测和快照重同步 |
| 隐私 | 私有正文默认不进共享 Cache Storage；退出、撤回和删除触发本地清理 |
| 安全 | 所有资源按服务端主体过滤；越权查询返回不泄露存在性的结果 |
| 可访问性 | WCAG 2.2 AA，严重/致命 axe 问题为 0 |
| 合规 | 上线前确认生成式 AI/算法备案责任、ICP/网络出版适用性及内容审核责任 |

## 8. 质量与评测

小说质量不是单一分数，由三轴共同构成：

1. **硬一致性**：信息泄露、Canon、时间、空间、人物状态和规则；
2. **目标作者接受度**：方向控制感、人工介入负担、局部修复满意度；
3. **目标读者持续阅读**：章完读、D1/D7 续读、第 3/10 章到达。

### 8.1 硬门槛

- 严重角色知识泄露和严重 Canon 冲突原则上零容忍；
- 重复外部副作用为零；
- 评测必须返回证据片段；无证据时自动评审应 abstain；
- 自动评价器必须与双盲人评校准，单一 LLM Judge 不得作为发布闸门；
- 20/50/100/150 章分别认证，不允许从短篇结果外推。

### 8.2 数据集

- `NE-Seed-zh-v1`：按 D-04 确定的题材数量（1–2 个），每题材至少 30 个原创 seed；
- `NE-CanonTrap-v1`：至少 500 个事实、时间、空间、状态、物品和规则探针；
- `NE-InfoBoundary-v1`：至少 300 个场景，覆盖未来、缺席、私密三类秘密；
- `NE-Voice-v1`：12 角色、每角色至少 40 段匿名盲测；
- `NE-LongArc-v1`：20/50/100/150 章冻结升级集；
- `NE-Recovery-v1`：超时、崩溃、重复投递、改意和冲突；
- `NE-HumanPref-v1`：目标作者与目标读者分组盲评。

所有数据只能使用自有、明确授权或公版材料，并记录许可、版本和污染信息。

## 9. 指标与决策闸门

### 9.1 北极星

**双环转正后**：每周至少产生一章被目标试读者有效阅读的活跃作品数。

**MVP 与长篇认证阶段**：尚不存在“目标试读者”这一角色，北极星降级为**每周至少推进一章且通过硬一致性门槛的活跃作品数**；进入 M-T 后切换为上面的完整定义。**不允许在 MVP 阶段用一个没有读者的指标来衡量团队。**

“生成字数”“开书数量”仅是产量信号，不能成为北极星或收益激励依据。

### 9.2 核心漏斗

- 构思提交→方向确认率；
- 方向确认→首章正文可读率；
- 首章完读、D1/D7 续读；
- 第 3/10 章与首卷到达率；
- 作者第二部启动率；
- 首个裁决完成率和错过率；
- 每万字人工打断与改稿诉求率；
- 自动局部修复成功率；
- 每个有效阅读章的成本。

### 9.3 三道决策闸门

1. **导演式交互闸门**：先用 20–30 名目标作者 Wizard-of-Oz 测试澄清、改稿诉求、方案复述、裁决和默认策略接受度。
2. **持续阅读闸门**：在明确 AI 标识的真实条件下验证受邀读者是否持续阅读。
3. **单位经济闸门**：前两项通过后，才建设公共池并验证广告/打赏能否覆盖完整生成与治理成本。

## 10. 待决策

**D-01～D-03 已在正文锁定**，列出仅为备查；**真正待拍板的是 D-04～D-06**。其余参数通过 pilot 标定，不阻塞基础开发。

> 修正说明（v4.1）：v4.0 把 D-01～D-03 与 D-04～D-06 混在同一张“待决策”表里，但 §1.2 与 §3.1 已把前三项写死。**同一件事在两处给出不同状态，读者无法判断该信哪个**，故此处显式标注状态并给出正文出处。

| ID | 状态 | 决策 | 结论 / 建议默认 | 阻塞 |
|---|---|---|---|---|
| D-01 | 已定（§1.2） | MVP 是否取消锚点仿写 | 取消 | 无 |
| D-02 | 已定（§1.2） | MVP 是否仅原创+私有/定向试读 | 是 | 无 |
| D-03 | 已定（§3.1） | 纠错边界 | 事实纠错；不逐句编辑 | 无 |
| D-04 | **待拍板** | 首批题材 | 选择 1–2 个高文笔容忍题材 | 数据集与内容包 |
| D-05 | **待拍板** | 关键裁决超时 | 可逆节点默认继续；高影响节点默认等待 | 状态机 |
| D-06 | **待拍板** | MVP 是否真实收费 | 封闭测试免费，记录影子价格 | 支付与额度 |

上线前必须另行取得法务/合规结论：国内生成式 AI 服务与算法备案责任方、ICP 与网络出版资质适用性、内容审核责任、AI 标识、定向分享规模边界和数据保留策略。

## 11. 竞品吸收边界

| 参照 | 吸收 | 不吸收 |
|---|---|---|
| [Sudowrite Story Bible](https://docs.sudowrite.com/using-sudowrite/1ow1qkGqof9rtcyGnrWUBS/what-is-story-bible/jmWepHcQdJetNrE991fjJC) | 渐进式具体化、共享事实源 | 强制结构化维护 |
| [Novelcrafter](https://www.novelcrafter.com/) | 版本历史、可恢复、系列世界观 | 专业作者工作台作为主界面 |
| [AI Dungeon Memory](https://help.aidungeon.com/faq/the-memory-system) | 分层记忆、按需召回、可解释上下文 | 逐回合自由改写 |
| [Inkitt/Galatea](https://www.inkitt.com/writersblog/how-inkitt-publishes-your-books-from-preparation-to-promotion/) | 真实读者行为作为晋级信号 | 验证前建设完整分发平台 |
| [番茄小说](https://fanqienovel.com/) | 作者后台、连载反馈、反灌水 | 流量规模、广告单价和成本结构直接外推 |

产品差异化不是“大纲+记忆”，而是持续 loop、角色信息隔离、确定性导演、Canon/依赖图局部修复，以及用户只做高杠杆导演动作。

## 12. 当前实现声明

截至 2026-09-02：

- Regent Core 已具备可复用的长任务、lease、timer、会话 checkpoint、人工任务、预算预留、审计和 SSE 连接基础；
- `apps/regent-console` 是通用 Agent 运维控制台；
- Novel Engine 的作品、章节、关键路径、角色信息集、Canon、裁决、小说账本、用户端页面和领域 API 尚未实现；
- 本文描述的是目标产品契约，不表示这些功能已存在。

## 13. 修订记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-02 | v4.0 | 基于最新代码与 UI、UX、产品、前端、后端、AI 科学家复核重新生成；锁定当前用户与 MVP，压缩 P0，补齐页面、状态、恢复、权限、成本、评测和竞品吸收边界；旧版本细节由 Git 历史追溯。 |
| 2026-09-02 | v4.1 | 闭合性复核：D 表区分已定与待拍板（消除与 §1.2/§3.1 的双状态矛盾）；北极星补 MVP 阶段口径；§6 拆出独立商业化档与 §1.2 四阶段对齐；§8.2 题材数量交回 D-04；补 `/` 落地路由；`Canon` 首次出现加解释。 |
| 2026-09-03 | v4.2 | 冻结执行拓扑：Agent loop 为主核心流程；Hive 仅在局部步骤同时需要信息隔离和多线程处理时启用，不作为候选架构、实验变量或待讨论事项。 |
